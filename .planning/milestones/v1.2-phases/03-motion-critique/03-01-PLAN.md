---
phase: 03-motion-critique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PRODUCT.md
  - DESIGN.md
  - .impeccable/design.json
autonomous: true
requirements:
  - CRIT-01

must_haves:
  truths:
    - "PRODUCT.md existe na raiz do repo main com conteúdo não-vazio"
    - "DESIGN.md existe na raiz do repo main com conteúdo não-vazio"
    - ".impeccable/design.json existe com JSON válido contendo tokens de motion"
    - "Um commit existe em main adicionando os 3 arquivos impeccable"
    - "node .agents/skills/impeccable/scripts/load-context.mjs retorna hasProduct: true e hasDesign: true"
  artifacts:
    - path: "PRODUCT.md"
      provides: "Contexto estratégico: público, personalidade, anti-referências"
      min_lines: 10
    - path: "DESIGN.md"
      provides: "Spec A Forja do Arquiteto: paleta, tipografia, motion guidelines"
      min_lines: 10
    - path: ".impeccable/design.json"
      provides: "Tokens de design (easing, duração, cores) em JSON"
      contains: "ease"
  key_links:
    - from: ".impeccable/design.json"
      to: "critique executada na Phase 03-02"
      via: "load-context.mjs lê design.json para montar contexto da critique"
      pattern: "hasDesign.*true"
    - from: "C:/Repo/landing-page/deep-dive-vm/.claude/worktrees/impeccable-teach/"
      to: "C:/Repo/landing-page/deep-dive-vm/"
      via: "cópia direta de arquivo (cp / Copy-Item)"
      pattern: "PRODUCT.md|DESIGN.md|design.json"
---

<objective>
Migrar os 3 arquivos de contexto impeccable (PRODUCT.md, DESIGN.md, .impeccable/design.json) do worktree `impeccable-teach` para o working directory de main e commitá-los.

Purpose: A critique de motion (plano 03-02) exige PRODUCT.md e DESIGN.md em main antes de ser executada. Sem eles, `load-context.mjs` retorna `hasProduct: false` e a critique produz avaliação genérica desconectada da spec "A Forja do Arquiteto".

Output: 3 arquivos presentes em main + 1 commit com mensagem rastreável.
</objective>

<execution_context>
@C:/Repo/landing-page/deep-dive-vm/.agents/skills/impeccable/reference/critique.md
</execution_context>

<context>
@C:/Repo/landing-page/deep-dive-vm/.planning/ROADMAP.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.2-phases/03-motion-critique/03-CONTEXT.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.2-phases/03-motion-critique/03-RESEARCH.md

<interfaces>
<!-- Arquivos fonte que o executor irá copiar. Existem no worktree impeccable-teach como untracked. -->
<!-- NUNCA usar git cherry-pick — não existe commit de origem para esses arquivos. -->

Source paths (worktree):
- C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\PRODUCT.md
- C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\DESIGN.md
- C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\.impeccable\design.json

Destination paths (main working dir):
- C:\Repo\landing-page\deep-dive-vm\PRODUCT.md
- C:\Repo\landing-page\deep-dive-vm\DESIGN.md
- C:\Repo\landing-page\deep-dive-vm\.impeccable\design.json

Verificação pós-migração (load-context.mjs):
  node .agents/skills/impeccable/scripts/load-context.mjs
  → Deve retornar: hasProduct: true, hasDesign: true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copiar os 3 arquivos do worktree impeccable-teach para main</name>
  <files>PRODUCT.md, DESIGN.md, .impeccable/design.json</files>

  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\PRODUCT.md (verificar que existe e tem conteúdo)
    - C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\DESIGN.md (verificar que existe e tem conteúdo)
    - C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\.impeccable\design.json (verificar que é JSON válido)
  </read_first>

  <action>
    Executar no PowerShell a partir de C:\Repo\landing-page\deep-dive-vm:

    1. Verificar existência dos 3 arquivos fonte no worktree:
       Test-Path "C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\PRODUCT.md"
       Test-Path "C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\DESIGN.md"
       Test-Path "C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\.impeccable\design.json"
       Se qualquer um retornar False, parar e reportar o caminho ausente.

    2. Criar o diretório .impeccable no main se não existir:
       New-Item -ItemType Directory -Force -Path "C:\Repo\landing-page\deep-dive-vm\.impeccable"

    3. Copiar PRODUCT.md:
       Copy-Item -Path "C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\PRODUCT.md" `
                 -Destination "C:\Repo\landing-page\deep-dive-vm\PRODUCT.md" -Force

    4. Copiar DESIGN.md:
       Copy-Item -Path "C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\DESIGN.md" `
                 -Destination "C:\Repo\landing-page\deep-dive-vm\DESIGN.md" -Force

    5. Copiar design.json:
       Copy-Item -Path "C:\Repo\landing-page\deep-dive-vm\.claude\worktrees\impeccable-teach\.impeccable\design.json" `
                 -Destination "C:\Repo\landing-page\deep-dive-vm\.impeccable\design.json" -Force

    NÃO usar git cherry-pick. Os arquivos são untracked no worktree — não existe commit de origem (D-01 foi invalidado pela pesquisa, ver 03-RESEARCH.md Pitfall 1).
  </action>

  <verify>
    <automated>
      # Verificar que os 3 arquivos chegaram em main com conteúdo:
      # (executar no PowerShell a partir de C:\Repo\landing-page\deep-dive-vm)
      Test-Path "PRODUCT.md" -and (Get-Content "PRODUCT.md").Length -gt 5
      Test-Path "DESIGN.md" -and (Get-Content "DESIGN.md").Length -gt 5
      Test-Path ".impeccable\design.json" -and (Get-Content ".impeccable\design.json" | ConvertFrom-Json) -ne $null
    </automated>
  </verify>

  <acceptance_criteria>
    - PRODUCT.md existe em C:\Repo\landing-page\deep-dive-vm\PRODUCT.md com conteúdo (não arquivo vazio)
    - DESIGN.md existe em C:\Repo\landing-page\deep-dive-vm\DESIGN.md com conteúdo (não arquivo vazio)
    - .impeccable\design.json existe e é JSON parseável sem erros
    - .impeccable\design.json contém a string "ease" (indica presença dos tokens de motion)
    - Nenhum dos 3 arquivos pré-existia em main (confirmado por git status mostrando ?? ou A antes do commit)
  </acceptance_criteria>

  <done>Os 3 arquivos copiados existem em main com conteúdo válido, prontos para staging.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar contexto impeccable via load-context.mjs e commitar os 3 arquivos</name>
  <files>PRODUCT.md, DESIGN.md, .impeccable/design.json</files>

  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\.agents\skills\impeccable\scripts\load-context.mjs (entender o que o script verifica)
    - C:\Repo\landing-page\deep-dive-vm\PRODUCT.md (confirmar que o arquivo copiado tem conteúdo correto)
    - C:\Repo\landing-page\deep-dive-vm\.impeccable\design.json (confirmar presença dos tokens de easing)
  </read_first>

  <action>
    Executar a partir de C:\Repo\landing-page\deep-dive-vm:

    1. Rodar o script de verificação do contexto impeccable:
       node .agents/skills/impeccable/scripts/load-context.mjs
       O output deve conter: hasProduct: true, hasDesign: true
       Se hasProduct: false ou hasDesign: false, verificar que os arquivos foram copiados corretamente na Task 1 antes de prosseguir.

    2. Fazer staging dos 3 arquivos:
       git add PRODUCT.md DESIGN.md .impeccable/design.json

    3. Confirmar que somente os 3 arquivos esperados estão staged:
       git diff --cached --name-only
       Saída esperada: exatamente PRODUCT.md, DESIGN.md, .impeccable/design.json (nenhum arquivo extra).

    4. Commitar:
       git commit -m "feat(03): add impeccable context — PRODUCT.md, DESIGN.md, .impeccable/design.json"

    O commit em main preserva os arquivos no histórico da branch principal. Não é necessário nenhum outro passo git.
  </action>

  <verify>
    <automated>
      # Verificar que o commit existe e contém os 3 arquivos:
      git show --name-only HEAD
      # Saída deve incluir: PRODUCT.md, DESIGN.md, .impeccable/design.json
      # e mensagem: feat(03): add impeccable context

      # Verificar que load-context.mjs confirma o contexto:
      node .agents/skills/impeccable/scripts/load-context.mjs
      # Deve conter hasProduct: true e hasDesign: true no output
    </automated>
  </verify>

  <acceptance_criteria>
    - git log --oneline -1 mostra commit com mensagem "feat(03): add impeccable context"
    - git show --name-only HEAD lista exatamente: .impeccable/design.json, DESIGN.md, PRODUCT.md
    - node .agents/skills/impeccable/scripts/load-context.mjs retorna hasProduct: true
    - node .agents/skills/impeccable/scripts/load-context.mjs retorna hasDesign: true
    - git status não mostra PRODUCT.md, DESIGN.md ou .impeccable/design.json como untracked ou modified
  </acceptance_criteria>

  <done>Contexto impeccable commitado em main. load-context.mjs confirma hasProduct: true e hasDesign: true. Plano 03-02 pode prosseguir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree → main | Arquivos copiados de um worktree local para o working directory de main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Copy-Item (worktree → main) | accept | Cópia de arquivo local — sem rede, sem entrada de usuário. Origem e destino são caminhos absolutos verificados |
| T-03-02 | Repudiation | git commit | accept | Commit rastreável com mensagem descritiva; git log preserva autoria |
| T-03-03 | Information Disclosure | PRODUCT.md / DESIGN.md | accept | Arquivos de contexto de marca — sem PII, sem secrets, sem credenciais |
</threat_model>

<verification>
Verificação geral da Phase 03-01:

```powershell
# A partir de C:\Repo\landing-page\deep-dive-vm
Test-Path "PRODUCT.md"                    # deve retornar True
Test-Path "DESIGN.md"                     # deve retornar True
Test-Path ".impeccable\design.json"       # deve retornar True
node .agents/skills/impeccable/scripts/load-context.mjs
# deve conter hasProduct: true e hasDesign: true
git log --oneline -1
# deve mostrar "feat(03): add impeccable context"
```
</verification>

<success_criteria>
- Os 3 arquivos (PRODUCT.md, DESIGN.md, .impeccable/design.json) existem em main com conteúdo válido
- Um commit com mensagem "feat(03): add impeccable context" existe no histórico de main
- node .agents/skills/impeccable/scripts/load-context.mjs retorna hasProduct: true e hasDesign: true
- git status limpo para os 3 arquivos (nada de untracked ou modified)
</success_criteria>

<output>
Criar `.planning/milestones/v1.2-phases/03-motion-critique/03-01-SUMMARY.md` quando concluído.
</output>
