---
phase: 03-motion-critique
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/milestones/v1.2-phases/03-motion-critique/03-CRITIQUE.md
autonomous: true
requirements:
  - CRIT-01

must_haves:
  truths:
    - "03-CRITIQUE.md existe no diretório da phase com conteúdo não-vazio"
    - "03-CRITIQUE.md contém tabela com colunas Severidade | Componente | Gap de Motion | ANIM-XX"
    - "Pelo menos um item P0 está presente na tabela (seções sem animação)"
    - "Pelo menos um item P1 está presente na tabela (easing genérico)"
    - "Todos os 5 ANIM-IDs (ANIM-01 a ANIM-05) estão referenciados na tabela"
    - "03-CRITIQUE.md NÃO contém issues de tipografia, cores, layout, contraste ou espaçamento"
    - "A discrepância de easing entre design.json e REQUIREMENTS.md está documentada"
  artifacts:
    - path: ".planning/milestones/v1.2-phases/03-motion-critique/03-CRITIQUE.md"
      provides: "Inventário priorizado P0/P1/P2 de gaps de motion"
      contains: "ANIM-0"
  key_links:
    - from: ".planning/milestones/v1.2-phases/03-motion-critique/03-CRITIQUE.md"
      to: "Phase 4 planner"
      via: "Planner da Phase 4 lê 03-CRITIQUE.md antes de criar plans"
      pattern: "P0|P1|P2"
    - from: "Assessment A (LLM + Playwright)"
      to: "03-CRITIQUE.md"
      via: "Filtro D-07: apenas timing, easing, stagger, scroll-triggers, micro-interações"
      pattern: "easing|stagger|whileInView|scroll"
    - from: "Assessment B (npx impeccable detect)"
      to: "03-CRITIQUE.md"
      via: "Filtro D-07: descartar issues não-motion antes de incluir"
      pattern: "easing|generic|motion"
---

<objective>
Executar a critique de motion da homepage usando dois assessments independentes (LLM review + detector automático), filtrar os resultados para issues exclusivamente de motion, e gerar 03-CRITIQUE.md com tabela P0/P1/P2 mapeada para ANIM-XX.

Purpose: 03-CRITIQUE.md é o único input que guia 100% das decisões de implementação da Phase 4. Sem este artefato, nenhum trabalho de animação tem base priorizada. A critique deve acontecer APÓS a migração do contexto impeccable (plano 03-01) para que o Assessment A opere com a spec "A Forja do Arquiteto" carregada.

Output: .planning/milestones/v1.2-phases/03-motion-critique/03-CRITIQUE.md — inventário estruturado de gaps de motion com severidade, componente, descrição e ID de requirement.
</objective>

<execution_context>
@C:/Repo/landing-page/deep-dive-vm/.agents/skills/impeccable/reference/critique.md
@C:/Repo/landing-page/deep-dive-vm/.agents/skills/impeccable/reference/motion-design.md
@C:/Repo/landing-page/deep-dive-vm/.agents/skills/impeccable/reference/animate.md
</execution_context>

<context>
@C:/Repo/landing-page/deep-dive-vm/.planning/ROADMAP.md
@C:/Repo/landing-page/deep-dive-vm/.planning/REQUIREMENTS.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.2-phases/03-motion-critique/03-CONTEXT.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.2-phases/03-motion-critique/03-RESEARCH.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.2-phases/03-motion-critique/03-01-SUMMARY.md

<interfaces>
<!-- Estado atual de animações — extraído do código-fonte pela pesquisa (03-RESEARCH.md). -->
<!-- O executor NÃO precisa re-escanear o código para descobrir o que existe: use como ponto de partida. -->
<!-- Validar/atualizar com os assessments se necessário. -->

Componentes COM animação (verificar gaps):
  HeroMotion        → src/components/HeroMotion.tsx
                      Estado: whileInView único, duration: 0.15, ease: "easeOut" (genérico)
                      Gaps mapeados: sem stagger (ANIM-01), easing genérico (ANIM-03)

  NavBar            → src/components/layout/NavBar.astro
                      Estado: CSS transition nos links hover
                      Gap: easing genérico (ANIM-03)

  Button            → src/components/ui/Button.astro
                      Estado: CSS hover (background-color, box-shadow, arrow translate 4px)
                      Gap: easing genérico (ANIM-03)

  Pricing           → src/components/sections/Pricing.astro
                      Estado: CSS hover no card
                      Gap: easing genérico (ANIM-03)

  MobileMenuMotion  → src/components/MobileMenuMotion.tsx
                      Estado: motion/react menu slide
                      Gap: verificar se usa easing custom ou genérico

  CarouselMotion    → src/components/CarouselMotion.tsx
                      Estado: motion/react carrossel
                      Gap: verificar timing e easing; testimonials individuais sem stagger

  SettingsToggle    → src/components/SettingsToggle.tsx
                      Estado: SEM animação no toggle em si
                      Gap: ausência de spring no indicador e fade no label (ANIM-04)

Seções SEM animação (alvo ANIM-02 — P0 confirmado):
  Method.astro, Curriculum.astro, Bonuses.astro, Faq.astro,
  ForWho.astro, Mentor.astro, PainPoints.astro, Testimonials.astro

Tokens de easing relevantes (discrepância a ser reportada em 03-CRITIQUE.md):
  design.json token "ease-out-standard" → cubic-bezier(0.0, 0.0, 0.2, 1)
  REQUIREMENTS.md ANIM-03 "ease-out-quart" → cubic-bezier(0.25, 1, 0.5, 1)
  Decisão de qual usar → Claude's Discretion da Phase 4 (não desta phase)

Filtro de escopo obrigatório (D-07/D-08/D-09):
  INCLUIR: timing/duração, easing (genérico vs custom), ausência de stagger,
           ausência de scroll-triggered reveals, micro-interações faltando,
           sequenciamento de entrada, SettingsToggle spring/fade
  IGNORAR: tipografia, cores, layout, espaçamento, contraste, ícones, copy,
           AI slop tells, heuristics Nielsen, cognitive load (fora de escopo de motion)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar dev server e executar Assessment B (npx impeccable detect)</name>
  <files>(nenhum arquivo de produção modificado)</files>

  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\.agents\skills\impeccable\reference\critique.md (seção "Assessment B: Automated Detection")
    - C:\Repo\landing-page\deep-dive-vm\playwright.config.ts (verificar configuração de webServer e reuseExistingServer)
    - C:\Repo\landing-page\deep-dive-vm\package.json (confirmar comando dev e porta 4321)
  </read_first>

  <action>
    Executar a partir de C:\Repo\landing-page\deep-dive-vm:

    PASSO 1 — Verificar dev server:
    Tentar conectar na porta 4321:
      (Invoke-WebRequest -Uri "http://localhost:4321/deep-dive-vm/" -UseBasicParsing).StatusCode
    - Se retornar 200: dev server está ativo, prosseguir para o Passo 2.
    - Se falhar/timeout: iniciar dev server em background:
        Start-Process -NoNewWindow -FilePath "node" -ArgumentList "node_modules/.bin/astro dev" -WorkingDirectory "C:\Repo\landing-page\deep-dive-vm"
      Aguardar ~15 segundos e repetir a verificação.
    - Se ainda falhar: executar `npm run dev` e aguardar a mensagem "Local: http://localhost:4321/"
    Registrar o estado (estava rodando ou foi iniciado agora) para incluir no 03-CRITIQUE.md.

    PASSO 2 — Assessment B: CLI scan com impeccable detect:
      npx impeccable detect --json src/
    O comando retorna JSON com findings. Exit code 0 = clean, 2 = findings encontrados.
    Capturar o output JSON completo para análise no Task 2.

    PASSO 3 — Extrair findings de motion do Assessment B:
    Do JSON retornado por `npx impeccable detect`, aplicar filtro D-07:
    Manter apenas findings cujo campo `rule` ou `message` contenha palavras relacionadas a motion:
      "ease", "easing", "transition", "animation", "motion", "stagger", "duration", "keyframe", "transform", "opacity"
    Descartar findings sobre: "color", "contrast", "typography", "font", "spacing", "margin", "padding", "gradient", "glassmorphism"

    PASSO 4 — Assessment B: Browser overlay (se Playwright disponível):
    Iniciar o servidor de overlay:
      npx impeccable live
    (registrar a porta impressa no output — é diferente da porta 4321 do Astro)
    Abrir nova aba no browser via Playwright em http://localhost:4321/deep-dive-vm/
    Ler output do console do overlay para findings adicionais (NÃO fazer screenshots de scroll).
    Aplicar o mesmo filtro D-07 nos findings do overlay.

    Compilar lista intermediária de findings de motion do Assessment B, separados por componente.
    NÃO escrever 03-CRITIQUE.md ainda — isso acontece no Task 3.
  </action>

  <verify>
    <automated>
      # Verificar que dev server responde:
      (Invoke-WebRequest -Uri "http://localhost:4321/deep-dive-vm/" -UseBasicParsing).StatusCode
      # Deve retornar 200

      # Verificar que impeccable está disponível:
      npx impeccable --version
      # Deve retornar 2.1.9
    </automated>
  </verify>

  <acceptance_criteria>
    - http://localhost:4321/deep-dive-vm/ retorna HTTP 200
    - npx impeccable --version imprime 2.1.9 sem erro
    - npx impeccable detect --json src/ executa sem crash (exit 0 ou 2 são ambos aceitáveis — 2 = findings, não erro)
    - Existe uma lista intermediária de findings de motion do Assessment B (pode ser na memória do executor, usada no Task 3)
    - Findings de tipografia, cores e layout foram explicitamente descartados do Assessment B
  </acceptance_criteria>

  <done>Dev server ativo na 4321. Assessment B executado. Findings de motion extraídos e filtrados. Pronto para Task 2 (Assessment A).</done>
</task>

<task type="auto">
  <name>Task 2: Executar Assessment A (LLM review com Playwright) focado em motion gaps</name>
  <files>(nenhum arquivo de produção modificado)</files>

  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\.agents\skills\impeccable\reference\critique.md (seção "Assessment A: LLM Design Review")
    - C:\Repo\landing-page\deep-dive-vm\.agents\skills\impeccable\reference\motion-design.md (duração, easing, stagger)
    - C:\Repo\landing-page\deep-dive-vm\.agents\skills\impeccable\reference\animate.md (qualidade de animação)
    - C:\Repo\landing-page\deep-dive-vm\DESIGN.md (spec A Forja do Arquiteto — tokens de motion da marca)
    - C:\Repo\landing-page\deep-dive-vm\.impeccable\design.json (tokens: ease-out-standard, duration-standard, etc.)
    - C:\Repo\landing-page\deep-dive-vm\src\components\HeroMotion.tsx (verificar whileInView e easing atual)
    - C:\Repo\landing-page\deep-dive-vm\src\components\SettingsToggle.tsx (confirmar ausência de animação no toggle)
    - C:\Repo\landing-page\deep-dive-vm\src\components\sections\Bonuses.astro (confirmar ausência de scroll reveal)
    - C:\Repo\landing-page\deep-dive-vm\src\lib\motion-utils.ts (padrões reutilizáveis disponíveis)
  </read_first>

  <action>
    Assessment A é uma revisão de design pelo LLM com suporte de inspeção visual via Playwright.
    O escopo é ESTRITAMENTE motion (D-07). NÃO avaliar tipografia, cores, layout, contraste.

    PASSO 1 — Carregar contexto impeccable:
      node .agents/skills/impeccable/scripts/load-context.mjs
    Confirmar: hasProduct: true, hasDesign: true.
    Se falso, parar e reportar — o Task 2 do plano 03-01 não foi concluído corretamente.

    PASSO 2 — Inspeção visual via Playwright (nova aba):
    Abrir http://localhost:4321/deep-dive-vm/ em nova aba (não reutilizar aba do Assessment B).
    Label da aba (per protocolo):
      document.title = '[LLM] ' + document.title
    
    Verificar visualmente e via código cada região da página com foco em motion:
    
    a) Hero section:
       - Abrir src/components/HeroMotion.tsx
       - Verificar se headline, lede e CTA têm delays distintos (100-150ms entre elementos)
       - Verificar o valor do atributo `ease` — é string "easeOut" ou cubic-bezier?
       - Observar na página ao vivo: elementos aparecem simultaneamente ou em stagger?

    b) Seções de conteúdo (scroll reveals):
       - Rolar a página e observar: Method, Curriculum, Bonuses, Faq, ForWho, Mentor, PainPoints, Testimonials
       - Verificar se há qualquer transição de entrada ao scrollar para cada seção
       - Confirmar ausência de whileInView ou IntersectionObserver nessas seções

    c) SettingsToggle:
       - Abrir src/components/SettingsToggle.tsx
       - Localizar o mecanismo de toggle do estado (motionEnabled)
       - Verificar se há motion.div, spring, ou qualquer animação no indicador visual e no label
       - Clicar no toggle na página ao vivo: o switch anima ou pula instantaneamente?

    d) Easing em interações existentes:
       - Verificar Button.astro: quais propriedades CSS têm `transition-timing-function`?
         grep -r "ease" src/components/ui/Button.astro
       - Verificar Pricing.astro: hover do card tem easing custom ou genérico?
         grep -r "ease" src/components/sections/Pricing.astro
       - Verificar MobileMenuMotion.tsx: qual o valor de `ease` nas animações motion/react?
         grep -r "ease" src/components/MobileMenuMotion.tsx
       - Verificar CarouselMotion.tsx: easing e timing das animações de carrossel?
         grep -r "ease\|duration" src/components/CarouselMotion.tsx

    e) Stagger em listas e cards:
       - Verificar Bonuses.astro: há animation-delay ou staggerChildren nos cards de bônus?
       - Verificar Pricing.astro: há stagger nas features do plano?

    PASSO 3 — Identificar oportunidades P2 (motion design assessment):
    Avaliar, tendo como referência o animate.md e motion-design.md:
    - Há sequências de entrada que poderiam ter stagger mas não foram listadas nos requirements?
    - Há interações que se beneficiariam de animação spring vs. duration linear?
    - Há micro-interações (focus rings, form field activation) sem timing custom?
    Registrar como P2 (oportunidade sem requirement direto — D-06).

    PASSO 4 — Documentar discrepância de easing:
    Comparar os dois valores canônicos:
    - design.json `ease-out-standard`: cubic-bezier(0.0, 0.0, 0.2, 1)
    - REQUIREMENTS.md ANIM-03 `ease-out-quart`: cubic-bezier(0.25, 1, 0.5, 1)
    Registrar ambos como P1 na lista de findings do Assessment A. A decisão de qual adotar é da Phase 4 (Claude's Discretion — não resolver aqui).

    Compilar lista de findings do Assessment A com: Severidade (P0/P1/P2), componente, gap, ANIM-XX.
    NÃO escrever 03-CRITIQUE.md ainda — isso acontece no Task 3.
  </action>

  <verify>
    <automated>
      # Verificar contexto impeccable carregado:
      node .agents/skills/impeccable/scripts/load-context.mjs
      # Deve conter hasProduct: true e hasDesign: true

      # Verificar que as seções-alvo existem no código (sanity check antes de afirmar ausência de animação):
      Test-Path "src/components/sections/Method.astro"
      Test-Path "src/components/sections/Curriculum.astro"
      Test-Path "src/components/sections/Bonuses.astro"
      Test-Path "src/components/SettingsToggle.tsx"
    </automated>
  </verify>

  <acceptance_criteria>
    - load-context.mjs confirmou hasProduct: true e hasDesign: true antes do Assessment A
    - HeroMotion.tsx foi lido e o valor atual de `ease` foi confirmado (string "easeOut" ou cubic-bezier)
    - SettingsToggle.tsx foi lido e a ausência de animação no indicador foi confirmada
    - Pelo menos 5 componentes/seções foram inspecionados para presença/ausência de animação
    - A discrepância entre design.json (0.0, 0.0, 0.2, 1) e REQUIREMENTS.md (0.25, 1, 0.5, 1) foi registrada
    - Findings do Assessment A estão organizados por severidade P0/P1/P2 com componente e ANIM-XX
    - Nenhum finding de tipografia, cores, layout ou contraste está na lista do Assessment A
  </acceptance_criteria>

  <done>Assessment A concluído. Lista de findings de motion P0/P1/P2 compilada. Pronto para Task 3 (síntese e escrita do 03-CRITIQUE.md).</done>
</task>

<task type="auto">
  <name>Task 3: Sintetizar assessments, filtrar por motion-only e escrever 03-CRITIQUE.md</name>
  <files>.planning/milestones/v1.2-phases/03-motion-critique/03-CRITIQUE.md</files>

  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\.planning\REQUIREMENTS.md (verificar ANIM-01 a ANIM-05 — cada um deve aparecer na tabela)
    - C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\03-motion-critique\03-CONTEXT.md (D-05, D-06 — formato e severidades exatas)
    - C:\Repo\landing-page\deep-dive-vm\.planning\ROADMAP.md (critérios de sucesso da Phase 3)
  </read_first>

  <action>
    Sintetizar os findings dos dois assessments (Task 1 e Task 2) em um inventário único, deduplicar, e escrever 03-CRITIQUE.md.

    PASSO 1 — Deduplicação e merge:
    Para cada finding que aparece em ambos os assessments, manter uma única entrada com a severidade mais alta.
    Exemplo: se Assessment A e Assessment B ambos reportam "HeroMotion sem stagger", manter P0 uma vez.

    PASSO 2 — Filtro final D-07/D-08/D-09:
    Revisar CADA item merged. Descartar qualquer finding que mencione:
    - tipografia (font, letter-spacing, line-height)
    - cores (color, background, gradient, contrast)
    - layout (margin, padding, gap, spacing, grid, flex)
    - ícones, copy, imagens, acessibilidade não-motion
    Manter APENAS: timing, duração, easing, stagger, scroll-triggered reveals, micro-interações de motion, sequenciamento de entrada.

    PASSO 3 — Verificar cobertura de ANIM-IDs:
    Confirmar que todos os 5 requirements de animação estão cobertos na tabela:
    - ANIM-01: Hero stagger (P0) ← HeroMotion sem sequência de entrada
    - ANIM-02: Scroll reveals (P0) ← 8 seções sem whileInView/IntersectionObserver
    - ANIM-03: Easing tokens (P1) ← "easeOut" genérico em múltiplos componentes + discrepância design.json vs REQUIREMENTS.md
    - ANIM-04: SettingsToggle spring (P0) ← sem animação no toggle
    - ANIM-05: Stagger em listas/cards (P0) ← Bonuses, Pricing feature list sem stagger
    Se algum ANIM-ID não tiver representação, verificar se o assessment o encontrou ou se é gap genuíno.

    PASSO 4 — Escrever 03-CRITIQUE.md:
    Criar o arquivo em .planning/milestones/v1.2-phases/03-motion-critique/03-CRITIQUE.md.

    Estrutura obrigatória (D-05):

    ```markdown
    # Motion Critique: Deep Dive Azure VM

    **Data:** 2026-05-15
    **Alvo:** http://localhost:4321/deep-dive-vm/
    **Escopo:** Motion-only — timing, easing, stagger, scroll-triggers, micro-interações (D-07)
    **Contexto impeccable:** PRODUCT.md + DESIGN.md carregados (hasProduct: true, hasDesign: true)

    ## Inventário de Gaps

    | Severidade | Componente | Gap de Motion | ANIM-XX |
    |-----------|-----------|---------------|---------|
    | P0 | ... | ... | ANIM-XX |
    ...

    ## Nota sobre Discrepância de Easing

    Design.json define `ease-out-standard: cubic-bezier(0.0, 0.0, 0.2, 1)`.
    REQUIREMENTS.md ANIM-03 especifica `ease-out-quart: cubic-bezier(0.25, 1, 0.5, 1)`.
    Ambos são reportados. A decisão de qual adotar é da Phase 4.

    ## Issues Fora de Escopo (descartados por D-09)

    [Listar aqui, resumidamente, o que o detector reportou mas foi descartado por ser non-motion.
     Exemplo: "Detector reportou 3 issues de contraste de texto — descartados (D-08)"]

    ## Metodologia

    - Assessment A: LLM review com inspeção visual via Playwright em http://localhost:4321/deep-dive-vm/
    - Assessment B: npx impeccable detect --json src/ (v2.1.9) + browser overlay
    - Filtro: D-07/D-08/D-09 — apenas gaps de timing, easing, stagger, scroll-triggers e micro-interações

    ## Próximos Passos

    03-CRITIQUE.md é o input direto para Phase 4 (Animation Implementation).
    O planner da Phase 4 DEVE ler este arquivo antes de criar os plans ANIM-XX.
    ```

    Severidades conforme D-06:
    - P0 = ausência de animação onde ANIM-XX a exige explicitamente
    - P1 = animação existe mas com timing/easing incorreto (genérico em vez de token custom)
    - P2 = oportunidade de melhoria sem requirement direto (ex: NavBar link hover easing)

    Organização da tabela: por severidade decrescente (P0 primeiro, P1, P2), dentro de cada severidade por componente.
  </action>

  <verify>
    <automated>
      # Verificar que o arquivo foi criado:
      Test-Path "C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md"

      # Verificar presença dos 5 ANIM-IDs:
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "ANIM-01"
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "ANIM-02"
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "ANIM-03"
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "ANIM-04"
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "ANIM-05"
      # Todos devem retornar match

      # Verificar ausência de issues não-motion:
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "(?i)(color:|typography:|contrast:)" -NotMatch
      # Deve retornar sem match (ou match apenas na seção "Issues Fora de Escopo")

      # Verificar presença de P0 e P1:
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "^\| P0"
      Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "^\| P1"
      # Ambos devem ter matches
    </automated>
  </verify>

  <acceptance_criteria>
    - 03-CRITIQUE.md existe em .planning/milestones/v1.2-phases/03-motion-critique/
    - O arquivo contém tabela markdown com colunas: Severidade | Componente | Gap de Motion | ANIM-XX
    - ANIM-01, ANIM-02, ANIM-03, ANIM-04 e ANIM-05 estão todos referenciados na tabela
    - Pelo menos 1 linha P0 existe (ausência de animação onde requirement exige)
    - Pelo menos 1 linha P1 existe (easing genérico ou timing incorreto)
    - A seção "Nota sobre Discrepância de Easing" documenta ambos os valores: cubic-bezier(0.0, 0.0, 0.2, 1) e cubic-bezier(0.25, 1, 0.5, 1)
    - A seção "Issues Fora de Escopo" lista o que foi descartado por D-09 (mesmo que vazia com "nenhum" se o detector foi clean para non-motion)
    - Nenhuma linha da tabela principal menciona tipografia, cores, contraste, espaçamento ou layout como o gap primário
    - O arquivo menciona que é o input para Phase 4
  </acceptance_criteria>

  <done>03-CRITIQUE.md escrito com inventário P0/P1/P2 cobrindo todos os 5 ANIM-IDs. Phase 3 entregável principal concluído. Phase 4 pode ser planejada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx impeccable (rede) → análise local | npx baixa e executa pacote npm — verificado via Package Legitimacy Audit da RESEARCH.md |
| Playwright → browser local | Automação de browser abre URL local (localhost:4321) — sem dados externos |
| Assessment output → 03-CRITIQUE.md | LLM filtra e escreve arquivo de planejamento — sem input de usuário externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | npx impeccable detect | accept | Package verificado: v2.1.9 no npm registry (Package Legitimacy Audit — RESEARCH.md). Sem instalação permanente via npx |
| T-03-05 | Information Disclosure | 03-CRITIQUE.md | accept | Arquivo de planejamento interno — sem PII, sem credentials, sem dados de usuário |
| T-03-06 | Denial of Service | Dev server porta 4321 | accept | Dev server local; fallback documentado (D-04): iniciar com npm run dev se ausente |
| T-03-SC | Tampering | npx install de impeccable | mitigate | slopcheck: impeccable@2.1.9 verificado como legítimo (RESEARCH.md Package Legitimacy Audit). Executar via npx sem instalação permanente em package.json |
</threat_model>

<verification>
Verificação geral da Phase 03-02:

```powershell
# A partir de C:\Repo\landing-page\deep-dive-vm

# 1. Dev server respondeu:
(Invoke-WebRequest -Uri "http://localhost:4321/deep-dive-vm/" -UseBasicParsing).StatusCode
# Deve retornar 200

# 2. 03-CRITIQUE.md existe:
Test-Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md"
# Deve retornar True

# 3. Todos os 5 ANIM-IDs presentes:
$c = Get-Content ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Raw
@("ANIM-01","ANIM-02","ANIM-03","ANIM-04","ANIM-05") | ForEach-Object { $c -match $_ }
# Deve retornar True True True True True

# 4. Pelo menos um P0 e um P1:
(Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "^\| P0").Count -ge 1
(Select-String -Path ".planning\milestones\v1.2-phases\03-motion-critique\03-CRITIQUE.md" -Pattern "^\| P1").Count -ge 1
# Ambos devem retornar True
```
</verification>

<success_criteria>
- Dev server Astro estava ou foi iniciado na porta 4321 e respondeu HTTP 200
- npx impeccable detect --json src/ executou sem crash e seus findings foram filtrados por D-07
- Assessment A (LLM + Playwright) inspecionou os componentes-chave (HeroMotion, SettingsToggle, 8 seções sem animação)
- 03-CRITIQUE.md existe em .planning/milestones/v1.2-phases/03-motion-critique/ com conteúdo válido
- A tabela do inventário referencia todos os 5 ANIM-IDs (ANIM-01 a ANIM-05)
- A discrepância de easing entre design.json e REQUIREMENTS.md está documentada em 03-CRITIQUE.md
- Nenhum issue de tipografia, cores ou layout entrou na tabela principal (D-09 respeitado)
- Phase 4 tem um input concreto e priorizado para começar a implementação
</success_criteria>

<output>
Criar `.planning/milestones/v1.2-phases/03-motion-critique/03-02-SUMMARY.md` quando concluído.
</output>
