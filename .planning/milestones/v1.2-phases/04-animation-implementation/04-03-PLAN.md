---
phase: 04-animation-implementation
plan: 03
type: tdd
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/HeroMotion.tsx
  - tests/unit/components/HeroMotion.test.tsx
autonomous: true
requirements:
  - ANIM-01
  - ANIM-03

must_haves:
  truths:
    - "HeroMotion usa variants com staggerChildren: 0.12 no container e item variant em cada filho"
    - "O trigger é animate (load) em vez de whileInView — dispara assim que a island hidrata"
    - "Cada filho de React.Children.map recebe <motion.div variants={item}> com hidden={opacity:0, y:20} → visible={opacity:1, y:0}"
    - "ease: 'easeOut' (string) não existe mais em HeroMotion.tsx"
    - "HeroMotion.test.tsx valida que múltiplos motion.div filhos são renderizados (um por child)"
    - "MotionConfig reducedMotion='user' é preservado"
  artifacts:
    - path: "src/components/HeroMotion.tsx"
      provides: "HeroMotion com variants + staggerChildren + animate trigger"
      contains: "staggerChildren"
    - path: "tests/unit/components/HeroMotion.test.tsx"
      provides: "Testes para a nova API de variants + múltiplos filhos"
      contains: "staggerChildren"
  key_links:
    - from: "src/components/HeroMotion.tsx"
      to: "src/pages/index.astro (client:visible)"
      via: "HeroMotion island já wired — sem alteração em index.astro necessária"
      pattern: "HeroMotion"
---

<objective>
Refatorar HeroMotion.tsx para stagger sequencial (ANIM-01) e substituir ease: "easeOut" pelo array numérico (ANIM-03), seguindo TDD: atualizar testes primeiro (RED), depois implementar (GREEN).

Purpose: O HeroMotion atual usa um único motion.div que anima todos os elementos como bloco. A refatoração introduz variants + staggerChildren: 0.12 para delay sequencial de 120ms entre cada filho, e muda o trigger de whileInView para animate (load).

Output: HeroMotion.tsx refatorado com stagger + testes atualizados para a nova API.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-CONTEXT.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-RESEARCH.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-01-SUMMARY.md

<interfaces>
<!-- HeroMotion.tsx atual (completo — ler antes de editar):
     - Importa: React, motion, MotionConfig de "motion/react"
     - Interface: HeroMotionProps { children: React.ReactNode }
     - Usa: motion.div com initial/whileInView/transition/viewport
     - ease atual: "easeOut" (string — substituir por [0.25, 1, 0.5, 1])
     - NÃO usa variants, staggerChildren, React.Children.map

     API alvo (per D-HERO-01/02/03):
       container variant: hidden={}, visible={ transition: { staggerChildren: 0.12 } }
       item variant: hidden={ opacity: 0, y: 20 }, visible={ opacity: 1, y: 0, transition: { duration: 0.4, ease: [0.25, 1, 0.5, 1] } }
       motion.div container: initial="hidden" animate="visible" variants={container}
       Cada filho: motion.div key={i} variants={item} (via React.Children.map)

     ARMADILHA CRÍTICA (Pitfall 1 do RESEARCH.md):
       index.astro usa <HeroMotion client:visible><Hero /></HeroMotion>
       Hero.astro renderiza um único <header id="top" class="hero"> — portanto
       React.Children.count(children) será provavelmente 1, não múltiplos.
       Se for 1, o stagger terá apenas 1 filho e não haverá delays sequenciais.

     RESOLUÇÃO PARA O EXECUTOR:
       Verificar React.Children.count(children) na implementação.
       Se count === 1: mudar estratégia para querySelectorAll interno após montagem.
       A estratégia alternativa: useRef no container motion.div, no useEffect após mount
       selecionar container.current.querySelectorAll("h1, p, .hero-ctas") e aplicar
       animationDelay inline em CSS (como ANIM-05), SEM depender de React.Children.map.
       Documentar a decisão tomada no SUMMARY.

     HeroMotion.test.tsx atual: mocka motion.div mas não variants, não staggerChildren.
     O mock atual passa props desconhecidas como _i, _a, _w etc. e as ignora.
-->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Atualizar HeroMotion.test.tsx para a nova API de variants</name>
  <files>tests/unit/components/HeroMotion.test.tsx</files>
  <read_first>
    tests/unit/components/HeroMotion.test.tsx — ver mock atual e estrutura dos testes existentes
    src/components/HeroMotion.tsx — ver interface e props atuais para saber o que testar
  </read_first>
  <action>
    Atualizar o mock de motion/react no topo do arquivo para capturar props variants, initial e animate passados ao motion.div.

    Adicionar os seguintes novos testes (RED — devem FALHAR antes da implementação):

    1. "container motion.div tem variants.visible.transition.staggerChildren === 0.12"
       — Renderizar HeroMotion com children. Inspecionar props do motion.div raiz via mock capturando variants.

    2. "renderiza um motion.div filho por child recebido"
       — Renderizar HeroMotion com 3 children (h1, p, div). Verificar que há 3 motion.div filhos (além do container).

    3. "item variant tem hidden.opacity === 0 e hidden.y === 20"
       — Via mock capturando variants prop dos motion.div filhos.

    4. "container tem animate='visible' e NÃO tem prop whileInView"
       — Inspecionar props do motion.div container.

    5. "grep da string 'easeOut' em HeroMotion.tsx retorna 0" (verificação estática)
       — Pode ser um teste de sanidade que lê o arquivo e verifica.

    Preservar os testes existentes que ainda forem válidos após a refatoração.
  </action>
  <acceptance_criteria>
    - tests/unit/components/HeroMotion.test.tsx contém pelo menos os 5 novos casos de teste
    - O mock de motion/react captura e expõe a prop variants dos motion.div
    - `npx vitest run tests/unit/components/HeroMotion.test.tsx` FALHA com os novos testes (RED confirmado — HeroMotion.tsx ainda tem a implementação antiga)
  </acceptance_criteria>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && grep -c "staggerChildren" tests/unit/components/HeroMotion.test.tsx
    </automated>
  </verify>
  <done>
    HeroMotion.test.tsx atualizado com os novos testes. Eles falham com o código atual (RED confirmado). Pronto para implementação.
  </done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implementar HeroMotion.tsx com variants + staggerChildren</name>
  <files>src/components/HeroMotion.tsx</files>
  <read_first>
    src/components/HeroMotion.tsx — ver implementação atual completa antes de editar
    tests/unit/components/HeroMotion.test.tsx — ver exatamente o que os testes verificam para guiar a implementação
    src/pages/index.astro — confirmar como HeroMotion é instanciado (client:visible ou client:load)
  </read_first>
  <action>
    Implementar HeroMotion.tsx com a seguinte estrutura de variants:

    container: { hidden: {}, visible: { transition: { staggerChildren: 0.12 } } }
    item: { hidden: { opacity: 0, y: 20 }, visible: { opacity: 1, y: 0, transition: { duration: 0.4, ease: [0.25, 1, 0.5, 1] } } }

    motion.div container: initial="hidden" animate="visible" variants={container}
    Cada filho: React.Children.map wrapping cada child em motion.div com key={i} e variants={item}

    VERIFICAR React.Children.count(children) ANTES de usar map:
    - Se count > 1: usar React.Children.map normalmente
    - Se count === 1 (Hero.astro passa um único <header>): adotar estratégia alternativa
      usando useRef + useEffect + querySelectorAll("h1, p.hero-sub, .hero-ctas") no container
      e aplicar style.animationDelay progressivo (como D-STAGGER-01 do ANIM-05)
      Documentar a estratégia adotada no SUMMARY.

    Remover: whileInView, viewport prop, ease: "easeOut" string.
    Preservar: MotionConfig reducedMotion="user".
    Não alterar a interface HeroMotionProps (ainda aceita children: React.ReactNode).
  </action>
  <acceptance_criteria>
    - `grep "staggerChildren" src/components/HeroMotion.tsx` retorna >= 1 resultado
    - `grep "whileInView" src/components/HeroMotion.tsx` retorna 0 resultados
    - `grep '"easeOut"' src/components/HeroMotion.tsx` retorna 0 resultados
    - `grep "animate=" src/components/HeroMotion.tsx` (ou `animate={`) retorna >= 1 resultado no container
    - `npx vitest run tests/unit/components/HeroMotion.test.tsx` passa (todos os testes GREEN)
    - `npm run build` passa sem erros TypeScript
  </acceptance_criteria>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && npx vitest run tests/unit/components/HeroMotion.test.tsx --reporter=verbose 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    HeroMotion.tsx implementado com variants + staggerChildren: 0.12. Trigger é animate (load). ease: "easeOut" removido. Testes GREEN. MotionConfig reducedMotion="user" preservado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React island → DOM | HeroMotion anima elementos do DOM; sem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | React.Children.map | accept | Número de filhos é fixo e pequeno (Hero tem no máximo 3-4 elementos animados); sem loop dinâmico |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
npx vitest run tests/unit/components/HeroMotion.test.tsx — todos os testes passam.
grep "easeOut" src/components/HeroMotion.tsx retorna 0 resultados.
grep "staggerChildren" src/components/HeroMotion.tsx retorna >= 1 resultado.
grep "whileInView" src/components/HeroMotion.tsx retorna 0 resultados.
npm run build passa sem erros de TypeScript.
</verification>

<success_criteria>
HeroMotion.tsx refatorado com variants + staggerChildren: 0.12. Trigger é animate (load). ease: "easeOut" removido. Testes passam. Múltiplos filhos recebem motion.div wrapper com item variant (ou estratégia alternativa se count===1). MotionConfig reducedMotion="user" preservado.
</success_criteria>

<output>
Criar .planning/milestones/v1.2-phases/04-animation-implementation/04-03-SUMMARY.md quando concluído.
Documentar no SUMMARY se React.Children.map funcionou (count > 1) ou se a estratégia alternativa foi adotada.
</output>