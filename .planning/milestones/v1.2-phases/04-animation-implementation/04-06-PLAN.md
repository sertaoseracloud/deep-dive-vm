---
phase: 04-animation-implementation
plan: 06
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/components/sections/Bonuses.astro
  - src/components/sections/Pricing.astro
autonomous: true
requirements:
  - ANIM-05
  - ANIM-03

must_haves:
  truths:
    - "Os 3 bonus-card em Bonuses.astro têm data-stagger e animation-delay 0ms, 80ms, 160ms respectivamente"
    - "Os 8 li de .price-includes em Pricing.astro têm data-stagger e animation-delay 0ms a 560ms (incrementos de 80ms)"
    - "Bonuses.astro e Pricing.astro têm data-reveal na tag <section> (Pricing recebe data-reveal neste plano)"
    - "Pricing.astro linha ~281 tem ease-out substituído por var(--ease-micro) (ANIM-03 para o card hover)"
    - "npm run build passa sem erros"
  artifacts:
    - path: "src/components/sections/Bonuses.astro"
      provides: "3 bonus-card com data-stagger e animation-delay inline"
      contains: "data-stagger"
    - path: "src/components/sections/Pricing.astro"
      provides: "section com data-reveal, 8 li com data-stagger, ease-out substituído por var(--ease-micro)"
      contains: "data-stagger"
  key_links:
    - from: "src/components/sections/Bonuses.astro [data-stagger]"
      to: "src/layouts/Layout.astro @keyframes fade-up (04-01)"
      via: "animation-play-state: paused → running quando section-pai recebe data-revealed"
      pattern: "data-stagger"
    - from: "src/components/sections/Pricing.astro [data-reveal]"
      to: "src/layouts/Layout.astro IO script (04-01)"
      via: "IO adiciona data-revealed na section.pricing, CSS running ativa os li"
      pattern: "data-reveal"
---

<objective>
Adicionar stagger coordenado nos cards de bônus e na feature list de Pricing, e adicionar data-reveal na section de Pricing.

Purpose: Sem data-stagger + animation-delay nos elementos individuais, os cards e itens aparecem simultaneamente quando a section é revelada. Este plano fecha ANIM-05 (stagger) e completa ANIM-03 para Pricing.astro.

Output: Bonuses.astro com 3 bonus-card staggerados, Pricing.astro com data-reveal + 8 li staggerados + ease-out substituído.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-CONTEXT.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-01-SUMMARY.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-02-SUMMARY.md

<interfaces>
<!-- Bonuses.astro atual (verificado):
     - Linha 5: <section class="bonuses" id="bonus" data-reveal>  (data-reveal adicionado em 04-02)
     - 3 bonus-card ESTÁTICOS (não iterados):
         <div class="bonus-card">         — card 1 (delay: 0ms)
         <div class="bonus-card featured"> — card 2 (delay: 80ms)
         <div class="bonus-card">         — card 3 (delay: 160ms)
     - Transformação (D-STAGGER-01, Pitfall 3):
         Adicionar data-stagger e style="animation-delay: Xms" em cada div.bonus-card
         Não converter para iteração Astro — manter estático por simplicidade

     Pricing.astro atual (verificado):
     - Linha 6: <section class="pricing" id="investimento">  (SEM data-reveal — 04-02 foi orientado a pular Pricing)
     - Linha ~281: transition: transform 0.15s ease-out, box-shadow 0.15s ease-out  (ANIM-03)
     - 8 <li> estáticos em .price-includes (verificado pela leitura parcial do arquivo)
     - Transformação:
         Linha 6: adicionar data-reveal à tag section
         Linha ~281: substituir ease-out por var(--ease-micro)
         Cada <li> de .price-includes: adicionar data-stagger + style="animation-delay: Xms"
         Delays: li[0]=0ms, li[1]=80ms, li[2]=160ms, li[3]=240ms, li[4]=320ms,
                 li[5]=400ms, li[6]=480ms, li[7]=560ms

     @keyframes fade-up e [data-stagger] CSS já definidos em Layout.astro (04-01).
     [data-revealed] [data-stagger] { animation-play-state: running } também em Layout.astro.
     Portanto ao Pricing section receber data-revealed, os li com data-stagger animarão automaticamente.

     NOTA: Pricing.astro linha ~295-303 tem @media (prefers-reduced-motion: reduce) para .price-card hover.
     Verificar se a linha 281 (ease-out no .price-card) está dentro ou fora desse bloco.
     Substituir apenas ease-out na transition do estado normal (fora do @media).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stagger nos 3 bonus-card em Bonuses.astro</name>
  <files>src/components/sections/Bonuses.astro</files>
  <read_first>
    src/components/sections/Bonuses.astro — ler o arquivo inteiro para localizar as 3 divs.bonus-card e confirmar que data-reveal já está na section (adicionado em 04-02)
  </read_first>
  <behavior>
    - grep de "data-stagger" em Bonuses.astro retorna 3 resultados (um por card)
    - grep de "animation-delay: 0ms" em Bonuses.astro retorna 1 resultado
    - grep de "animation-delay: 80ms" em Bonuses.astro retorna 1 resultado
    - grep de "animation-delay: 160ms" em Bonuses.astro retorna 1 resultado
    - grep de "data-reveal" em Bonuses.astro retorna >= 1 resultado (preservado de 04-02)
  </behavior>
  <action>
    Editar src/components/sections/Bonuses.astro.

    Localizar as 3 divs com class="bonus-card" e adicionar os atributos data-stagger e style com animation-delay:

    Primeiro bonus-card (BÔNUS 01):
      &lt;div class="bonus-card" data-stagger style="animation-delay: 0ms"&gt;

    Segundo bonus-card (BÔNUS 02 · DESTAQUE, com class featured):
      &lt;div class="bonus-card featured" data-stagger style="animation-delay: 80ms"&gt;

    Terceiro bonus-card (BÔNUS 03):
      &lt;div class="bonus-card" data-stagger style="animation-delay: 160ms"&gt;

    NÃO alterar o conteúdo interno de nenhum card.
    NÃO remover data-reveal da section (adicionado em 04-02).
    NÃO alterar CSS do componente.
  </action>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && grep -c "data-stagger" src/components/sections/Bonuses.astro
    </automated>
  </verify>
  <done>
    grep de "data-stagger" em Bonuses.astro retorna 3. Os 3 animation-delay (0ms, 80ms, 160ms) existem no arquivo. data-reveal na section ainda presente. npm run build passa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: data-reveal + stagger nos 8 li de Pricing.astro + substituição de ease-out</name>
  <files>src/components/sections/Pricing.astro</files>
  <read_first>
    src/components/sections/Pricing.astro — ler o arquivo inteiro (não apenas o trecho já lido) para localizar:
    (a) tag &lt;section&gt; de abertura (linha 6 aprox.)
    (b) todos os 8 &lt;li&gt; de .price-includes com seus conteúdos exatos
    (c) linha ~281 com transition: ease-out no .price-card
  </read_first>
  <behavior>
    - grep de "data-reveal" em Pricing.astro retorna >= 1 resultado (na tag section)
    - grep de "data-stagger" em Pricing.astro retorna 8 resultados (um por li)
    - grep de "animation-delay: 0ms" em Pricing.astro retorna 1 resultado
    - grep de "animation-delay: 560ms" em Pricing.astro retorna 1 resultado
    - grep de "ease-out" em Pricing.astro retorna 0 resultados (ou apenas dentro do bloco @media já existente se houver — verificar)
    - grep de "var(--ease-micro)" em Pricing.astro retorna >= 1 resultado
    - npm run build passa sem erros
  </behavior>
  <action>
    Editar src/components/sections/Pricing.astro.

    1. Tag section de abertura (linha ~6): adicionar data-reveal
       &lt;section class="pricing" id="investimento"&gt; → &lt;section class="pricing" id="investimento" data-reveal&gt;

    2. Os 8 &lt;li&gt; de .price-includes: adicionar data-stagger e style com animation-delay:
       li[0] (primeiro li): data-stagger style="animation-delay: 0ms"
       li[1]: data-stagger style="animation-delay: 80ms"
       li[2]: data-stagger style="animation-delay: 160ms"
       li[3]: data-stagger style="animation-delay: 240ms"
       li[4]: data-stagger style="animation-delay: 320ms"
       li[5]: data-stagger style="animation-delay: 400ms"
       li[6]: data-stagger style="animation-delay: 480ms"
       li[7]: data-stagger style="animation-delay: 560ms"

    3. Linha ~281 no CSS .price-card:
       transition: transform 0.15s ease-out, box-shadow 0.15s ease-out
       → transition: transform 0.15s var(--ease-micro), box-shadow 0.15s var(--ease-micro)

    NOTA: Verificar se há ease-out também no bloco @media (prefers-reduced-motion: reduce) do .price-card (linha ~295-303). O bloco @media tem "transition: box-shadow 0.15s ease-out" — substituir por var(--ease-micro) também. Ambos os contextos devem usar o token correto.

    NÃO alterar o conteúdo interno dos li (SVG icons e spans de texto).
  </action>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && grep -c "data-stagger" src/components/sections/Pricing.astro
    </automated>
  </verify>
  <done>
    grep de "data-stagger" em Pricing.astro retorna 8. grep de "data-reveal" retorna >= 1. grep de "ease-out" retorna 0. grep de "var(--ease-micro)" retorna >= 1. npm run build passa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS stagger → Browser compositor | animation-delay é valor numérico estático em build time; sem runtime JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | animation-delay inline style | accept | Valores numéricos estáticos calculados em build time no template Astro; sem input de usuário |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
grep -c "data-stagger" src/components/sections/Bonuses.astro retorna 3.
grep -c "data-stagger" src/components/sections/Pricing.astro retorna 8.
grep "data-reveal" src/components/sections/Pricing.astro retorna >= 1 resultado.
grep "ease-out" src/components/sections/Pricing.astro retorna 0 resultados.
grep "var(--ease-micro)" src/components/sections/Pricing.astro retorna >= 1 resultado.
npm run build passa sem erros.
</verification>

<success_criteria>
Bonuses.astro tem 3 bonus-card com data-stagger e delays 0/80/160ms. Pricing.astro tem data-reveal na section, 8 li com data-stagger e delays 0–560ms, e ease-out substituído por var(--ease-micro). O CSS de Layout.astro (04-01) já define @keyframes fade-up e [data-revealed] [data-stagger] { animation-play-state: running } — os stagger funcionam sem JS adicional.
</success_criteria>

<output>
Criar .planning/milestones/v1.2-phases/04-animation-implementation/04-06-SUMMARY.md quando concluído
</output>
