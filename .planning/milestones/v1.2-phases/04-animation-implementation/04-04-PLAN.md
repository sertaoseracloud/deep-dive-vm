---
phase: 04-animation-implementation
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/MobileMenuMotion.tsx
  - src/components/CarouselMotion.tsx
  - src/components/ui/Button.astro
  - src/components/layout/NavBar.astro
  - src/lib/motion-utils.ts
autonomous: true
requirements:
  - ANIM-03

must_haves:
  truths:
    - "Nenhum arquivo modificado contém a string 'easeOut' (verificável por grep)"
    - "Arquivos CSS (.astro) usam var(--ease-micro) onde havia ease-out genérico"
    - "Arquivos motion/react (.tsx/.ts) usam arrays numéricos [0, 0, 0.2, 1] onde havia 'easeOut' string"
    - "applyFallback em motion-utils.ts usa cubic-bezier(0.0, 0.0, 0.2, 1) literal (não var()) pois é inline JS"
    - "Button.astro arrow sem timing-function recebe var(--ease-micro)"
  artifacts:
    - path: "src/components/MobileMenuMotion.tsx"
      provides: "ease substituído por [0, 0, 0.2, 1]"
      contains: "0.0, 0.2, 1"
    - path: "src/components/CarouselMotion.tsx"
      provides: "ease substituído por [0, 0, 0.2, 1]"
      contains: "0.0, 0.2, 1"
    - path: "src/components/ui/Button.astro"
      provides: "ease-out substituído por var(--ease-micro) em todas as ocorrências"
      contains: "var(--ease-micro)"
    - path: "src/components/layout/NavBar.astro"
      provides: "ease-out substituído por var(--ease-micro)"
      contains: "var(--ease-micro)"
    - path: "src/lib/motion-utils.ts"
      provides: "applyFallback com cubic-bezier literal"
      contains: "cubic-bezier(0.0, 0.0, 0.2, 1)"
  key_links:
    - from: "src/layouts/Layout.astro :root (04-01)"
      to: "Button.astro, NavBar.astro"
      via: "var(--ease-micro) resolve para cubic-bezier(0.0, 0.0, 0.2, 1)"
      pattern: "var\\(--ease-micro\\)"
---

<objective>
Substituir todas as ocorrências de easing genérico nos arquivos motion/react (.tsx/.ts) e CSS (.astro) por tokens corretos — arrays numéricos para React e var(--ease-micro) para CSS.

Purpose: Nenhum "easeOut" genérico deve permanecer após esta fase (ANIM-03). Este plano cobre os 5 arquivos que não tocam Pricing.astro (tratado em 04-05) nem HeroMotion.tsx (tratado em 04-03).

Output: MobileMenuMotion.tsx, CarouselMotion.tsx, Button.astro, NavBar.astro e motion-utils.ts com easing padronizado.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-CONTEXT.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-01-SUMMARY.md

<interfaces>
<!-- Inventário de ocorrências a substituir (D-EASE-03 do CONTEXT.md, verificado em RESEARCH.md):

     MobileMenuMotion.tsx:44 — ease: "easeOut" → ease: [0, 0, 0.2, 1]  (micro — menu slide)
     CarouselMotion.tsx:92  — ease: "easeOut" → ease: [0, 0, 0.2, 1]  (micro — keyboard nav)

     Button.astro:49  — transition: transform 0.15s ease-out, filter 0.15s ease-out,
                         background 0.15s ease-out, box-shadow 0.15s ease-out
                       → cada ease-out → var(--ease-micro)
     Button.astro:66  — transition: transform 0.25s  (sem timing-function)
                       → adicionar var(--ease-micro) como timing-function: transform 0.25s var(--ease-micro)

     NavBar.astro:59  — transition: background 0.2s ease-out, box-shadow 0.2s ease-out
                       → var(--ease-micro)
     NavBar.astro:123 — transition: color 0.15s ease-out, border-color 0.15s ease-out
                       → var(--ease-micro)

     motion-utils.ts:107 — transition: "all 150ms ease-out"
                          → transition: "all 150ms cubic-bezier(0.0, 0.0, 0.2, 1)"
                          NOTA: usar valor LITERAL, não var() — CSS custom properties
                          não funcionam em element.style aplicado via JS (Pitfall 2 do RESEARCH.md)

     FORA DESTE PLANO (tratados em outros planos):
       - HeroMotion.tsx:14 → tratado em 04-03
       - Pricing.astro:281 → tratado em 04-05
       - Method.astro accordion (P2) → Method.astro pode ser verificado mas não está em D-EASE-03 confirmado
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Substituir easing em MobileMenuMotion.tsx, CarouselMotion.tsx e motion-utils.ts</name>
  <files>
    src/components/MobileMenuMotion.tsx,
    src/components/CarouselMotion.tsx,
    src/lib/motion-utils.ts
  </files>
  <read_first>
    src/components/MobileMenuMotion.tsx — localizar linha ~44 com ease: "easeOut"
    src/components/CarouselMotion.tsx — localizar linha ~92 com ease: "easeOut"
    src/lib/motion-utils.ts — localizar linha ~107 com "all 150ms ease-out" em applyFallback
  </read_first>
  <behavior>
    - grep de '"easeOut"' em MobileMenuMotion.tsx retorna 0 resultados após edição
    - grep de '"easeOut"' em CarouselMotion.tsx retorna 0 resultados após edição
    - grep de 'ease-out' em motion-utils.ts retorna 0 resultados após edição
    - grep de '0.0, 0.2, 1' em MobileMenuMotion.tsx retorna >= 1 resultado
    - grep de '0.0, 0.2, 1' em CarouselMotion.tsx retorna >= 1 resultado
    - grep de 'cubic-bezier(0.0, 0.0, 0.2, 1)' em motion-utils.ts retorna 1 resultado
    - npx vitest run tests/unit/components/MobileMenuMotion.test.tsx passa
    - npx vitest run tests/unit/lib/motion-utils.test.ts passa
  </behavior>
  <action>
    MobileMenuMotion.tsx: localizar a linha com ease: "easeOut" (aprox. linha 44) e substituir por ease: [0, 0, 0.2, 1]. Não alterar nada mais no arquivo.

    CarouselMotion.tsx: localizar a linha com ease: "easeOut" (aprox. linha 92, keyboard nav) e substituir por ease: [0, 0, 0.2, 1]. O auto-scroll com ease: "linear" é aceitável — NÃO alterar. Não alterar nada mais no arquivo.

    motion-utils.ts: localizar a linha ~107 em applyFallback com a string "all 150ms ease-out" e substituir por "all 150ms cubic-bezier(0.0, 0.0, 0.2, 1)". USAR VALOR LITERAL — não usar var(--ease-micro) pois element.style.transition não processa CSS custom properties em todos os browsers.
  </action>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && grep -c "easeOut" src/components/MobileMenuMotion.tsx src/components/CarouselMotion.tsx; grep -c "ease-out" src/lib/motion-utils.ts
    </automated>
  </verify>
  <done>
    grep de "easeOut" nos 3 arquivos retorna 0. Os testes existentes de MobileMenuMotion e motion-utils continuam passando. npm run build sem erros de TypeScript.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substituir easing em Button.astro e NavBar.astro</name>
  <files>
    src/components/ui/Button.astro,
    src/components/layout/NavBar.astro
  </files>
  <read_first>
    src/components/ui/Button.astro — localizar linhas 49 e 66 com as transitions
    src/components/layout/NavBar.astro — localizar linhas 59 e 123 com as transitions
  </read_first>
  <behavior>
    - grep de 'ease-out' em Button.astro retorna 0 resultados após edição
    - grep de 'var(--ease-micro)' em Button.astro retorna >= 2 resultados (linhas 49 e 66)
    - grep de 'ease-out' em NavBar.astro retorna 0 resultados para as linhas 59 e 123
    - grep de 'var(--ease-micro)' em NavBar.astro retorna >= 2 resultados
    - npm run build passa sem erros
  </behavior>
  <action>
    Button.astro linha ~49: na declaração de transition que contém "ease-out" múltiplas vezes (transform, filter, background, box-shadow), substituir cada token "ease-out" por "var(--ease-micro)". Preservar os valores de duration (0.15s) e properties.

    Button.astro linha ~66: na declaração "transition: transform 0.25s" (sem timing-function), adicionar var(--ease-micro) como terceiro token: "transition: transform 0.25s var(--ease-micro)".

    NavBar.astro linha ~59: na declaração "transition: background 0.2s ease-out, box-shadow 0.2s ease-out", substituir cada "ease-out" por "var(--ease-micro)".

    NavBar.astro linha ~123: na declaração "transition: color 0.15s ease-out, border-color 0.15s ease-out", substituir cada "ease-out" por "var(--ease-micro)".

    NÃO alterar as propriedades data-scrolled do NavBar (IntersectionObserver logic) — apenas as declarations de transition nas linhas especificadas.

    VERIFICAÇÃO EXTRA: após editar NavBar.astro, confirmar que a linha do IntersectionObserver existente (aprox. 170-187) não foi tocada.
  </action>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && grep -c "ease-out" src/components/ui/Button.astro src/components/layout/NavBar.astro
    </automated>
  </verify>
  <done>
    grep de "ease-out" em Button.astro e NavBar.astro retorna 0 para cada arquivo. grep de "var(--ease-micro)" em Button.astro retorna >= 2; em NavBar.astro retorna >= 2. npm run build passa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS transitions → Browser compositor | Substituição de strings de easing; sem novo input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | applyFallback inline style | accept | Valor de easing é string literal hardcoded; sem interpolação de dados externos |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
grep -r '"easeOut"' src/components/MobileMenuMotion.tsx src/components/CarouselMotion.tsx retorna 0.
grep -r 'ease-out' src/components/ui/Button.astro src/components/layout/NavBar.astro src/lib/motion-utils.ts retorna 0.
grep 'var(--ease-micro)' src/components/ui/Button.astro retorna >= 2.
grep 'var(--ease-micro)' src/components/layout/NavBar.astro retorna >= 2.
npm run test:unit passa.
npm run build passa sem erros de TypeScript.
</verification>

<success_criteria>
Todos os 5 arquivos deste plano têm easing padronizado. Nenhuma string "easeOut" ou token CSS "ease-out" genérico permanece. motion/react usa arrays numéricos. CSS usa var(--ease-micro). motion-utils.ts usa valor literal cubic-bezier. Os testes existentes continuam passando.
</success_criteria>

<output>
Criar .planning/milestones/v1.2-phases/04-animation-implementation/04-04-SUMMARY.md quando concluído
</output>
