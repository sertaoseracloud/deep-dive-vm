---
phase: 04-animation-implementation
plan: 05
type: tdd
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/SettingsToggle.tsx
  - tests/unit/components/SettingsToggle.test.ts
autonomous: true
requirements:
  - ANIM-04

must_haves:
  truths:
    - "SettingsToggle renderiza motion.span para o indicador (substitui <span> com CSS left)"
    - "motion.span do indicador tem animate={{ x: motionEnabled ? 16 : 0 }}"
    - "motion.span do indicador tem transition spring (stiffness:400, damping:30) quando motionEnabled=true"
    - "motion.span do indicador tem transition duration:0 (instantâneo) quando motionEnabled=false (per D-TOGGLE-03)"
    - "Label 'Animações' é wrapped em motion.span com animate={{ opacity: motionEnabled ? 1 : 0.4 }}"
    - "CSS 'transition: left 0.15s ease-out' do indicador foi removido"
    - "MotionConfig reducedMotion='user' envolve o componente"
    - "SettingsToggle.test.ts valida que motion.span é renderizado para indicador e label"
  artifacts:
    - path: "src/components/SettingsToggle.tsx"
      provides: "SettingsToggle com motion.span spring no indicador e fade no label"
      contains: "motion.span"
    - path: "tests/unit/components/SettingsToggle.test.ts"
      provides: "Testes atualizados para motion.span"
      contains: "motion.span"
  key_links:
    - from: "src/components/SettingsToggle.tsx"
      to: "src/pages/index.astro (client:load)"
      via: "Island já wired — sem alteração em index.astro necessária"
      pattern: "SettingsToggle"
---

<objective>
Refatorar SettingsToggle.tsx para spring no indicador e fade no label usando motion/react, seguindo TDD: atualizar testes primeiro (RED), depois implementar (GREEN).

Purpose: O indicador atual usa CSS transition: left — reflow de layout. A refatoração troca para motion.span com x transform (compositor-only) e adiciona spring physics (stiffness:400, damping:30). O label ganha fade opacity.

Output: SettingsToggle.tsx refatorado com motion.span + testes atualizados para a nova estrutura.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-CONTEXT.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.2-phases\04-animation-implementation\04-RESEARCH.md

<interfaces>
<!-- SettingsToggle.tsx atual (completo — ler antes de editar):
     - Importa: React, useMotionEnabled de motion-utils
     - NÃO importa motion/react — adicionar import { motion, MotionConfig }
     - Indicador: <span style={{ left: motionEnabled ? "18px" : "2px", transition: "left 0.15s ease-out" }}>
     - Label: <span style={{ fontSize:"12px", ... }}>Animações</span>

     Transformação do indicador (D-TOGGLE-01):
       - Mudar de <span> para <motion.span>
       - Manter style={{position:"absolute", top:"2px", left:"2px", width:"16px", height:"16px", borderRadius:"8px", background:"#fff"}}
       - REMOVER: left dinâmico (motionEnabled ? "18px" : "2px") — agora left é FIXO "2px"
       - REMOVER: transition: "left 0.15s ease-out"
       - ADICIONAR: animate={{ x: motionEnabled ? 16 : 0 }}
       - ADICIONAR: transition condicional:
           motionEnabled=true  → { type: "spring", stiffness: 400, damping: 30 }
           motionEnabled=false → { duration: 0 }

     Transformação do label (D-TOGGLE-02):
       - Mudar de <span> para <motion.span>
       - Preservar todos os estilos existentes
       - ADICIONAR: animate={{ opacity: motionEnabled ? 1 : 0.4 }}
       - ADICIONAR: transition={{ duration: 0.2, ease: [0, 0, 0.2, 1] }}

     Wrapper MotionConfig (RESEARCH.md Pattern 4):
       - Envolver o componente inteiro com <MotionConfig reducedMotion="user">

     PITFALL 5 (do RESEARCH.md):
       - NÃO manter transition:"left 0.15s ease-out" — conflita com x transform
       - left base do indicador deve ser fixo "2px" (não dinâmico)
       - x: 16 + left: "2px" = posição 18px do estado "on"

     SettingsToggle.test.ts atual: NÃO mocka motion/react.
-->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Atualizar SettingsToggle.test.ts com mock de motion/react e novos testes</name>
  <files>tests/unit/components/SettingsToggle.test.ts</files>
  <read_first>
    tests/unit/components/SettingsToggle.test.ts — ver estrutura atual, mocks existentes e testes
    src/components/SettingsToggle.tsx — ver interface atual para saber o que testar
  </read_first>
  <action>
    Adicionar vi.mock("motion/react") no topo do arquivo de teste:
    - motion.span: renderiza como um <span> passando todas as props, incluindo animate, transition, data-testid
    - MotionConfig: passthrough de children sem comportamento adicional

    Adicionar os seguintes novos testes (RED — devem FALHAR antes da implementação):

    1. "mock de motion/react está presente" — vi.mock("motion/react") configurado
    2. "renderiza motion.span para o indicador" — container.querySelector("[data-testid='motion-indicator']") não null
    3. "motion.span do indicador tem animate.x === 16 quando motionEnabled=true" — props capturadas via mock
    4. "motion.span do indicador tem animate.x === 0 quando motionEnabled=false"
    5. "motion.span do label 'Animações' existe" — container.querySelector("[data-testid='motion-label']") não null
    6. "motion.span do label tem animate.opacity === 1 quando motionEnabled=true"
    7. "motion.span do label tem animate.opacity === 0.4 quando motionEnabled=false"

    Mockar useMotionEnabled de motion-utils para retornar [true, vi.fn()] e [false, vi.fn()] conforme o caso de teste.
  </action>
  <acceptance_criteria>
    - tests/unit/components/SettingsToggle.test.ts contém vi.mock("motion/react")
    - O arquivo contém pelo menos 5 novos casos de teste referenciando motion.span
    - `npx vitest run tests/unit/components/SettingsToggle.test.ts` FALHA (RED — SettingsToggle.tsx ainda não usa motion.span)
    - `grep -c "motion.span" tests/unit/components/SettingsToggle.test.ts` retorna >= 2
  </acceptance_criteria>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && grep -c "motion.span" tests/unit/components/SettingsToggle.test.ts
    </automated>
  </verify>
  <done>
    SettingsToggle.test.ts atualizado com mock de motion/react e novos testes. Eles falham com o código atual (RED confirmado). Pronto para implementação.
  </done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implementar SettingsToggle.tsx com motion.span spring e label fade</name>
  <files>src/components/SettingsToggle.tsx</files>
  <read_first>
    src/components/SettingsToggle.tsx — ver implementação atual completa (indicador, label, estrutura)
    tests/unit/components/SettingsToggle.test.ts — ver exatamente o que os testes verificam
  </read_first>
  <action>
    Adicionar import { motion, MotionConfig } from "motion/react" no topo do arquivo.

    Indicador: substituir o <span> interno (o bolinha branca) por <motion.span>:
    - Remover: style={{ left: motionEnabled ? "18px" : "2px" }} (left dinâmico)
    - Remover: transition: "left 0.15s ease-out" do style
    - Manter: position:"absolute", top:"2px", left:"2px" (left FIXO "2px"), width:"16px", height:"16px", borderRadius:"8px", background:"#fff"
    - Adicionar: data-testid="motion-indicator"
    - Adicionar: animate={{ x: motionEnabled ? 16 : 0 }}
    - Adicionar: transition={motionEnabled ? { type: "spring", stiffness: 400, damping: 30 } : { duration: 0 }}

    Label: substituir o <span> que contém o texto "Animações" por <motion.span>:
    - Preservar todos os estilos existentes (fontSize, fontFamily, letterSpacing, color, userSelect)
    - Adicionar: data-testid="motion-label"
    - Adicionar: animate={{ opacity: motionEnabled ? 1 : 0.4 }}
    - Adicionar: transition={{ duration: 0.2, ease: [0, 0, 0.2, 1] }}

    Wrapper: envolver o retorno do componente com <MotionConfig reducedMotion="user">.
    NÃO alterar: container externo (fixed, bottom:24px, right:24px), track span (background transition), input checkbox, label htmlFor, role, aria-label.
  </action>
  <acceptance_criteria>
    - `grep -c "motion.span" src/components/SettingsToggle.tsx` retorna >= 2
    - `grep -c "MotionConfig" src/components/SettingsToggle.tsx` retorna >= 1
    - `grep "stiffness" src/components/SettingsToggle.tsx` retorna 1 resultado (stiffness: 400)
    - `grep "left.*ease-out" src/components/SettingsToggle.tsx` retorna 0 resultados
    - `npx vitest run tests/unit/components/SettingsToggle.test.ts` passa (todos GREEN)
    - `npm run build` passa sem erros TypeScript
  </acceptance_criteria>
  <verify>
    <automated>
      cd "C:/Repo/landing-page/deep-dive-vm" && npx vitest run tests/unit/components/SettingsToggle.test.ts --reporter=verbose 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    SettingsToggle.tsx usa motion.span para o indicador (spring x:0/16, stiffness:400, damping:30) e para o label (opacity:0.4/1). CSS transition:left removido. MotionConfig adicionado. Testes passam com mock de motion/react. npm run build sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React island → DOM | SettingsToggle lê motionEnabled de localStorage; sem novo input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | motion.span animate prop | accept | Props são computadas de motionEnabled (boolean do hook); sem interpolação de input externo |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
npx vitest run tests/unit/components/SettingsToggle.test.ts — todos os testes passam.
grep "ease-out" src/components/SettingsToggle.tsx retorna 0 resultados.
grep "motion.span" src/components/SettingsToggle.tsx retorna >= 2 resultados.
grep "MotionConfig" src/components/SettingsToggle.tsx retorna >= 1 resultado.
grep "stiffness" src/components/SettingsToggle.tsx retorna 1 resultado.
npm run build passa sem erros de TypeScript.
</verification>

<success_criteria>
SettingsToggle.tsx usa motion.span para o indicador (spring x:0/16, stiffness:400, damping:30) e para o label (opacity:0.4/1, duration:0.2). CSS transition:left removido. MotionConfig reducedMotion="user" adicionado. Testes passam com mock de motion/react. npm run build sem erros.
</success_criteria>

<output>
Criar .planning/milestones/v1.2-phases/04-animation-implementation/04-05-SUMMARY.md quando concluído
</output>