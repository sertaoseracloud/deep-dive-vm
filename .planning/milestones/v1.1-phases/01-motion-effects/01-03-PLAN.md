---
phase: 01-motion-effects
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01-PLAN.md"
  - "01-02-PLAN.md"
files_modified:
  - tests/unit/components/CarouselMotion.test.ts
  - tests/unit/components/MobileMenuMotion.test.ts
  - tests/unit/components/SettingsToggle.test.ts
  - tests/unit/lib/motion-utils.test.ts
  - tests/e2e/motion-accessibility.spec.ts
  - package.json
  - vitest.config.ts
autonomous: true
requirements:
  - MOT-01
  - MOT-02
  - MOT-03

must_haves:
  truths:
    - "Vitest unit tests cover motion-utils exports (useMotionEnabled, setMotionEnabled, isMotionSupported, applyFallback)"
    - "Vitest unit tests use @testing-library/react renderHook to test useMotionEnabled() with mocked window.matchMedia and localStorage"
    - "Vitest unit tests cover each component renders without errors"
    - "Playwright E2E test verifies reduce-motion toggle disables animations"
    - "Playwright E2E test verifies CarouselMotion is keyboard navigable"
    - "Playwright E2E test verifies MobileMenuMotion aria-hidden toggles correctly"
    - "vitest run --coverage passes with 95%+ threshold including new src/lib/motion-utils.ts and src/components/*.tsx"
    - "npm run test:axe (axe-core via @axe-core/playwright) returns zero motion-sensory violations"
    - "npm run test:perf runs a Lighthouse CI or unlighthouse audit asserting CLS <= 0.1 and TBT < 50ms"
    - "package.json contains both test:axe and test:perf scripts"
  artifacts:
    - path: "tests/unit/lib/motion-utils.test.ts"
      provides: "Unit tests for all five motion-utils exports including renderHook tests for useMotionEnabled"
    - path: "tests/unit/components/CarouselMotion.test.ts"
      provides: "Unit test: renders items, handles disabled motion state"
    - path: "tests/unit/components/MobileMenuMotion.test.ts"
      provides: "Unit test: renders open/closed states, aria-hidden value"
    - path: "tests/unit/components/SettingsToggle.test.ts"
      provides: "Unit test: checkbox renders with correct checked state and aria-label"
    - path: "tests/e2e/motion-accessibility.spec.ts"
      provides: "E2E: reduce-motion toggle, carousel keyboard nav, menu ARIA, axe audit"
  key_links:
    - from: "tests/unit/lib/motion-utils.test.ts"
      to: "src/lib/motion-utils.ts"
      via: "named imports of all five exported symbols"
      pattern: "from \".*motion-utils\""
    - from: "tests/e2e/motion-accessibility.spec.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder import for WCAG 2.1 AA audit"
      pattern: "from \"@axe-core/playwright\""
---

<objective>
Write the full test suite for the motion effects phase: Vitest unit tests for the motion-utils library (including hook coverage via renderHook) and the three components, plus Playwright E2E tests for accessibility (axe-core audit, reduce-motion toggle, keyboard navigation, ARIA state verification), plus a Lighthouse performance audit script asserting CLS <= 0.1 and TBT < 50ms.

Purpose: Satisfies the 95%+ coverage gate required by vitest.config.ts, the MOT-02 reduced-motion integration requirement, and the MOT-03 Lighthouse CLS/TBT performance requirement. The project has `@axe-core/playwright` already installed.

Output: Four new unit test files, one new E2E spec file, a `test:axe` script and a `test:perf` script in package.json.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.1-phases\01-motion-effects\01-CONTEXT.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.1-phases\01-motion-effects\01-motion-effects-RESEARCH.md
@C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.1-phases\01-motion-effects\01-02-SUMMARY.md

<interfaces>
<!-- Existing test infrastructure patterns (from tests/unit/components/NavBar.test.ts): -->
<!--
import { describe, it, expect, beforeAll } from "vitest";
import { readFileSync } from "node:fs";
// Unit tests read from dist/index.html (built HTML) — they test server-rendered output.
// React components (.tsx) are CLIENT-ONLY (client:load) — they may not appear in dist HTML.
// For motion components (React), unit tests must use @testing-library/react or happy-dom rendering.
-->

<!-- vitest.config.ts environment for unit-integration project: "happy-dom" -->
<!-- @testing-library/react MUST be installed as devDependency (see Task 1 action) -->
<!-- renderHook from @testing-library/react tests React hooks in isolation -->

<!-- @axe-core/playwright is in devDependencies (already installed). -->
<!-- Import pattern: import AxeBuilder from "@axe-core/playwright"; -->
<!-- Usage: const results = await new AxeBuilder({ page }).analyze(); -->

<!-- playwright.config.ts uses baseURL http://localhost:4321/deep-dive-vm/ -->
<!-- E2E tests use page.goto("./") and expect(results.violations).toEqual([]) -->

<!-- Existing E2E pattern (from tests/e2e/accessibility.spec.ts): -->
<!--
import { test, expect } from "@playwright/test";
test.describe("...", () => {
  test("...", async ({ page }) => {
    await page.goto("./");
    ...
  });
});
-->

<!-- motion-utils exports: -->
<!--
MOTION_STORAGE_KEY: string
useMotionEnabled(): boolean  (React hook)
setMotionEnabled(enabled: boolean): void
isMotionSupported(): boolean
applyFallback(el: HTMLElement, props: Partial<CSSStyleDeclaration>): void
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Vitest unit tests for motion-utils (including renderHook hook coverage) and the three components</name>
  <files>
    tests/unit/lib/motion-utils.test.ts,
    tests/unit/components/CarouselMotion.test.ts,
    tests/unit/components/MobileMenuMotion.test.ts,
    tests/unit/components/SettingsToggle.test.ts,
    vitest.config.ts,
    package.json
  </files>
  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\tests\unit\setup.ts — globalSetup pattern; note it builds the site. React component tests cannot rely on dist HTML since these are client-only components.
    - C:\Repo\landing-page\deep-dive-vm\tests\unit\components\NavBar.test.ts — pattern for built-HTML tests.
    - C:\Repo\landing-page\deep-dive-vm\vitest.config.ts — projects array, environment "happy-dom", coverage include "src/**", exclude paths.
    - C:\Repo\landing-page\deep-dive-vm\package.json — devDependencies; check whether @testing-library/react is present; add it if absent.
    - C:\Repo\landing-page\deep-dive-vm\src\lib\motion-utils.ts — all exports to cover.
    - C:\Repo\landing-page\deep-dive-vm\src\components\CarouselMotion.tsx — interface Item, exports.
    - C:\Repo\landing-page\deep-dive-vm\src\components\MobileMenuMotion.tsx — isOpen prop.
    - C:\Repo\landing-page\deep-dive-vm\src\components\SettingsToggle.tsx — checkbox and aria-label.
  </read_first>
  <behavior>
    motion-utils.test.ts — non-hook exports:
      - isMotionSupported() returns true in happy-dom (window is defined)
      - setMotionEnabled(true) sets localStorage "motionEnabled" to JSON "true"
      - setMotionEnabled(false) sets localStorage "motionEnabled" to JSON "false"
      - applyFallback(el, { color: "red" }) sets el.style.transition = "all 150ms ease-out" and el.style.color = "red"
      - MOTION_STORAGE_KEY equals "motionEnabled"

    motion-utils.test.ts — useMotionEnabled hook (via renderHook):
      - Returns true by default when localStorage has no entry and matchMedia does not report prefers-reduced-motion
      - Returns false when localStorage has "motionEnabled"="false"
      - Returns false when matchMedia("(prefers-reduced-motion: reduce)").matches is true (regardless of localStorage)

    CarouselMotion.test.ts:
      - Component file exists at src/components/CarouselMotion.tsx (import resolves)
      - Exported symbol CarouselMotion is a function (React component)

    MobileMenuMotion.test.ts:
      - Component file exists at src/components/MobileMenuMotion.tsx (import resolves)
      - Exported symbol MobileMenuMotion is a function (React component)

    SettingsToggle.test.ts:
      - Component file exists at src/components/SettingsToggle.tsx (import resolves)
      - Exported symbol SettingsToggle is a function (React component)
  </behavior>
  <action>
    STEP 0 — Install @testing-library/react if not present:
      Read package.json devDependencies first. If "@testing-library/react" is absent, run:
        npm install --save-dev @testing-library/react @testing-library/user-event
      This is required to call renderHook() for testing the useMotionEnabled React hook.

    STEP 1 — Create tests/unit/lib/motion-utils.test.ts:

    Use a two-tier approach:

    Tier A — Non-hook exports (pure function tests with happy-dom):
      Import isMotionSupported, setMotionEnabled, applyFallback, MOTION_STORAGE_KEY from the module.
      Use happy-dom's globalThis.localStorage (available in the happy-dom environment).
      Test applyFallback by creating document.createElement("div") and asserting style properties.

    Tier B — useMotionEnabled hook (via renderHook from @testing-library/react):
      Before each hook test, mock window.matchMedia:
        Object.defineProperty(window, "matchMedia", {
          writable: true,
          value: vi.fn().mockImplementation((query: string) => ({
            matches: query.includes("reduce") ? mockReduceMotion : false,
            media: query,
            onchange: null,
            addListener: vi.fn(),
            removeListener: vi.fn(),
            addEventListener: vi.fn(),
            removeEventListener: vi.fn(),
            dispatchEvent: vi.fn(),
          })),
        });
      Use a local variable mockReduceMotion (boolean) toggled per test case.
      Use localStorage.setItem(MOTION_STORAGE_KEY, ...) to set storage state before each hook test.
      Call renderHook(() => useMotionEnabled()) and assert result.current.

      Test cases:
        1. Default state (no localStorage, matchMedia.matches=false) → result.current === true
        2. localStorage="false", matchMedia.matches=false → result.current === false
        3. localStorage="true", matchMedia.matches=true (reduce motion) → result.current === false

      After each hook test: localStorage.removeItem(MOTION_STORAGE_KEY) and vi.restoreAllMocks().

    STEP 2 — Create component existence tests:
    For each component test file (CarouselMotion.test.ts, MobileMenuMotion.test.ts, SettingsToggle.test.ts):
      Use vi.mock strategy to prevent browser-only APIs from crashing Vitest:
        vi.mock("motion/react", () => ({ motion: { div: "div", nav: "nav" }, useReducedMotion: () => false }))
        vi.mock("motion", () => ({ animate: vi.fn(() => ({ pause: vi.fn(), play: vi.fn(), stop: vi.fn() })) }))
      Then import the component and assert: expect(typeof CarouselMotion).toBe("function")

    STEP 3 — Update vitest.config.ts if needed:
      Verify the "unit-integration" project's include array covers "tests/unit/lib/**/*.test.ts".
      The existing "tests/unit/**/*.test.ts" glob may already cover this — if so, no change needed.
      The coverage include already covers "src/**" so motion-utils.ts and the .tsx components are instrumented automatically.

    Create the directory tests/unit/lib/ if it does not exist.
  </action>
  <verify>
    <automated>cd C:/Repo/landing-page/deep-dive-vm && npx vitest run tests/unit/lib/motion-utils.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/lib/motion-utils.test.ts: all assertions pass in Vitest including the three renderHook hook tests
    - tests/unit/components/CarouselMotion.test.ts: typeof CarouselMotion === "function" passes
    - tests/unit/components/MobileMenuMotion.test.ts: typeof MobileMenuMotion === "function" passes
    - tests/unit/components/SettingsToggle.test.ts: typeof SettingsToggle === "function" passes
    - `npx vitest run tests/unit` exits with code 0
    - `npm run test:unit` exits with code 0 (95% coverage threshold met)
    - @testing-library/react is present in package.json devDependencies
  </acceptance_criteria>
  <done>All four unit test files pass; coverage report shows src/lib/motion-utils.ts covered including the useMotionEnabled hook body; vitest exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Write Playwright E2E accessibility spec and add test:axe script</name>
  <files>tests/e2e/motion-accessibility.spec.ts, package.json</files>
  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\tests\e2e\accessibility.spec.ts — existing Playwright accessibility test; note AxeBuilder is NOT imported there; uses @axe-core/playwright directly from Playwright test.
    - C:\Repo\landing-page\deep-dive-vm\playwright.config.ts — baseURL and browser configuration.
    - C:\Repo\landing-page\deep-dive-vm\.planning\milestones\v1.1-phases\01-motion-effects\01-CONTEXT.md — D-01: "Playwright testa toggle de reduce motion"; D-01: "integrar axe-core no CI (npm run test:axe)".
    - C:\Repo\landing-page\deep-dive-vm\package.json — existing scripts; test:visual uses playwright --project=chromium.
  </read_first>
  <action>
    Create tests/e2e/motion-accessibility.spec.ts with four test groups:

    Group 1 — "Axe accessibility audit after motion":
      - Navigate to "./"
      - Run AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa"]).analyze()
      - Assert results.violations.toEqual([]) (zero WCAG 2.1 AA violations)
      - Import AxeBuilder from "@axe-core/playwright"

    Group 2 — "SettingsToggle reduce-motion behavior":
      - Navigate to "./"
      - Locate the settings toggle checkbox by aria-label "Enable animations" (page.locator('input[aria-label="Enable animations"]'))
      - If toggle is visible: uncheck it; assert localStorage["motionEnabled"] equals "false"
      - If toggle is not visible (not yet wired into a page): use page.evaluate() to call localStorage.setItem("motionEnabled", "false") directly, then reload
      - After disabling: assert no element with an active motion animation is present (check that no motion.div has a non-zero animation-play-state of running for the carousel)

    Group 3 — "CarouselMotion keyboard navigation":
      - Navigate to "./"
      - Locate the element with role="region" and aria-label="Testimonials carousel"
      - If found: focus it via keyboard Tab navigation or locator.focus()
      - Press ArrowRight; assert no JavaScript error is thrown (page.on("pageerror") trap)
      - Press ArrowLeft; assert no JavaScript error is thrown
      - NOTE: if the carousel is not yet wired into a page (only the component file exists), mark this test as test.skip with a TODO comment; do not fail the CI for missing page integration

    Group 4 — "MobileMenuMotion ARIA state":
      - Locate element with aria-label="Mobile navigation menu"
      - If found and closed: assert aria-hidden="true"
      - If found and opened by clicking a trigger: assert aria-hidden="false" (or null)
      - If no trigger exists in the page yet: test.skip with TODO comment

    Add to package.json scripts:
      "test:axe": "npx playwright test tests/e2e/motion-accessibility.spec.ts --project=chromium"

    IMPORTANT SKIP STRATEGY: Tests that depend on page-level DOM elements that do not exist must use test.skip(). This prevents CI breakage. Add a TODO comment on each skipped test indicating the page integration work needed. Do NOT use test.fail() — use test.skip().

    The axe audit in Group 1 must run unconditionally (it tests the existing page, not the new components specifically).
  </action>
  <verify>
    <automated>cd C:/Repo/landing-page/deep-dive-vm && npx playwright test tests/e2e/motion-accessibility.spec.ts --project=chromium --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/motion-accessibility.spec.ts exists and is valid Playwright syntax (--list exits 0)
    - package.json contains "test:axe" script pointing to the new spec file
    - AxeBuilder is imported from "@axe-core/playwright"
    - Axe audit test is NOT skipped
    - Component-dependent tests use test.skip() with TODO comments if components not yet in a page
    - `npm run test:axe` can be run (requires dev server running; documents this prerequisite in a comment)
  </acceptance_criteria>
  <done>motion-accessibility.spec.ts lists at least 4 tests; test:axe script exists in package.json; axe audit test runs and passes against the existing built page (zero WCAG 2.1 AA violations before new components are added to pages)</done>
</task>

<task type="auto">
  <name>Task 3: Add test:perf script with Lighthouse CI asserting CLS <= 0.1 and TBT < 50ms</name>
  <files>package.json</files>
  <read_first>
    - C:\Repo\landing-page\deep-dive-vm\package.json — read existing scripts and devDependencies to choose the right Lighthouse tooling and avoid duplicating script names.
  </read_first>
  <action>
    Add a Lighthouse-based performance audit script to package.json that asserts:
      - Cumulative Layout Shift (CLS) <= 0.1
      - Total Blocking Time (TBT) < 50ms

    Choose tooling based on what is already installed:

    Option A — If `@unlighthouse/cli` or `unlighthouse` is present in devDependencies: use it.
      Add script: "test:perf": "unlighthouse-ci --site http://localhost:4321/deep-dive-vm/ --budget '{\"cumulative-layout-shift\": 0.1, \"total-blocking-time\": 50}'"

    Option B (preferred if nothing is installed) — Use `@lhci/cli` (Lighthouse CI):
      Run: npm install --save-dev @lhci/cli
      Add script: "test:perf": "lhci autorun"
      Create a lighthouserc.cjs file in the project root with:
        module.exports = {
          ci: {
            collect: {
              url: ["http://localhost:4321/deep-dive-vm/"],
              numberOfRuns: 1,
            },
            assert: {
              assertions: {
                "cumulative-layout-shift": ["error", { maxNumericValue: 0.1 }],
                "total-blocking-time": ["error", { maxNumericValue: 50 }],
              },
            },
            upload: {
              target: "temporary-public-storage",
            },
          },
        };

    If @lhci/cli is chosen:
      - files_modified must also include lighthouserc.cjs in the executor's mental model (it is a new file, not tracked in this plan's frontmatter — but the executor creates it as part of this task).
      - Add a comment in package.json "test:perf" value: the command requires the dev server to be running at http://localhost:4321/deep-dive-vm/ (run `npm run dev` in a separate terminal first).

    Validation:
      - After adding the script, run: npm run test:perf -- --help (or lhci --version) to confirm the CLI is present and executable.
      - Do NOT run a full Lighthouse audit during this task (it requires a live server); only verify the script is wired and the CLI responds.

    The "test:perf" script must be distinct from "test:axe" (already added in Task 2).
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); process.exit(p.scripts['test:perf'] ? 0 : 1)" && echo "test:perf script present"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "test:perf" script
    - The script invokes either lhci or unlighthouse CLI
    - If @lhci/cli was chosen: lighthouserc.cjs exists in project root with CLS <= 0.1 and TBT < 50ms assertions
    - Running `npm run test:perf -- --help` (or equivalent) exits with code 0 (CLI is available)
  </acceptance_criteria>
  <done>package.json "test:perf" script present; Lighthouse CI or unlighthouse configured with CLS <= 0.1 and TBT < 50ms budget; the script can be invoked once a dev server is running</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment → production code | Mocks in unit tests must not mask real import errors that would fail in production |
| Playwright → live dev server | E2E tests require a running server; misconfigured baseURL silently skips real DOM assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | vi.mock("motion/react") in unit tests | accept | Mocks are test-only; TypeScript compilation (tsc --noEmit) in Wave 2 validates real import paths without mocks. |
| T-03-02 | Denial of Service | Axe audit blocking CI on false positives | mitigate | Scope axe tags to wcag2a + wcag2aa only; suppress informational notices via .withTags(). |
| T-03-03 | Information Disclosure | localStorage values logged in CI | accept | "motionEnabled" is a non-sensitive boolean preference; no PII involved. |
| T-03-SC | Tampering | npm/pip/cargo installs (@lhci/cli, @testing-library/react) | mitigate | Both are well-known official packages; verify on npmjs.com before install if flagged. |
</threat_model>

<verification>
After all three tasks complete:

1. `npx vitest run tests/unit` exits with code 0 (including 95% coverage threshold)
2. `npx vitest run tests/unit/lib/motion-utils.test.ts` exits with code 0 (including renderHook hook tests)
3. `npx playwright test tests/e2e/motion-accessibility.spec.ts --list` exits with code 0 and lists >= 4 tests
4. `grep "test:axe" package.json` returns one match
5. `grep "test:perf" package.json` returns one match
6. `grep "AxeBuilder" tests/e2e/motion-accessibility.spec.ts` returns one match
7. `npm run test:unit` exits with code 0 (no regression in existing unit tests)
8. lighthouserc.cjs exists (if @lhci/cli was chosen) with cumulative-layout-shift and total-blocking-time assertions
</verification>

<success_criteria>
- Four Vitest unit test files exist and pass
- useMotionEnabled hook is tested via renderHook with window.matchMedia mock (95%+ hook body coverage)
- tests/e2e/motion-accessibility.spec.ts exists with axe audit + 3 component test groups
- package.json "test:axe" script added
- package.json "test:perf" script added, pointing to Lighthouse CI or unlighthouse
- Lighthouse budget: CLS <= 0.1, TBT < 50ms
- npm run test:unit exits 0 (95%+ coverage, no regressions)
- Axe audit test passes against the existing page (zero WCAG 2.1 AA violations)
- All skipped tests have TODO comments documenting the page-integration prerequisite
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.1-phases/01-motion-effects/01-03-SUMMARY.md` documenting: which tests are active vs skipped and why, coverage percentages for the new files (including useMotionEnabled hook), the result of the axe audit (number of violations found, if any), and which Lighthouse tooling was selected (lhci vs unlighthouse) and why.
</output>
