# Phase 01: Motion Effects – Plan

**Date:** 2026-05-13
**Owner:** team (frontend)

## Goal
Add motion effects to landing‑page components using the **motion** npm package, while preserving accessibility (prefers‑reduced‑motion), performance (CLS ≤ 0.1), and SEO.

## Scope
- Install `motion` (v12.38.0).
- Create reusable utility `src/lib/motion-utils.ts` for reduced‑motion handling.
- Update existing components:
  - `src/components/CarouselMotion.tsx`
  - `src/components/MobileMenuMotion.tsx`
  - `src/components/SettingsToggle.tsx`
- Add new animated components as needed (e.g., `FadeIn` wrapper).
- Write unit and integration tests covering animation rendering and reduced‑motion behavior.
- Update CI to run Lighthouse check for CLS.

## Tasks (ordered)
1. **Setup** – Install package and add to `package.json`.
2. **Utility** – Implement `motion-utils.ts` with `useReducedMotion` helper.
3. **Component Refactor** – Replace static elements with `<motion.div>` using patterns from RESEARCH.
   - `CarouselMotion.tsx` – entrance fade/slide, scroll reveal.
   - `MobileMenuMotion.tsx` – slide‑in menu with gesture support.
   - `SettingsToggle.tsx` – hover lift, respect reduced‑motion.
4. **New Wrapper** – Create `FadeIn` component for generic use.
5. **Testing** – Add tests:
   - Verify components render without animation when `prefers-reduced-motion` is true.
   - Snapshot tests for animated output.
   - Lighthouse CLS test in CI.
6. **Documentation** – Update README with motion usage guidelines.
7. **Verification** – Run `gsd-verifier` to generate `VERIFICATION.md` confirming goals.

## Acceptance Criteria
- All affected components compile and pass `npm run test`.
- Lighthouse CLS ≤ 0.1 on CI.
- Accessibility audit shows no violations related to motion.
- `package.json` includes `"motion": "12.38.0"`.

## Risks & Mitigations
- **Risk:** Over‑animation causing CLS spikes.
  **Mitigation:** Limit durations ≤ 300 ms, use reduced‑motion fallback.
- **Risk:** Bundle size increase.
  **Mitigation:** Import only needed primitives, verify tree‑shaking.

## Verification Steps (post‑merge)
- Run `npm run build` and inspect bundle size (`.gz`).
- Execute `npm run lighthouse` and check CLS.
- Manual QA: ensure animations respect system reduced‑motion setting.

## Dependencies
- `motion` (npm) – installed in step 1.
- Existing React components.

---
Generated by gsd‑plan‑phase based on RESEARCH.md.
