---
phase: 02-page-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01-PLAN.md"
files_modified:
  - src/components/HeroMotion.tsx
  - src/components/layout/NavBar.astro
  - src/pages/index.astro
  - src/components/ui/Button.astro
autonomous: true
requirements:
  - MOT-01
  - MOT-03
must_haves:
  truths:
    - "O conteúdo do Hero faz fade-in (opacity 0→1, translateY 20px→0) ao entrar no viewport"
    - "O fade-in do Hero ocorre apenas uma vez por visita (viewport.once: true)"
    - "Com prefers-reduced-motion ativo, o Hero aparece imediatamente sem animação"
    - "O conteúdo do Hero é visível mesmo antes da hidratação React (CSS fallback opacity:1)"
    - "A NavBar exibe background mais opaco e box-shadow ao rolar além do Hero"
    - "A transição da NavBar usa CSS ease-out 0.2s e não causa CLS"
    - "Com prefers-reduced-motion ativo, a NavBar não transiciona — muda instantaneamente"
    - "Os botões CTA do Hero elevam 2px e clarificam ao receber hover"
    - "Com prefers-reduced-motion ativo, o hover dos CTAs não aplica transform — apenas muda brilho"
    - "Links do NavBar em hover exibem destaque de cor com transição suave"
    - "Ao rolar, o link correspondente à seção visível recebe aria-current='true' via IntersectionObserver"
  artifacts:
    - path: "src/components/HeroMotion.tsx"
      provides: "Wrapper React com motion.div whileInView + MotionConfig reducedMotion='user'"
      exports: ["HeroMotion"]
    - path: "src/components/layout/NavBar.astro"
      provides: "CSS data-scrolled + IntersectionObserver script para sticky scrolled state + IntersectionObserver para active section + aria-current nos links"
      contains: "data-scrolled"
    - path: "src/pages/index.astro"
      provides: "Monta HeroMotion com client:visible envolvendo o Hero; CSS fallback hero-content"
    - path: "src/components/ui/Button.astro"
      provides: "CSS hover com transform translateY(-2px) e filter brightness nos CTAs do Hero"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/HeroMotion.tsx"
      via: "client:visible island"
      pattern: "HeroMotion"
    - from: "src/components/layout/NavBar.astro"
      to: "DOM #top"
      via: "IntersectionObserver observa sentinel #top"
      pattern: "data-scrolled"
    - from: "src/components/HeroMotion.tsx"
      to: "motion/react"
      via: "import motion, MotionConfig"
      pattern: "whileInView"
    - from: "src/components/layout/NavBar.astro"
      to: "DOM sections"
      via: "IntersectionObserver observa #metodo, #ementa, #investimento, #depoimentos"
      pattern: "aria-current"
---

<objective>
Implementar as animações de scroll e o sticky state da NavBar. Este plano cobre: (1) wrapper React HeroMotion.tsx com fade-in ao entrar no viewport via whileInView, respeitando prefers-reduced-motion via MotionConfig, com CSS fallback para garantir visibilidade sem JS; (2) sticky NavBar com transição CSS de background/shadow usando IntersectionObserver + data-scrolled attribute, sem React; (3) hover states nos botões CTA do Hero com transform e brightness; (4) indicadores de seção ativa nos links do NavBar via IntersectionObserver + aria-current.

(Nota: parallax scroll removido per D-SCOPE-02-B — substituído por whileInView fade-in para preservar CLS ≤ 0.1 e conformidade com WCAG 2.1 AA.)

Purpose: Completa os requisitos de scroll animation (Hero fade-in), navigation behavior (sticky scrolled), hover nos CTAs e active state nos links de REQUIREMENTS.md, seguindo as decisões arquiteturais de D-SCOPE-02-B (sem parallax) e D-SCOPE-02-C (motion/react para React islands, CSS+IO para Astro components).

Output: HeroMotion.tsx (novo componente), NavBar.astro atualizado com scripts IO e CSS scrolled/active state, index.astro com HeroMotion client:visible + CSS fallback, Button.astro com CSS hover elevação.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Repo/landing-page/deep-dive-vm/.planning/ROADMAP.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.1-phases/02-page-integration/02-CONTEXT.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.1-phases/02-page-integration/02-motion-integration-RESEARCH.md
@C:/Repo/landing-page/deep-dive-vm/.planning/milestones/v1.1-phases/02-page-integration/02-01-SUMMARY.md

<interfaces>
<!-- Contratos e seletores extraídos do codebase. O executor NÃO precisa explorar esses arquivos. -->

De src/components/layout/NavBar.astro (estado atual, pós 02-01):
```
Seletor raiz: nav.nav (elemento <nav class="nav" aria-label="Navegação principal">)
CSS existente:
  .nav { position: sticky; top: 0; z-index: 40; background: rgba(10,15,30,0.85); backdrop-filter: blur(14px); border-bottom: 1px solid var(--hairline); }
Sentinel: <header id="top"> em src/components/sections/Hero.astro (já existe no DOM)
O script de IntersectionObserver deve ser adicionado no mesmo bloco <script> do hamburger (ou em bloco separado — Astro faz bundle de múltiplos script tags).

Links de navegação atuais (anchor links para seções da landing page):
  .nav-links a — links como href="#metodo", href="#ementa", href="#investimento", href="#depoimentos"
  :global(.nav-cta) — botão "Garantir Vaga"
```

De src/components/sections/Hero.astro:
```
Elemento raiz: <header id="top" class="hero">
O id="top" é o sentinel natural para o IntersectionObserver de scroll.
O conteúdo do Hero está em header.hero > div.container.hero-grid > div.hero-content
Os botões CTA são renderizados pelo componente <Button> e recebem classes .btn.primary e .btn.ghost
```

De src/components/ui/Button.astro (estado atual — SERÁ ATUALIZADO):
```
Classe base: .btn (display inline-flex, border-radius 2px, transition: all 0.25s)
Variante primary hover: .btn.primary:hover { background: var(--chama-hover); box-shadow: 0 0 36px rgba(0,255,255,0.6)... }
Variante ghost hover: .btn.ghost:hover { background: rgba(0,255,255,0.06); box-shadow: 0 0 20px rgba(0,255,255,0.3); }
Nota: hover com transform translateY(-2px) NÃO existe ainda — será adicionado neste plano
```

De src/lib/motion-utils.ts (para referência no HeroMotion):
```tsx
export function useMotionEnabled(): [boolean, (value: boolean) => void]
// HeroMotion pode usar useMotionEnabled para verificação adicional,
// mas MotionConfig reducedMotion="user" já cobre prefers-reduced-motion automaticamente.
```

APIs motion/react a usar no HeroMotion.tsx:
```tsx
import { motion, MotionConfig } from "motion/react";
// motion.div com:
//   initial={{ opacity: 0, y: 20 }}
//   whileInView={{ opacity: 1, y: 0 }}
//   transition={{ duration: 0.15, ease: "easeOut" }}
//   viewport={{ once: true, amount: 0.3 }}
// MotionConfig com reducedMotion="user" envolve o motion.div
```

De src/pages/index.astro (pós 02-01):
```astro
// Hero é renderizado como: <Hero />
// Será substituído por: <HeroMotion client:visible><Hero /></HeroMotion>
// client:visible: hidrata apenas quando elemento está próximo do viewport — reduz TBT inicial
```

Contexto de active state no NavBar (single-page):
```
IMPORTANTE: Todos os links do NavBar são anchor links (#metodo, #ementa, #investimento, #depoimentos).
Esta é uma landing page de página única — não há múltiplas rotas. Por isso, aria-current baseado
em Astro.url.pathname não se aplica aqui (seria sempre "true" para todos os links).
A solução correta é IntersectionObserver que observa cada seção e adiciona aria-current="true"
ao link correspondente enquanto a seção está no viewport.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar HeroMotion.tsx e conectar no index.astro com client:visible + CSS fallback</name>
  <files>
    src/components/HeroMotion.tsx,
    src/pages/index.astro
  </files>
  <behavior>
    - HeroMotion aceita children: React.ReactNode
    - HeroMotion envolve children em MotionConfig com reducedMotion="user"
    - Dentro do MotionConfig, um motion.div com initial={{ opacity: 0, y: 20 }}, whileInView={{ opacity: 1, y: 0 }}, transition={{ duration: 0.15, ease: "easeOut" }}, viewport={{ once: true, amount: 0.3 }}
    - Com prefers-reduced-motion: reduce ativo, MotionConfig desabilita transforms — children aparecem imediatamente sem animação (opacity 0 nunca aplicado; motion trata reducedMotion="user" desabilitando initial/animate)
    - Em index.astro, Hero fica dentro de HeroMotion client:visible
    - HeroMotion não usa client:load (aumenta TBT) — usa client:visible (per D-SCOPE-02-C)
    - CSS fallback em Hero.astro: .hero-content { opacity: 1 } garante visibilidade antes da hidratação React
  </behavior>
  <action>
    1. Criar src/components/HeroMotion.tsx. Importar React e { motion, MotionConfig } de "motion/react". Definir interface HeroMotionProps com children: React.ReactNode. Exportar componente HeroMotion: FC que retorna MotionConfig com prop reducedMotion="user" envolvendo um motion.div com as props: initial={{ opacity: 0, y: 20 }}, whileInView={{ opacity: 1, y: 0 }}, transition={{ duration: 0.15, ease: "easeOut" }}, viewport={{ once: true, amount: 0.3 }}. O motion.div renderiza {children} internamente. Não adicionar estilos inline no motion.div — ele não deve alterar o layout do Hero.

    2. Abrir src/pages/index.astro. Adicionar import de HeroMotion no bloco de imports do frontmatter: import { HeroMotion } from "../components/HeroMotion". No template, localizar a tag <Hero /> e substituí-la por <HeroMotion client:visible><Hero /></HeroMotion>. A diretiva client:visible garante que a hidratação React só ocorra quando o elemento se aproximar do viewport.

    3. Abrir src/components/sections/Hero.astro. No bloco <style> existente, adicionar a seguinte regra CSS ao seletor .hero-content (ou criar o seletor se não existir): opacity: 1. Esta regra garante que o conteúdo do Hero seja visível mesmo que o JavaScript não tenha carregado ou a hidratação do HeroMotion ainda não tenha ocorrido. Quando o HeroMotion hidrata, o motion.div sobrescreve a opacity via inline style durante a animação; ao final da animação (opacity: 1), não há conflito visual.

    Nota importante: MotionConfig com reducedMotion="user" intercepta as props initial/animate em componentes motion filhos e as desabilita quando prefers-reduced-motion está ativo. O Hero não ficará invisível para usuários com reduced motion porque o motion reseta opacity para 1 e y para 0 antes de renderizar quando motion está desabilitado.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>
    src/components/HeroMotion.tsx existe e exporta HeroMotion; index.astro contém HeroMotion client:visible envolvendo Hero; Hero.astro contém .hero-content { opacity: 1 } no bloco de estilos; build sem erros TypeScript; grep "whileInView" src/components/HeroMotion.tsx retorna match.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar sticky scrolled state à NavBar (CSS + IntersectionObserver)</name>
  <files>
    src/components/layout/NavBar.astro
  </files>
  <action>
    Abrir src/components/layout/NavBar.astro e realizar duas adições:

    1. No bloco CSS (dentro de <style>): Adicionar ao seletor .nav existente a propriedade transition: background 0.2s ease-out, box-shadow 0.2s ease-out. Adicionar novo seletor .nav[data-scrolled="true"] com background: rgba(10,15,30,0.97) e box-shadow: 0 4px 24px rgba(0,0,0,0.45). Adicionar bloco @media (prefers-reduced-motion: reduce) que sobrescreve .nav com transition: none — removendo completamente a transição CSS quando o sistema reporta preferência por movimento reduzido. O seletor .nav[data-scrolled="true"] permanece válido no contexto reduced-motion (background mais opaco aparece instantaneamente, sem animação).

    2. No bloco <script> (adicionar bloco separado ao final do arquivo, após o script do hamburger que já existe): Selecionar o elemento .nav com document.querySelector(".nav") tipado como HTMLElement | null. Selecionar o sentinel com document.getElementById("top"). Se ambos existirem, criar IntersectionObserver com callback que recebe entries: o callback pega entries[0] e define nav.dataset.scrolled = String(!entries[0].isIntersecting). Opções do observer: { threshold: 0 }. Chamar observer.observe(sentinel). O data-scrolled="true" é adicionado quando #top sai do viewport (usuário rolou além do Hero); data-scrolled="false" quando #top volta a ser visível.

    Não converter NavBar para React. Não usar motion/react aqui. A abordagem é 100% CSS + browser API nativa (per D-SCOPE-02-C e decisão da RESEARCH.md Tópico 3).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "^.*error" | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>
    NavBar.astro contém seletor CSS .nav[data-scrolled="true"] com background e box-shadow; contém @media prefers-reduced-motion que zera transition do .nav; contém script com IntersectionObserver observando #top; build sem erros; grep "data-scrolled" src/components/layout/NavBar.astro retorna ao menos 2 matches (CSS e script).
  </done>
</task>

<task type="auto">
  <name>Task 3: Adicionar hover states nos botões CTA do Hero (CSS em Button.astro)</name>
  <files>
    src/components/ui/Button.astro
  </files>
  <action>
    Abrir src/components/ui/Button.astro e realizar as seguintes adições no bloco <style>:

    1. No seletor .btn existente, adicionar will-change: transform à lista de propriedades. O seletor .btn já tem transition: all 0.25s — substituir por transition: transform 0.15s ease-out, filter 0.15s ease-out, background 0.15s ease-out, box-shadow 0.15s ease-out para controle granular e alinhamento com os timings de animação da fase (per D-SCOPE-02-D, ≤ 150ms ease-out).

    2. Adicionar seletor .btn:hover com: transform: translateY(-2px). Este é o hover base para todos os botões — o lift sutil de 2px.

    3. No seletor .btn.primary:hover já existente, adicionar: filter: brightness(1.1) para clarificar o botão primário no hover além do translateY herdado de .btn:hover.

    4. No seletor .btn.ghost:hover já existente, adicionar: filter: brightness(1.15) para o mesmo efeito no ghost button.

    5. Adicionar bloco @media (prefers-reduced-motion: reduce) contendo:
       - .btn { will-change: auto; }
       - .btn:hover { transform: none; }
       Isso preserva as mudanças de background e box-shadow nos hovers existentes (.btn.primary:hover, .btn.ghost:hover), mas remove o transform translateY e o filtro de brilho para usuários com preferência por movimento reduzido.

    Esta é uma mudança puramente CSS em Button.astro. Não adicionar JavaScript. Não converter para React. Os botões .btn.primary e .btn.ghost em Hero.astro herdam estas regras automaticamente (per D-SCOPE-02-C).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>
    Button.astro contém .btn:hover { transform: translateY(-2px) }; contém filter: brightness nos hovers primário e ghost; contém @media prefers-reduced-motion que remove transform e will-change; build sem erros; grep "translateY(-2px)" src/components/ui/Button.astro retorna match; grep "prefers-reduced-motion" src/components/ui/Button.astro retorna match.
  </done>
</task>

<task type="auto">
  <name>Task 4: Adicionar indicadores de seção ativa nos links do NavBar (IntersectionObserver + aria-current)</name>
  <files>
    src/components/layout/NavBar.astro
  </files>
  <action>
    Abrir src/components/layout/NavBar.astro e realizar duas adições:

    1. No bloco CSS (dentro de <style>): Adicionar ao seletor .nav-links a (ou criar se não existir) a propriedade transition: color 0.15s ease-out, border-color 0.15s ease-out para hover suave. Adicionar seletor .nav-links a[aria-current="true"] com: color: var(--nucleo-eletrico, #00FFFF) e border-bottom: 2px solid currentColor. Adicionar seletor .nav-links a:hover com: color: var(--nucleo-eletrico, #00FFFF). Adicionar @media (prefers-reduced-motion: reduce) com .nav-links a { transition: none } para remover a transição de cor quando o usuário prefere movimento reduzido.

    2. Adicionar novo bloco <script> ao final do arquivo (separado dos scripts do hamburger e do IntersectionObserver de scroll):

    O script implementa active section detection para single-page landing page. Como todos os links são anchor links (não rotas), aria-current baseado em pathname não se aplica. Em vez disso:
    - Selecionar todos os links do NavBar: document.querySelectorAll(".nav-links a[href^='#']")
    - Para cada link, extrair o id da seção do href (ex: "#metodo" → "metodo")
    - Selecionar o elemento de seção correspondente (document.getElementById(sectionId))
    - Criar um único IntersectionObserver com threshold: 0.3 que, ao detectar uma seção como intersecting, remove aria-current="true" de todos os links e adiciona aria-current="true" apenas ao link cujo href corresponde à seção visível
    - Observer observa todos os elementos de seção encontrados
    - Inicializar sem aria-current em nenhum link (estado neutro ao carregar no topo da página)

    Não usar React. Não usar motion/react. Esta é solução pura CSS + script nativo (per D-SCOPE-02-C).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "^.*error" | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>
    NavBar.astro contém CSS .nav-links a[aria-current="true"] com color e border-bottom; contém .nav-links a com transition de color; contém @media prefers-reduced-motion que remove transition dos links; contém script com IntersectionObserver observando seções e atualizando aria-current nos links; build sem erros; grep "aria-current" src/components/layout/NavBar.astro retorna ao menos 2 matches (CSS e script).
  </done>
</task>

</tasks>

<threat_model>
## Fronteiras de Confiança

| Fronteira | Descrição |
|-----------|-----------|
| Browser API → DOM | IntersectionObserver escreve apenas em dataset.scrolled do elemento .nav e aria-current dos links — sem input do usuário |
| React island → DOM | HeroMotion apenas aplica transforms CSS via motion/react — sem acesso a dados externos |
| CSS hover → DOM | Transformações CSS puras nos botões CTA; sem JavaScript envolvido |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|---------------------|
| T-02-05 | Denial of Service | IntersectionObserver no NavBar | accept | IO tem overhead mínimo; roda no main thread apenas em threshold events (não por pixel de scroll); risco de performance negligenciável |
| T-02-06 | Tampering | dataset.scrolled em NavBar | accept | Atributo data-* não contém PII nem dados sensíveis; afeta apenas visual CSS; não pode ser usado para injeção |
| T-02-07 | Elevation of Privilege | client:visible HeroMotion | accept | Island hidratado apenas com código de animação; sem acesso a dados privilegiados; prefers-reduced-motion respeitado via MotionConfig |
| T-02-08 | Information Disclosure | motion/react no HeroMotion | accept | Apenas aplica transforms CSS; sem chamadas de rede; sem acesso a localStorage ou cookies |
| T-02-09 | Tampering | aria-current em links do NavBar | accept | Atributo aria manipulado apenas por IntersectionObserver interno; não aceita input do usuário; afeta apenas estado de acessibilidade visual |
| T-02-SC | Tampering | Nenhuma instalação de pacote npm neste plano | accept | motion já está no package.json@12.38.0; zero risco de supply chain neste plano |
</threat_model>

<verification>
## Verificações pós-execução

1. Build sem erros: `npm run build` com exit 0
2. HeroMotion existe e usa whileInView: `grep -c "whileInView" src/components/HeroMotion.tsx` retorna 1
3. HeroMotion usa MotionConfig: `grep -c "MotionConfig" src/components/HeroMotion.tsx` retorna ao menos 1
4. index.astro usa client:visible: `grep -c "client:visible" src/pages/index.astro` retorna ao menos 1
5. Hero.astro tem CSS fallback: `grep -c "hero-content" src/components/sections/Hero.astro` retorna ao menos 1 (no CSS block com opacity: 1)
6. NavBar tem CSS scrolled: `grep -c "data-scrolled" src/components/layout/NavBar.astro` retorna ao menos 2
7. NavBar tem reduced-motion CSS: `grep -c "prefers-reduced-motion" src/components/layout/NavBar.astro` retorna ao menos 1
8. NavBar tem IntersectionObserver: `grep -c "IntersectionObserver" src/components/layout/NavBar.astro` retorna ao menos 2 (scroll + active section)
9. NavBar tem aria-current nos links: `grep -c "aria-current" src/components/layout/NavBar.astro` retorna ao menos 2 (CSS e script)
10. Button.astro tem hover translateY: `grep -c "translateY(-2px)" src/components/ui/Button.astro` retorna ao menos 1
11. Button.astro tem prefers-reduced-motion: `grep -c "prefers-reduced-motion" src/components/ui/Button.astro` retorna ao menos 1
12. Tipo: `npx tsc --noEmit` sem erros em HeroMotion.tsx
</verification>

<success_criteria>
- HeroMotion.tsx criado com motion.div whileInView + MotionConfig reducedMotion="user"
- Hero em index.astro envolto em HeroMotion client:visible
- Hero.astro tem .hero-content { opacity: 1 } garantindo visibilidade sem JS
- NavBar.astro tem CSS .nav transition + .nav[data-scrolled="true"] com background opaco e shadow
- NavBar.astro tem @media prefers-reduced-motion que remove transition
- NavBar.astro tem script IntersectionObserver observando #top para setar data-scrolled
- Button.astro tem .btn:hover { transform: translateY(-2px) } e filter: brightness nos hovers
- Button.astro tem @media prefers-reduced-motion removendo transform do hover
- NavBar.astro tem CSS .nav-links a[aria-current="true"] com color e border-bottom
- NavBar.astro tem script IntersectionObserver observando seções para setar aria-current nos links
- Build sem erros TypeScript
- Nenhum pacote novo instalado
</success_criteria>

<output>
Após execução, criar `.planning/milestones/v1.1-phases/02-page-integration/02-02-SUMMARY.md` com:
- Arquivos criados/modificados
- Decisões tomadas durante implementação
- Verificações realizadas
- Qualquer desvio do plano e justificativa
</output>
