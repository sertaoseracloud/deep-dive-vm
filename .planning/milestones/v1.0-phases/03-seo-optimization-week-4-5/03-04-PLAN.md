---
phase: 03-seo-optimization-week-4-5
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - seo-performance-gate
  - seo-image-optimization
  - seo-font-loading
  - seo-test-expansion
  - seo-sitemap

must_haves:
  truths:
    - "All 13 SEO static assertions pass"
    - "Lighthouse CI gates pass: SEO >= 90, Accessibility >= 90, Performance >= 80"
    - "No render-blocking Google Fonts link in dist/index.html"
    - "dist/sitemap-index.xml exists and is valid XML"
    - "All three PNG images are served as WebP in built output"
  artifacts:
    - path: "dist/index.html"
      provides: "Final optimized landing page HTML"
      contains: "rel=\"preload\" as=\"style\""
    - path: "dist/sitemap-index.xml"
      provides: "Sitemap entry point for search crawlers"
      contains: "sitemapindex"
    - path: "dist/_astro"
      provides: "WebP image assets"
      contains: ".webp"
  key_links:
    - from: ".lighthouserc.json"
      to: "LHCI runner"
      via: "npx lhci autorun"
      pattern: "categories:performance.*error"
    - from: "tests/seo/seo-meta.test.ts"
      to: "dist/index.html"
      via: "npx vitest run tests/seo"
      pattern: "DIST_INDEX"
---

<objective>
Phase gate: run the full test suite and Lighthouse CI audit to confirm all Phase 3 requirements are met. This plan produces no code changes — it validates the output of Waves 1-3 and surfaces any remaining issues for human review before the phase is closed.

Purpose: Confirm all five requirement IDs (seo-performance-gate, seo-image-optimization, seo-font-loading, seo-test-expansion, seo-sitemap) are satisfied and the CI pipeline would pass on the current branch.
Output: Lighthouse report confirming Performance >= 80 and Accessibility >= 90, plus full test suite green.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-optimization-week-4-5/03-CONTEXT.md
@.planning/phases/03-seo-optimization-week-4-5/03-01-SUMMARY.md
@.planning/phases/03-seo-optimization-week-4-5/03-02-SUMMARY.md
@.planning/phases/03-seo-optimization-week-4-5/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite and build verification</name>
  <files>dist/index.html dist/sitemap-index.xml .lighthouserc.json tests/seo/seo-meta.test.ts</files>
  <read_first>
    - .lighthouserc.json — confirm 4 assertion entries are present before running LHCI
    - dist/sitemap-index.xml — confirm it exists (should be present from Wave 3 build)
  </read_first>
  <action>
    Run the complete verification sequence in order:

    Step 1 — Fresh build (ensures dist/ reflects all Wave 3 changes):
      npm run build

    Confirm exit code 0. If the build fails, stop and investigate — do not proceed to tests with a stale dist/.

    Step 2 — SEO static assertions (all 13):
      npx vitest run tests/seo

    Expected: 13 tests pass, 0 fail. Record any failures.

    Step 3 — Full unit + integration suite (regression check):
      npm run test:all

    Expected: all tests pass. Note: `tests/seo/seo-meta.test.ts` is NOT in `vitest.config.ts` include globs — it is NOT run by `test:all`; it was run explicitly in Step 2.

    Step 4 — Lighthouse CI audit:
      npx lhci autorun

    This reads `.lighthouserc.json` and runs 3 Lighthouse collections against `staticDistDir: ./dist`.
    Expected results (per D-01, D-02):
    - `categories:seo`: >= 0.9 [error gate] — MUST pass
    - `categories:accessibility`: >= 0.9 [error gate] — MUST pass
    - `categories:best-practices`: >= 0.8 [warn gate] — may warn, will not fail
    - `categories:performance`: >= 0.8 [error gate] — MUST pass (new gate from D-01)

    LHCI exit code 0 = all error gates pass. Non-zero = at least one error gate failed.

    Step 5 — Inspect dist/ for correctness:
      node -e "
        const fs = require('fs');
        const h = fs.readFileSync('dist/index.html','utf8');
        const checks = [
          [/rel=\"preload\"[^>]*as=\"style\"[^>]*fonts\.googleapis\.com/i, 'preload font link'],
          [/<noscript>[\s\S]*?fonts\.googleapis\.com[\s\S]*?<\/noscript>/i, 'noscript fallback'],
          [/loading=\"eager\"/i, 'eager loading for hero'],
          [/fetchpriority=\"high\"/i, 'fetchpriority=high for hero'],
          [/loading=\"lazy\"/i, 'lazy loading for mentor images'],
          [/application\/ld\+json/i, 'JSON-LD script'],
        ];
        let ok = true;
        for (const [re, name] of checks) {
          if (!re.test(h)) { console.error('FAIL: missing', name); ok = false; }
          else { console.log('PASS:', name); }
        }
        if (!fs.existsSync('dist/sitemap-index.xml')) { console.error('FAIL: sitemap-index.xml missing'); ok = false; }
        else { console.log('PASS: sitemap-index.xml exists'); }
        if (!ok) process.exit(1);
        console.log('All checks passed');
      "

    Record results of all steps in the SUMMARY.md.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/seo && npm run test:all && npx lhci autorun</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npx vitest run tests/seo` exits 0 with 13/13 tests reported as passing
    - `npm run test:all` exits 0 with no regressions in unit/integration tests
    - `npx lhci autorun` exits 0 (all error-level gates pass: SEO >= 90, Accessibility >= 90, Performance >= 80)
    - The node inspection script above exits 0 with all 7 "PASS" lines
    - No `rel="stylesheet"` for Google Fonts outside of noscript in dist/index.html
    - `dist/sitemap-index.xml` exists and is non-empty
    - `dist/_astro/` contains `.webp` files for all three images
  </acceptance_criteria>
  <done>Full suite green. LHCI audit passes all four gates. All Phase 3 requirement IDs are satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on visual and functional quality</name>
  <what-built>
    Wave 1-3 automated changes:
    - @astrojs/sitemap installed; astro.config.mjs updated; dist/sitemap-index.xml generated
    - .lighthouserc.json: accessibility promoted to error, performance gate added at >= 80
    - Hero image: WebP via Astro Image, loading=eager, fetchpriority=high
    - Mentor images (claudio2 + marcelo): WebP via Astro Image, loading=lazy
    - Google Fonts: rel=preload + onload swap + noscript fallback (no more render-blocking stylesheet)
    - 3 new SEO test assertions (JSON-LD, heading hierarchy, sitemap)
  </what-built>
  <how-to-verify>
    1. Run `npm run preview` to start the preview server (serves from dist/).
    2. Open the site in a browser.
    3. Check: hero portrait image loads correctly (no broken image, no distortion, fills the frame).
    4. Check: mentor portraits (Cláudio + Marcelo) load correctly below the fold.
    5. Check: custom fonts (Space Grotesk, Chakra Petch) appear — there may be a brief FOUT (Flash of Unstyled Text) before fonts load; this is expected and correct.
    6. Scroll through the full page — confirm no layout shifts or broken sections.
    7. Open DevTools > Network > filter by "webp" — confirm hero and mentor images are served as .webp files.
    8. Open DevTools > Network > filter by "fonts.googleapis" — confirm the font CSS request is NOT render-blocking (it should appear after first paint, not before).
    9. Open DevTools > Console — confirm no JavaScript errors.
    10. Visit `[preview URL]/sitemap-index.xml` (note: due to base path config, this may be at `/deep-dive-vm/sitemap-index.xml`). OR check that `dist/sitemap-index.xml` opens in a text editor and shows valid XML with the site URL.
  </how-to-verify>
  <resume-signal>Type "approved" if the page looks correct, images load as WebP, fonts load without errors, and the sitemap XML is present. Describe any visual issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LHCI runner → dist/ static files | LHCI serves files from dist/ locally; no external network calls during collection |
| Vitest → dist/index.html | Test reads our own build output; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | LHCI score threshold | accept | LHCI reads from our `.lighthouserc.json` in the repo; the config is version-controlled and was verified in Wave 1; no external input modifies it |
| T-03-04-02 | Denial of Service | LHCI performance gate flapping | mitigate | `numberOfRuns: 3` with `aggregationMethod: optimistic` (LHCI default) uses the best of 3 runs; the 80% threshold provides headroom; if CI flaps, re-run before investigating code changes |
</threat_model>

<verification>
Phase 3 is complete when:
- All 5 requirement IDs covered:
  - seo-performance-gate: `categories:performance` gate in .lighthouserc.json, passing in LHCI
  - seo-image-optimization: all three PNGs migrated to Astro Image (WebP output confirmed in dist/)
  - seo-font-loading: preload+onload pattern in Layout.astro, no render-blocking stylesheet
  - seo-test-expansion: 13 assertions in seo-meta.test.ts, all passing
  - seo-sitemap: @astrojs/sitemap installed, astro.config.mjs updated, dist/sitemap-index.xml present
- Human checkpoint approved
- All automated checks exit 0
</verification>

<success_criteria>
- `npx vitest run tests/seo` exits 0 (13/13 passing)
- `npx lhci autorun` exits 0 (Performance >= 80, SEO >= 90, Accessibility >= 90)
- Human verifies no visual regressions in the preview
- All five Phase 3 requirement IDs (seo-performance-gate, seo-image-optimization, seo-font-loading, seo-test-expansion, seo-sitemap) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-optimization-week-4-5/03-04-SUMMARY.md`
</output>
