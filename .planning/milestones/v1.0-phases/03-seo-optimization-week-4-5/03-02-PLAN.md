---
phase: 03-seo-optimization-week-4-5
plan: 02
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tests/seo/seo-meta.test.ts
autonomous: true
requirements:
  - seo-test-expansion

must_haves:
  truths:
    - "Three new it() blocks are appended to seo-meta.test.ts"
    - "Test 11 (JSON-LD) is RED or GREEN when run against current dist/"
    - "Test 12 (heading hierarchy) is RED or GREEN when run against current dist/"
    - "Test 13 (sitemap) is RED when run before Wave 3 build, GREEN after Wave 3 build"
    - "Running npx vitest run tests/seo exits non-zero until all three pass"
  artifacts:
    - path: "tests/seo/seo-meta.test.ts"
      provides: "Extended SEO assertion suite with 13 total tests"
      contains: "application/ld+json"
    - path: "tests/seo/seo-meta.test.ts"
      provides: "Heading hierarchy assertion"
      contains: "Heading hierarchy"
    - path: "tests/seo/seo-meta.test.ts"
      provides: "Sitemap file existence assertion"
      contains: "sitemap-index.xml"
  key_links:
    - from: "tests/seo/seo-meta.test.ts"
      to: "dist/index.html"
      via: "readFileSync(DIST_INDEX)"
      pattern: "DIST_INDEX"
    - from: "tests/seo/seo-meta.test.ts"
      to: "dist/sitemap-index.xml"
      via: "existsSync(join(DIST_DIR, 'sitemap-index.xml'))"
      pattern: "DIST_DIR"
---

<objective>
Write three new failing SEO assertions using TDD RED→GREEN protocol. This plan produces the RED state — tests are written first, confirmed to be in the expected pass/fail state against the current dist/, and committed before any production code changes in Wave 3.

Purpose: Enforce D-06 decisions as verifiable test contracts before implementation. Test 13 (sitemap) is the canonical RED test for this phase — it fails until Wave 3 `npm run build` with the new astro.config.mjs produces `dist/sitemap-index.xml`.
Output: `tests/seo/seo-meta.test.ts` with 3 new `it()` blocks (assertions 11, 12, 13) and a `DIST_DIR` constant.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-optimization-week-4-5/03-CONTEXT.md
@.planning/phases/03-seo-optimization-week-4-5/03-RESEARCH.md
@.planning/phases/03-seo-optimization-week-4-5/03-01-SUMMARY.md

<interfaces>
<!-- Existing helpers in tests/seo/seo-meta.test.ts the executor must use: -->

From tests/seo/seo-meta.test.ts (current state — read before editing):
```typescript
import { describe, it, expect, beforeAll } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";

const __dirname = dirname(fileURLToPath(import.meta.url));
const DIST_INDEX = join(__dirname, "../../dist/index.html");
let html = "";

beforeAll(() => {
  if (!existsSync(DIST_INDEX)) { throw new Error(...); }
  html = readFileSync(DIST_INDEX, "utf-8");
});
```

New constant to add alongside DIST_INDEX:
```typescript
const DIST_DIR = join(__dirname, "../../dist");
```

The `existsSync` and `join` are already imported — no new imports required.
</interfaces>
</context>

<feature>
  <name>Three new SEO assertions: JSON-LD, heading hierarchy, sitemap (D-06)</name>
  <files>tests/seo/seo-meta.test.ts</files>
  <behavior>
    Test 11 — JSON-LD parse:
    - GIVEN dist/index.html exists with a `<script type="application/ld+json">` block
    - WHEN the test reads the block and calls JSON.parse()
    - THEN @context is truthy AND @type is truthy
    - RED state: test should PASS against current dist/ (JSON-LD already present in Layout.astro)
    - Role: regression guard ensuring JSON-LD is never accidentally removed

    Test 12 — Heading hierarchy:
    - GIVEN dist/index.html exists
    - WHEN all h1–h6 tags are extracted in document order
    - THEN: at least one H1 exists; no H2 appears before the first H1; no consecutive heading pair jumps more than 1 level downward
    - RED state: PASSES against current dist/ (Hero.astro has H1, sections use H2)
    - Role: regression guard; catches if a refactor adds H3 before H2

    Test 13 — Sitemap file existence:
    - GIVEN the build has been run
    - WHEN checking existsSync(join(DIST_DIR, "sitemap-index.xml"))
    - THEN the result is true
    - RED state before Wave 3: FAILS — dist/sitemap-index.xml does not exist yet (even after Wave 1 build, the astro.config.mjs change is in Wave 3)
    - Wait: Wave 1 Task 2 already added sitemap() to astro.config.mjs AND ran npm run build — so dist/sitemap-index.xml already exists at this point. Test 13 may already be GREEN if Wave 1 ran fully.
    - Regardless: write the test, run it, record the result. If GREEN, that is correct — Wave 1 already made it green. If RED for any reason, Wave 3 fixes it.
  </behavior>
  <implementation>
    Append ONLY to the existing `describe` block — do NOT create a new describe block.
    Add the `DIST_DIR` constant immediately after `DIST_INDEX` at the top of the file.
    Append the three `it()` blocks at the end of the existing describe block, before the closing `});`.
    Do NOT modify any existing test (assertions 1–10 are unchanged).
    Do NOT add try/catch around JSON.parse — Vitest catches thrown errors as test failures, which is the desired behavior.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write 3 new SEO assertions — RED phase (D-06a, D-06b, D-06c)</name>
  <files>tests/seo/seo-meta.test.ts</files>
  <read_first>
    - tests/seo/seo-meta.test.ts — read the ENTIRE file before editing; note the existing imports, constants, helpers, and the structure of the describe block (assertions 1–10)
    - dist/index.html — if it exists, check for `<script type="application/ld+json">` to confirm Test 11 expected state
    - dist/sitemap-index.xml — check existsSync to determine Test 13 expected state before writing
  </read_first>
  <behavior>
    - Test 11: JSON.parse of ld+json block yields { "@context": truthy, "@type": truthy }
    - Test 12: headings array contains 1; no H2 index before first H1 index; no consecutive downward jump > 1
    - Test 13: existsSync(join(DIST_DIR, "sitemap-index.xml")) === true
  </behavior>
  <action>
    Step 1 — Add DIST_DIR constant.
    In `tests/seo/seo-meta.test.ts`, immediately after the line:
      `const DIST_INDEX = join(__dirname, "../../dist/index.html");`
    add:
      `const DIST_DIR = join(__dirname, "../../dist");`

    Step 2 — Append the three it() blocks.
    Inside the existing `describe("SEO meta-tag static assertions (dist/index.html)", () => { ... })` block, after the last existing `it()` (assertion 10, which ends around line 129), append these three assertions:

      it("11. JSON-LD <script type=application/ld+json> is present and parseable with @context and @type", () => {
        const match = html.match(
          /<script[^>]+type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/i
        );
        expect(match).not.toBeNull();
        const parsed = JSON.parse(match![1]);
        expect(parsed["@context"]).toBeTruthy();
        expect(parsed["@type"]).toBeTruthy();
      });

      it("12. Heading hierarchy: no H2 before first H1, no heading level skip", () => {
        const headingMatches = [...html.matchAll(/<(h[1-6])[\s>]/gi)];
        const levels = headingMatches.map((m) => parseInt(m[1].slice(1), 10));
        expect(levels).toContain(1);
        const firstH1Index = levels.indexOf(1);
        const firstH2Index = levels.indexOf(2);
        if (firstH2Index !== -1) {
          expect(firstH2Index).toBeGreaterThan(firstH1Index);
        }
        for (let i = 1; i < levels.length; i++) {
          const diff = levels[i] - levels[i - 1];
          if (diff > 0) {
            expect(diff).toBeLessThanOrEqual(1);
          }
        }
      });

      it("13. dist/sitemap-index.xml exists after build", () => {
        expect(existsSync(join(DIST_DIR, "sitemap-index.xml"))).toBe(true);
      });

    Step 3 — Confirm RED state.
    Run: `npx vitest run tests/seo`

    Observe which tests pass and which fail.
    - Tests 1–10: MUST all pass (no change to existing assertions)
    - Test 11: Expected GREEN (JSON-LD already in Layout.astro)
    - Test 12: Expected GREEN (heading structure already correct)
    - Test 13: Expected GREEN if Wave 1 ran `npm run build` with sitemap integration; expected RED if dist/ is from a pre-Wave-1 build

    Record the actual pass/fail state of tests 11, 12, 13 in the SUMMARY.md.
    If ALL 13 tests pass at this point (because Wave 1 already built with sitemap), that is the correct outcome — the tests serve as regression guards.
    If Test 13 is RED, that is also correct — Wave 3 will make it GREEN.

    Step 4 — Commit (RED phase):
    `git add tests/seo/seo-meta.test.ts`
    `git commit -m "test(03-02): add assertions 11 JSON-LD, 12 heading hierarchy, 13 sitemap (D-06)"`
  </action>
  <verify>
    <automated>npx vitest run tests/seo</automated>
  </verify>
  <acceptance_criteria>
    - `tests/seo/seo-meta.test.ts` has exactly 13 `it()` blocks inside the describe block
    - `const DIST_DIR` is present as a top-level constant in the file, defined as `join(__dirname, "../../dist")`
    - Tests 1–10 all PASS (no regression)
    - Test 11 references `application\/ld\+json` regex and calls `JSON.parse(match![1])`
    - Test 12 uses `matchAll(/<(h[1-6])[\s>]/gi)` and checks `diff <= 1` for downward moves
    - Test 13 uses `existsSync(join(DIST_DIR, "sitemap-index.xml"))`
    - `npx vitest run tests/seo` exits 0 (all 13 pass) OR exits non-zero only on test 13 (acceptable RED state if sitemap not yet in dist/)
    - Git commit exists with message matching `test(03-02): add assertions 11 JSON-LD, 12 heading hierarchy, 13 sitemap`
  </acceptance_criteria>
  <done>Three new assertions are written and committed. Tests 1–12 pass. Test 13 is either GREEN (Wave 1 already built sitemap) or RED (will be fixed in Wave 3). Either state is acceptable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/index.html → test regex | Static HTML read from filesystem; content is build output from our own code, not external input |
| dist/sitemap-index.xml → existsSync | File existence check; no content parsing, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | JSON.parse in test 11 | accept | The HTML is our own build output (not external input); JSON.parse throwing is caught as a test failure, which is the desired behavior |
| T-03-02-02 | Tampering | Regex in test 12 | accept | Regex operates on our own build output; catastrophic backtracking is not possible on `/<(h[1-6])[\s>]/gi` |
| T-03-02-03 | Information Disclosure | `DIST_DIR` path in test | accept | Test file is not shipped to production; path is a local filesystem reference |
</threat_model>

<verification>
- `npx vitest run tests/seo` runs and reports exactly 13 tests
- Tests 1–10 all pass (no regressions)
- The output shows test names matching "11.", "12.", "13."
- `git log --oneline -1` shows the TDD RED commit
</verification>

<success_criteria>
- 13 total it() blocks in seo-meta.test.ts describe block
- DIST_DIR constant present alongside DIST_INDEX
- Tests 1–12 pass; test 13 pass or fail depending on Wave 1 build state (both acceptable)
- TDD RED commit exists in git history
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-optimization-week-4-5/03-02-SUMMARY.md`
</output>
