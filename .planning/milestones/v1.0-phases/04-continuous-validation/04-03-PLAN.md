---
phase: 04-continuous-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/lighthouse-weekly.yml
autonomous: true
requirements: [D-04, D-05, D-06]
tags: [lighthouse, ci, cron, weekly]

must_haves:
  truths:
    - "Weekly workflow file exists at .github/workflows/lighthouse-weekly.yml"
    - "Workflow triggers on schedule cron '0 0 * * 0' (Sunday midnight UTC) and supports workflow_dispatch"
    - "Workflow runs npm ci, npm run build, then npx lhci autorun"
    - "Workflow declares contents: write permission at workflow level"
    - "Workflow does NOT trigger on push or pull_request — separate from test.yml gate"
  artifacts:
    - path: ".github/workflows/lighthouse-weekly.yml"
      provides: "Sunday weekly Lighthouse audit job"
      contains: "cron: '0 0 * * 0'"
  key_links:
    - from: ".github/workflows/lighthouse-weekly.yml schedule trigger"
      to: "GitHub Actions cron scheduler"
      via: "Sunday midnight UTC cron expression"
      pattern: "cron:\\s*['\"]0 0 \\* \\* 0['\"]"
---

<objective>
Create a new GitHub Actions workflow `.github/workflows/lighthouse-weekly.yml` per D-04/D-05/D-06: triggers on `schedule: cron: '0 0 * * 0'` (Sunday midnight UTC) and `workflow_dispatch`, builds fresh from source, runs `npx lhci autorun`, and is independent of `test.yml` (does not gate pushes). Workflow-level `permissions: contents: write` is set in anticipation of Plan 04-04 wiring up the LHCI filesystem commit step.

Purpose: Continuous SEO/perf health monitoring without coupling to the push-gating CI. Plan 04-04 will extend this workflow to commit `.lighthouseci/*.json` to main after each run; this plan delivers the scaffold.

Output: New workflow file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-continuous-validation/04-CONTEXT.md
@.planning/phases/04-continuous-validation/04-PATTERNS.md
@.github/workflows/test.yml

<interfaces>
<!-- Analog: test.yml `lighthouse` job (lines 76–97 per 04-PATTERNS.md) — same checkout/setup-node/npm ci/build/autorun pattern -->
<!-- Trigger differs: schedule cron '0 0 * * 0' + workflow_dispatch (no push/pull_request) -->
<!-- Permissions: contents: write at workflow level — needed by 04-04 to commit .lighthouseci/ JSON -->
<!-- Node version: 22, cache: npm — matches all existing workflows -->
<!-- Artifact upload of .lighthouseci/ with retention-days: 30 — matches test.yml lighthouse job convention -->
<!-- This plan does NOT add the commit step or change .lighthouserc.json — that is Plan 04-04 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/lighthouse-weekly.yml</name>
  <files>.github/workflows/lighthouse-weekly.yml</files>
  <read_first>
    - .github/workflows/test.yml (the `lighthouse` job — copy structure for checkout/setup-node/npm ci/build/autorun/artifact)
    - .planning/phases/04-continuous-validation/04-PATTERNS.md (lighthouse-weekly.yml section — full target file content; note this plan emits a SCAFFOLD without the commit step, which Plan 04-04 adds)
    - .planning/phases/04-continuous-validation/04-CONTEXT.md (D-04 cron, D-05 build+autorun, D-06 separation from test.yml)
  </read_first>
  <action>
    Create the new file `.github/workflows/lighthouse-weekly.yml` with this exact structure:

    - `name: Weekly Lighthouse Audit`
    - `on:` block containing:
      - `schedule:` with one entry `- cron: '0 0 * * 0'` (Sunday midnight UTC — D-04)
      - `workflow_dispatch:` (manual trigger for testing)
      - Do NOT include `push:` or `pull_request:` (D-06 — weekly is independent)
    - Workflow-level `permissions:` with `contents: write` (needed by Plan 04-04 to push `.lighthouseci/` JSON)
    - One job named `lighthouse-weekly` with:
      - `runs-on: ubuntu-latest`
      - Steps in this order:
        1. `actions/checkout@v4` with `token: ${{ secrets.GITHUB_TOKEN }}` and `fetch-depth: 0` (needed for git push in 04-04)
        2. `actions/setup-node@v4` with `node-version: "22"` and `cache: "npm"`
        3. `- run: npm ci`
        4. `- run: npm run build` (D-05 — build fresh)
        5. `- name: Lighthouse CI weekly audit` running `npx lhci autorun` (D-05)
        6. `actions/upload-artifact@v4` with `if: always()`, `name: lighthouse-weekly-report`, `path: .lighthouseci/`, `retention-days: 30`

    Do NOT include a `Commit LHCI filesystem results` step in this plan — Plan 04-04 owns that step and the `.lighthouserc.json` upload.target change. Do NOT add a `needs:` dependency. The job is standalone.
  </action>
  <verify>
    <automated>node -e "const y=require('js-yaml');const f=require('fs');const d=y.load(f.readFileSync('.github/workflows/lighthouse-weekly.yml','utf8'));if(d.name!=='Weekly Lighthouse Audit')throw 1;if(!d.on.schedule||d.on.schedule[0].cron!=='0 0 * * 0')throw 2;if(!('workflow_dispatch' in d.on))throw 3;if(d.on.push||d.on.pull_request)throw 4;if(d.permissions.contents!=='write')throw 5;const j=d.jobs['lighthouse-weekly'];if(!j)throw 6;if(j['runs-on']!=='ubuntu-latest')throw 7;const names=j.steps.map(s=>s.name||(s.uses||s.run||'').slice(0,40));if(!j.steps.find(s=>s.run==='npm ci'))throw 8;if(!j.steps.find(s=>s.run==='npm run build'))throw 9;if(!j.steps.find(s=>s.name==='Lighthouse CI weekly audit'&&/lhci autorun/.test(s.run)))throw 10;console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/lighthouse-weekly.yml` exists
    - YAML parses cleanly
    - `name == 'Weekly Lighthouse Audit'`
    - `on.schedule[0].cron == '0 0 * * 0'`
    - `on.workflow_dispatch` key exists
    - `on.push` and `on.pull_request` are absent
    - Workflow-level `permissions.contents == 'write'`
    - Job `lighthouse-weekly` exists with `runs-on: ubuntu-latest`
    - Steps include (in order): `actions/checkout@v4`, `actions/setup-node@v4` (node 22 + npm cache), `npm ci`, `npm run build`, `npx lhci autorun`, `actions/upload-artifact@v4`
    - The checkout step sets `fetch-depth: 0`
    - The artifact upload step uses `if: always()`, `path: .lighthouseci/`, `retention-days: 30`
    - No `Commit LHCI filesystem results` step is present (Plan 04-04 adds that)
    - `npm run test:unit` still exits 0 (config-only change)
  </acceptance_criteria>
  <done>Workflow file created and structurally validated; weekly cron + manual dispatch wired; ready for Plan 04-04 to add the filesystem commit step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub cron scheduler → workflow runner | Trigger source is GitHub-internal; no external input |
| CI runner → external (Astro preview server, LHCI scripts) | Build artifacts are repo-local |
| Workflow → GITHUB_TOKEN scope | `contents: write` granted at workflow level — used by 04-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | contents:write granted workflow-wide | accept | Only one job in this workflow; broader scope harmless; alternative job-level grant deferred for simplicity |
| T-04-03-02 | Denial of Service | Cron storm if cron expression malformed | mitigate | Acceptance check validates exact cron `'0 0 * * 0'` |
| T-04-03-03 | Tampering | Workflow file modified to inject malicious steps | accept | File in version control; branch protection on main applies |
| T-04-03-04 | Information Disclosure | LHCI report contains URLs/scores only | accept | No PII; reports are repo-local |
</threat_model>

<verification>
- File exists
- YAML parse and field assertions per Task 1 automated verify
- `npm run test:unit` exits 0
- Manual: trigger `workflow_dispatch` from Actions UI to confirm end-to-end run before relying on cron
</verification>

<success_criteria>
- D-04: weekly cron at Sunday midnight UTC declared
- D-05: build + lhci autorun present
- D-06: workflow has no push/PR triggers — fully separate from test.yml
- Workflow ready for Plan 04-04 to layer in the filesystem commit step
</success_criteria>

<output>
After completion, create `.planning/phases/04-continuous-validation/04-03-SUMMARY.md`
</output>
