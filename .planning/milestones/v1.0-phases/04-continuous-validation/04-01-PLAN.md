---
phase: 04-continuous-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - .github/workflows/test.yml
autonomous: true
requirements: [D-01, D-02, D-03]
tags: [vitest, coverage, ci, thresholds]

must_haves:
  truths:
    - "vitest exits non-zero when any of statements/branches/functions/lines falls below 95%"
    - "vitest.config.ts thresholds block enforces 95% on all four metrics"
    - "test.yml coverage gate step name reflects the new 95% threshold"
  artifacts:
    - path: "vitest.config.ts"
      provides: "95% coverage thresholds on all four metrics"
      contains: "thresholds:"
    - path: ".github/workflows/test.yml"
      provides: "Coverage gate step labelled for 95%"
      contains: "coverage gate >= 95%"
  key_links:
    - from: "vitest.config.ts coverage.thresholds"
      to: "npm run test:unit exit code"
      via: "vitest --coverage gate enforcement"
      pattern: "thresholds:\\s*\\{"
---

<objective>
Raise the vitest coverage gate from absent (comment "thresholds removed as they are not needed") to a hard 95% gate on all four metrics (statements, branches, functions, lines) per D-01/D-02/D-03. Update the CI step name in test.yml to reflect the new threshold.

Purpose: Enforce minimum coverage automatically — vitest exits non-zero when any metric falls below 95%, blocking merge to main. As the coverable surface grows (currently 0/0 due to .astro exclusion), the gate enforces quality from day one.

Output: Modified vitest.config.ts with thresholds block, modified test.yml step name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-continuous-validation/04-CONTEXT.md
@.planning/phases/04-continuous-validation/04-PATTERNS.md
@vitest.config.ts
@.github/workflows/test.yml

<interfaces>
<!-- Current state of vitest.config.ts coverage block (lines 30–41 per 04-PATTERNS.md): -->
<!--
    coverage: {
      provider: "v8",
      include: ["src/**"],
      exclude: ["src/assets/**", "src/pages/**", "src/**/*.astro"],
      reporter: ["text", "json", "html"],
      // thresholds removed as they are not needed
    },
-->
<!-- Target replaces the comment with thresholds: { statements: 95, branches: 95, functions: 95, lines: 95 } -->
<!-- test.yml currently has a step named "coverage gate >= 80%" — rename to "coverage gate >= 95%" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 95% thresholds block to vitest.config.ts</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (current state — confirm the comment "// thresholds removed as they are not needed" still sits inside the coverage block)
    - .planning/phases/04-continuous-validation/04-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/04-continuous-validation/04-PATTERNS.md (vitest.config.ts pattern assignment for exact current/target text)
  </read_first>
  <action>
    Inside the `coverage` config object in `vitest.config.ts`, replace the line `// thresholds removed as they are not needed` with a `thresholds` object containing exactly four keys: `statements: 95`, `branches: 95`, `functions: 95`, `lines: 95`. Do not modify `provider`, `include`, `exclude`, or `reporter` keys. Preserve indentation (2-space). Per D-03, do NOT add utility modules first — the current `.astro`-only coverable surface is acceptable.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "statements:\s*95" vitest.config.ts` matches exactly one line
    - `grep -E "branches:\s*95" vitest.config.ts` matches exactly one line
    - `grep -E "functions:\s*95" vitest.config.ts` matches exactly one line
    - `grep -E "lines:\s*95" vitest.config.ts` matches exactly one line
    - `grep -c "thresholds removed as they are not needed" vitest.config.ts` returns 0
    - `npm run test:unit` exits 0 (with empty coverable surface, vitest reports "Unknown%" but does not fail thresholds)
  </acceptance_criteria>
  <done>thresholds block present with all four metrics at 95; old comment removed; npm run test:unit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Update test.yml coverage gate step name to 95%</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (locate the step currently named "coverage gate >= 80%")
    - .planning/phases/04-continuous-validation/04-CONTEXT.md (D-02 — hard gate at 95%)
  </read_first>
  <action>
    In `.github/workflows/test.yml`, find the step whose `name:` is `coverage gate >= 80%` (inside the `unit-and-integration` job) and change it to `coverage gate >= 95%`. Do not modify the step's `run:` content or any surrounding steps. This is a label-only change to keep the CI log accurate; the actual gate enforcement is in vitest.config.ts from Task 1.
  </action>
  <verify>
    <automated>grep -c "coverage gate >= 95%" .github/workflows/test.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "coverage gate >= 95%" .github/workflows/test.yml` returns >= 1
    - `grep -c "coverage gate >= 80%" .github/workflows/test.yml` returns 0
    - YAML still parses: `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/test.yml','utf8'))"` exits 0 (or equivalent yq check)
  </acceptance_criteria>
  <done>Step label reads "coverage gate >= 95%"; YAML remains valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → CI | Test thresholds run inside CI sandbox; no external input crosses |
| CI → repo state | Threshold config affects build pass/fail only; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | vitest.config.ts thresholds | accept | Config in version control; PR review surfaces threshold changes |
| T-04-01-02 | Denial of Service | CI gate (false-positive failure) | mitigate | D-03 acknowledges empty coverable surface; vitest/v8 reports "Unknown%" without failing per A1 in RESEARCH.md |
| T-04-01-03 | Information Disclosure | Coverage report contents | accept | Reports are repo-local artifacts, no PII |
</threat_model>

<verification>
After both tasks:
- `npm run test:unit` exits 0
- `vitest.config.ts` contains a `thresholds` block with all four metrics at 95
- `.github/workflows/test.yml` step name reads `coverage gate >= 95%`
</verification>

<success_criteria>
- vitest thresholds enforced at 95% on statements, branches, functions, lines
- CI label matches actual threshold
- `npm run test:unit` green on current empty coverable surface
</success_criteria>

<output>
After completion, create `.planning/phases/04-continuous-validation/04-01-SUMMARY.md`
</output>
