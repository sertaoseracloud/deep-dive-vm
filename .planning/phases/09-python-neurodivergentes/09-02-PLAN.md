---
phase: 09-python-neurodivergentes
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/sections/Pricing.astro
  - src/components/sections/Hero.astro
  - src/components/sections/TrustBand.astro
  - src/components/sections/PainPoints.astro
  - src/components/sections/Method.astro
  - src/components/sections/Curriculum.astro
  - src/components/sections/Mentor.astro
  - src/components/sections/ForWho.astro
  - src/components/sections/Bonuses.astro
  - src/components/sections/Faq.astro
  - src/components/sections/FinalCTA.astro
  - src/components/ui/ModuleDetails.astro
  - src/pages/deep-dive-vm/index.astro
  - src/pages/deep-dive-python-neurodivergentes/index.astro
autonomous: true
requirements:
  - PY-01
  - PY-02
  - PY-03
  - PY-04

must_haves:
  truths:
    - "Visitando /deep-dive-python-neurodivergentes/ a página carrega com Hero, TrustBand, PainPoints, Method, Curriculum, Mentor, ForWho, Bonuses, Pricing, Faq, FinalCTA visíveis"
    - "Visitando /deep-dive-vm/ o design e conteúdo permanecem idênticos ao estado pré-refator (sem regressão)"
    - "O hub em / exibe 3 cards com Python card linkando para /deep-dive-python-neurodivergentes/"
    - "og:image na LP Python aponta para /python-neurodivergentes-og.png"
    - "npm run build sai com código 0 sem erros TypeScript"
  artifacts:
    - path: "src/components/sections/Pricing.astro"
      provides: "Pricing data-driven com PricingProps interface completa"
      contains: "interface PricingProps"
    - path: "src/components/sections/Hero.astro"
      provides: "Hero data-driven, portrait como ImageMetadata prop"
      contains: "interface HeroProps"
    - path: "src/components/sections/FinalCTA.astro"
      provides: "Nova seção FinalCTA com props e CSS spec do UI-SPEC"
    - path: "src/pages/deep-dive-python-neurodivergentes/index.astro"
      provides: "LP completa com Layout, JSON-LD Course, 11 seções compostas"
    - path: "src/pages/deep-dive-vm/index.astro"
      provides: "VM LP atualizada passando props explícitas para todos os componentes refatorados"
  key_links:
    - from: "src/pages/deep-dive-python-neurodivergentes/index.astro"
      to: "src/layouts/Layout.astro"
      via: "ogImage prop"
      pattern: "ogImage=\"/python-neurodivergentes-og.png\""
    - from: "src/pages/deep-dive-python-neurodivergentes/index.astro"
      to: "src/components/sections/Pricing.astro"
      via: "Python pricing props"
      pattern: "PYTHON_COURSE.hotmartUrl"
    - from: "src/pages/deep-dive-vm/index.astro"
      to: "src/components/sections/Pricing.astro"
      via: "VM pricing props (backward-compat)"
      pattern: "eyebrow.*INVESTIMENTO"
---

<objective>
Refatorar os 11 componentes de seção existentes para data-driven (props-based), criar FinalCTA.astro (novo), criar a página index.astro da LP Python, e atualizar a VM LP para passar props explícitas — mantendo zero regressão.

Purpose: Pricing.astro é refatorado PRIMEIRO como modelo. Os demais componentes seguem o mesmo padrão. A VM LP continua funcionando com os mesmos dados via props. A LP Python fica disponível em /deep-dive-python-neurodivergentes/ com todo o conteúdo do wireframe.
Output: 11 componentes data-driven, FinalCTA.astro novo, index.astro da LP Python, VM LP atualizada.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-python-neurodivergentes/09-CONTEXT.md
@.planning/phases/09-python-neurodivergentes/09-RESEARCH.md
@.planning/phases/09-python-neurodivergentes/09-PATTERNS.md
@.planning/phases/09-python-neurodivergentes/09-UI-SPEC.md
@.planning/phases/09-python-neurodivergentes/09-01-SUMMARY.md

<interfaces>
<!-- Contratos críticos extraídos das fontes de verdade. O executor NÃO precisa explorar o codebase. -->

Interface PricingProps (D-03, UI-SPEC §9 — USAR EXATAMENTE):
```typescript
interface PricingProps {
  eyebrow: string;
  titleHtml: string;
  lede: string;
  ribbon: string;
  tierLabel: string;
  courseName: string;
  originalPrice: string;
  installments: { count: number; value: string; cents: string };
  pixPrice: string;
  pixNote?: string;
  includes: Array<{ html: string; delay?: number }>;
  ctaHref: string;
  ctaText: string;
  guarantee: { days: number; heading: string; body: string };
}
```

Python Pricing values (UI-SPEC §9):
- eyebrow: "INVESTIMENTO"
- titleHtml: 'A diferença entre <span class="flame">começar</span> e terminar está aqui.'
- lede: "Acesso por 12 meses, todos os bônus liberados, certificado e Body Doubling semanal. Pagamento via Hotmart · cartão, PIX ou boleto."
- ribbon: "⟡ ACESSO COMPLETO · ECONOMIA DE R$ 1.050"
- tierLabel: "O Sertao será Cloud"
- courseName: "DEEP DIVE · PYTHON"
- originalPrice: "R$ 1.997"
- installments: { count: 12, value: "78", cents: ",92" }
- pixPrice: "R$ 947"
- ctaText: "Quero começar agora"
- ctaHref: PYTHON_COURSE.hotmartUrl (importado de src/data/python-course.ts)
- 7 includes: ver PATTERNS.md §Pricing para os 7 itens com HTML bold
- guarantee: { days: 7, heading: "Garantia incondicional de 7 dias.", body: "Acesse o curso, comece a trilha, faça os projetos. Se em 7 dias você sentir que não é para você · qualquer motivo · devolvemos 100% do investimento. O risco é todo nosso." }

VM Pricing backward-compat values (extrair do Pricing.astro atual antes de refatorar):
- eyebrow: "INVESTIMENTO · TURMA CHAMA AZUL 01"
- titleHtml: 'A diferença entre <span class="flame">executor e arquiteto</span> está aqui.'
- ribbon: "⟡ ACESSO COMPLETO · ECONOMIA DE R$ 1.050"
- tierLabel: "O Sertao será Cloud"
- courseName: "DEEP DIVE · AZURE VM"
- ctaHref: "https://www.hotmart.com"
- ctaText: "Quero garantir minha vaga agora"
- guarantee.days: 7
- 8 includes: extrair os 8 itens hardcoded antes de substituir

Interface HeroProps (UI-SPEC §1, PATTERNS.md §Hero):
```typescript
interface HeroProps {
  eyebrowTag: string;
  eyebrowDesc: string;
  titleHtml: string;
  sub: string;
  points: Array<{ html: string }>;
  ctaPrimary: { href: string; label: string };
  ctaGhost: { href: string; label: string };
  metaBadges: Array<{ icon: string; label: string }>;
  portrait: { src: ImageMetadata; alt: string };
  portraitStats: Array<{ label: string }>;
  portraitTag: { name: string; badge: string };
}
```

Python Hero values (UI-SPEC §1):
- eyebrowTag: "DEEP DIVE"
- eyebrowDesc: "Curso 60h · Python · 5 Frentes · Body Doubling"
- titleHtml: "Pare de começar 10 cursos de Python e abandonar todos. Aprenda do jeito que seu <span class=\"flame\">cérebro pede</span>."
- sub: "Um Deep Dive em Python desenhado para devs neurodivergentes (TDAH, autismo, dislexia, ansiedade) que já tentaram aprender e não conseguiram terminar."
- points[0]: "<strong>60h em micro-aulas</strong> curtas · cadência respeitosa ao hiperfoco e ao cansaço."
- points[1]: "<strong>5 frentes por módulo</strong>: Visual, Hands-on, Repetição, Projeto e Body Doubling."
- points[2]: "Professor <strong>ND-friendly</strong> · neurodivergente por dentro, não só por empatia."
- points[3]: "<strong>30 projetos reais</strong> · nada de exercícios artificiais desconectados do mundo."
- ctaPrimary: { href: "#investimento", label: "Quero aprender Python do meu jeito" }
- ctaGhost: { href: "#ementa", label: "Ver Ementa Completa" }
- metaBadges: 4 items com ícones inline SVG e labels "60H · MICRO-AULAS", "6 MÓDULOS · 30 PROJETOS", "BODY DOUBLING SEMANAL", "ACESSO 12 MESES"
- portrait: { src: claudio2 (ImageMetadata importado), alt: "Cláudio Filipe Lima Raposo, professor do curso Python para Neurodivergentes" }
- portraitStats: [{ label: "⟡ 10× MVP · MICROSOFT" }, { label: "⟡ 7× CERTIFIED · AWS" }]
- portraitTag: { name: "Cláudio F. L. Raposo", badge: "PROFESSOR PRINCIPAL" }

Interface FinalCtaProps (UI-SPEC §11, RESEARCH.md §FinalCTA):
```typescript
interface FinalCtaProps {
  eyebrow: string;
  headlineHtml: string;
  ctaHref: string;
  ctaText: string;
}
```
Python FinalCTA values:
- eyebrow: "PYTHON PARA NEURODIVERGENTES"
- headlineHtml: 'Python do jeito que seu <span class="flame">cérebro pede</span>.'
- ctaHref: "#investimento"
- ctaText: "Quero começar agora"

CSS da FinalCTA (novo — especificado no UI-SPEC §11 / RESEARCH.md §FinalCTA):
- section.final-cta: background radial-gradient(ellipse 60% 40% at 50% 50%, rgba(0,255,255,0.06), transparent 70%), border-top: 1px solid var(--hairline-strong), data-reveal
- .final-cta-inner: max-width: 640px; margin: 0 auto; text-align: center
- .final-cta-eyebrow: font-family JetBrains Mono, font-size 11px, letter-spacing 0.28em, color var(--nucleo-eletrico), margin-bottom 16px
- h2.final-cta-headline: font Chakra Petch, font-size clamp(28px,3vw,44px), weight 600, text-wrap balance, margin-bottom 32px
- :global(.flame) dentro de FinalCTA: color var(--nucleo-eletrico), font-style italic, text-shadow 0 0 32px rgba(0,255,255,0.6)
- .final-cta-actions: display flex, justify-content center
- Mobile (≤720px): .final-cta-actions .btn { width: 100% }

Layout props da LP Python:
- title: "Python para Neurodivergentes · O Sertao será Cloud"
- description: "Deep Dive em Python para devs com TDAH, autismo, dislexia ou ansiedade. 60h em micro-aulas, 5 frentes, Body Doubling semanal. Com Cláudio Raposo, Microsoft MVP."
- url: "https://mentoria.sertaoseracloud.com/deep-dive-python-neurodivergentes/"
- ogImage: "/python-neurodivergentes-og.png"
- jsonLd: Course schema com courseCode "PY-ND-DEEP-DIVE", price "947.00", duration "PT60H"

JSON-LD completo (RESEARCH.md / PATTERNS.md):
```json
{
  "@context": "https://schema.org",
  "@type": "Course",
  "name": "Python para Neurodivergentes · O Sertao será Cloud",
  "description": "Deep Dive em Python para devs com TDAH, autismo, dislexia ou ansiedade.",
  "courseCode": "PY-ND-DEEP-DIVE",
  "educationalLevel": "Beginner",
  "provider": { "@type": "Person", "name": "Cláudio Filipe Lima Raposo", "sameAs": ["https://www.linkedin.com/in/cfraposo/", "https://github.com/sertaoseracloud"] },
  "offers": { "@type": "Offer", "price": "947.00", "priceCurrency": "BRL", "availability": "https://schema.org/InStock", "url": "https://mentoria.sertaoseracloud.com/deep-dive-python-neurodivergentes/#investimento" },
  "hasCourseInstance": { "@type": "CourseInstance", "courseMode": "Online", "duration": "PT60H", "instructor": { "@type": "Person", "name": "Cláudio Filipe Lima Raposo", "jobTitle": "Microsoft MVP" } }
}
```

Regra crítica de imagem (Pitfall 6 do RESEARCH.md):
- Hero e Mentor precisam receber portrait como ImageMetadata
- No index.astro da LP Python: `import claudio2 from "../../assets/claudio2.png";`
- Passar como: `portrait={{ src: claudio2, alt: "..." }}`
- Os componentes Hero e Mentor removem seus próprios imports hardcoded e aceitam via prop

Regra crítica de backward-compat VM (Pitfall 1 do RESEARCH.md):
- Ao refatorar Pricing.astro: PRIMEIRO extrair todos os valores hardcoded atuais (8 includes, todos os textos)
- DEPOIS substituir por bindings de prop
- DEPOIS atualizar deep-dive-vm/index.astro com os valores extraídos
- As 3 etapas devem ser atômicas — nunca commitar Pricing.astro sem atualizar VM index.astro

Módulos Python para Curriculum (UI-SPEC §5):
- M.01 (08H, isOpen:true): Fundamentos sem Tédio — tracks: VISUAL, HANDS-ON, REPETIÇÃO, PROJETO, BODY DOUBLING
- M.02 (10H): Estruturas de Dados na Prática
- M.03 (10H): Orientação a Objetos sem Fórmula
- M.04 (10H): Automação do Mundo Real
- M.05 (12H): Web e APIs com FastAPI
- M.06 (10H): Dados, Análise e Pequena IA
Todos os módulos usam as mesmas 5 track heads: VISUAL · HANDS-ON · REPETIÇÃO · PROJETO · BODY DOUBLING

Ordem das seções em index.astro (per D-02 / UI-SPEC §0):
skip-link → UrgencyBar → NavBar → main#main
  Hero → TrustBand → PainPoints → Method → Curriculum → Mentor → ForWho → Bonuses → Pricing → Faq → FinalCTA → Footer
StickyCta → LegalModals
(SEM Testimonials — deferred per CONTEXT.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Refatorar Pricing.astro (modelo) + componentes de seção restantes + ModuleDetails</name>
  <files>
    src/components/sections/Pricing.astro
    src/components/sections/Hero.astro
    src/components/sections/TrustBand.astro
    src/components/sections/PainPoints.astro
    src/components/sections/Method.astro
    src/components/sections/Curriculum.astro
    src/components/sections/Mentor.astro
    src/components/sections/ForWho.astro
    src/components/sections/Bonuses.astro
    src/components/sections/Faq.astro
    src/components/ui/ModuleDetails.astro
  </files>
  <read_first>
    - src/components/sections/Pricing.astro — ler COMPLETO antes de refatorar: extrair todos os 8 includes hardcoded, textos do ribbon/tier/courseName/guarantee, href do button, CSS block inteiro
    - src/components/sections/Hero.astro — ler COMPLETO: linha do import claudio1.png, estrutura do eyebrow (.bar/.idx), portrait wrap com brackets, CSS block
    - src/components/sections/TrustBand.astro — ler COMPLETO: estrutura div.trust (NÃO section), grid label + 4 items, CSS
    - src/components/sections/Curriculum.astro — ler COMPLETO: como ModuleDetails é chamado atualmente via slot, modules-meta bar
    - src/components/ui/ModuleDetails.astro — ler COMPLETO: interface Props atual, como slot é usado para tracks, CSS do track grid
    - src/components/sections/Mentor.astro — ler COMPLETO: imports claudio2.png e marcelo.png, guest mentor section, brackets pattern
    - src/components/sections/PainPoints.astro — ler para extrair estrutura do pain-card (num, titleHtml, body)
    - src/components/sections/Method.astro — ler para extrair estrutura das method-cells (svgHtml, label, title, bodyHtml)
    - src/components/sections/ForWho.astro — ler para extrair estrutura is-for/is-not com ícones SVG específicos
    - src/components/sections/Bonuses.astro — ler para extrair estrutura bonus-card (num, featured, title, bodyHtml)
    - src/components/sections/Faq.astro — ler para extrair estrutura details/summary
  </read_first>
  <action>
    ORDEM DE EXECUÇÃO (seguir esta sequência — Pricing primeiro como modelo):

    1. Pricing.astro — PRIMEIRO (modelo para todos):
    Antes de qualquer mudança, extrair e registrar TODOS os valores hardcoded atuais da VM (ribbon, tierLabel, courseName, originalPrice, installments, pixPrice, 8 includes com HTML completo, ctaHref, ctaText, guarantee). Adicionar `interface PricingProps` com os campos exatos da seção <interfaces> acima. Adicionar `const { ... } = Astro.props;`. Substituir cada hardcode por binding de prop. CSS block: copiar EXATAMENTE sem nenhuma mudança. Para os includes: usar `{includes.map((item, i) => (<li data-stagger style={`animation-delay: ${item.delay ?? i * 80}ms`}>...svg...<span set:html={item.html} /></li>))}`.

    2. ModuleDetails.astro — adicionar prop tracks:
    Adicionar `tracks: Array<{ head: string; body: string }>` à interface Props existente. No template, substituir `<slot />` por `{tracks.map(track => (<div class="track"><div class="head">{track.head}</div><p set:html={track.body} /></div>))}`. CSS: INALTERADO.

    3. Hero.astro — refatorar:
    Remover `import claudio1 from "../../assets/claudio1.png"` da linha hardcoded. Adicionar `import { Image } from "astro:assets"; import type { ImageMetadata } from "astro";`. Adicionar `interface HeroProps` com os campos da seção <interfaces>. No template: substituir cada hardcode por binding. Para portrait: usar `<Image src={portrait.src} alt={portrait.alt} loading="eager" fetchpriority="high" />`. CSS: INALTERADO.

    4. TrustBand.astro — refatorar:
    Adicionar `interface TrustBandProps { label: string; items: Array<{ svgHtml: string; label: string }> }`. No template: substituir label hardcoded por `<div class="trust-label" set:html={label.replace('\n', '<br />')} />`. Iterar items com `Fragment set:html={item.svgHtml}`. IMPORTANTE: manter `<div class="trust">` — NÃO converter para `<section>`. CSS: INALTERADO.

    5. PainPoints.astro — refatorar:
    Adicionar `interface PainPointsProps { eyebrow, titleHtml, lede?, cards: Array<{num, titleHtml, body}> }`. Passar SectionHead com props. Iterar cards no pain-grid. CSS: INALTERADO.

    6. Method.astro — refatorar:
    Adicionar `interface MethodProps { eyebrow, titleHtml, lede?, steps: Array<{svgHtml, label, title, bodyHtml}> }`. Iterar steps no method-grid com `Fragment set:html={step.svgHtml}`. CSS: INALTERADO.

    7. Curriculum.astro — refatorar:
    Adicionar `interface CurriculumProps { eyebrow, titleHtml, lede, meta: Array<{value, label}>, modules: CurriculumModule[] }` onde `CurriculumModule { num, title, subtitle, hours, isOpen?, tracks }`. No template: iterar modules renderizando `<ModuleDetails ... tracks={m.tracks} />` (sem slot). CSS: INALTERADO.

    8. Mentor.astro — refatorar:
    Remover imports hardcoded de claudio2.png e marcelo.png. Adicionar `import { Image } from "astro:assets"; import type { ImageMetadata } from "astro";`. Adicionar `interface MentorProps { primary: MentorInfo; guest?: MentorInfo }`. Guest mentor renderizado apenas quando `guest` prop estiver definido (`{guest && ...}`). Brackets pattern: 4 spans dentro de div.brackets — preservar. CSS: INALTERADO.

    9. ForWho.astro — refatorar:
    Adicionar `interface ForWhoProps { eyebrow, titleHtml, isFor: {heading, items}, isNot: {heading, items} }`. Iterar items nas duas cards. Ícones SVG (checkmark para isFor, X para isNot) permanecem como HTML fixo no template. CSS: INALTERADO.

    10. Bonuses.astro — refatorar:
    Adicionar `interface BonusesProps { eyebrow, titleHtml, lede?, bonuses: Bonus[] }`. Iterar bonuses com `class={bonus.featured ? 'bonus-card featured' : 'bonus-card'}`. CSS: INALTERADO.

    11. Faq.astro — refatorar:
    Adicionar `interface FaqProps { eyebrow, titleHtml, items: FaqItem[] }`. Iterar items com details/summary pattern. CSS: INALTERADO.

    REGRA GERAL para todos os refatores: apenas frontmatter (+ interface + destructuring) e template (hardcode → prop binding) mudam. O bloco `<style>` de CADA componente é copiado/preservado byte a byte.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['Pricing','Hero','TrustBand','PainPoints','Method','Curriculum','Mentor','ForWho','Bonuses','Faq'].forEach(c => { const t=fs.readFileSync('src/components/sections/'+c+'.astro','utf-8'); if(!t.includes('interface Props')&&!t.includes('interface '+c+'Props')) throw new Error(c+': interface Props missing'); console.log(c+': OK'); })"</automated>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('src/components/sections/Pricing.astro','utf-8'); if(!p.includes('PricingProps')) throw new Error('PricingProps missing'); if(p.includes('TURMA CHAMA AZUL')) throw new Error('VM hardcode still present in Pricing.astro'); console.log('Pricing refactor OK')"</automated>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('src/components/sections/Hero.astro','utf-8'); if(h.includes(\"import claudio1\")) throw new Error('Hero: claudio1 hardcoded import still present'); if(!h.includes('ImageMetadata')) throw new Error('Hero: ImageMetadata import missing'); console.log('Hero refactor OK')"</automated>
    <automated>node -e "const fs=require('fs'); const m=fs.readFileSync('src/components/ui/ModuleDetails.astro','utf-8'); if(!m.includes('tracks')) throw new Error('ModuleDetails: tracks prop missing'); console.log('ModuleDetails OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 10 componentes de seção refatorados contêm `interface Props` (ou `interface {Component}Props`) no frontmatter
    - `Pricing.astro` NÃO contém strings hardcoded "TURMA CHAMA AZUL" ou "DEEP DIVE · AZURE VM" no template
    - `Hero.astro` NÃO contém `import claudio1` — aceita `portrait: { src: ImageMetadata; alt: string }` via prop
    - `TrustBand.astro` mantém `<div class="trust">` (NÃO `<section>`) — sem data-reveal
    - `ModuleDetails.astro` aceita `tracks: Array<{head: string; body: string}>` e renderiza sem `<slot />`
    - `Mentor.astro` renderiza seção de guest mentor apenas quando prop `guest` estiver definida
    - CSS block de CADA componente permanece inalterado (nenhuma linha de CSS foi adicionada ou removida)
  </acceptance_criteria>
  <done>11 componentes data-driven (10 refatorados + ModuleDetails estendido). Nenhum CSS alterado.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar FinalCTA.astro + LP Python index.astro + atualizar VM LP</name>
  <files>
    src/components/sections/FinalCTA.astro
    src/pages/deep-dive-python-neurodivergentes/index.astro
    src/pages/deep-dive-vm/index.astro
  </files>
  <read_first>
    - src/pages/deep-dive-vm/index.astro — ler COMPLETO: padrão de imports, Layout props (jsonLd shape, ogImage), composição de seções, como Testimonials é importado (NÃO incluir na LP Python), invocações atuais de cada componente refatorado (para extrair os valores VM e passar como props)
    - src/components/ui/Button.astro — ler para confirmar API de props: variant="primary", size="massive", href
    - src/data/python-course.ts — confirmar que foi criado no plano 09-01 e verificar os campos (hotmartUrl, route, ogImage)
    - src/components/sections/FinalCTA.astro — NÃO existe ainda (novo arquivo a criar)
  </read_first>
  <action>
    Passo 1 — Criar src/components/sections/FinalCTA.astro (componente novo):
    Criar o arquivo com `interface FinalCtaProps { eyebrow: string; headlineHtml: string; ctaHref: string; ctaText: string; }` e `const { eyebrow, headlineHtml, ctaHref, ctaText } = Astro.props;`. Template: `<section class="final-cta" data-reveal>` com `.container > .final-cta-inner > .final-cta-eyebrow + h2.final-cta-headline + .final-cta-actions`. O h2 usa `set:html={headlineHtml}`. O CTA usa `<Button href={ctaHref} variant="primary" size="massive">`. Importar Button de "../ui/Button.astro". CSS (novo — per UI-SPEC §11): section.final-cta com radial-gradient(ellipse 60% 40% at 50% 50%, rgba(0,255,255,0.06), transparent 70%), border-top: 1px solid var(--hairline-strong). .final-cta-inner: max-width 640px, margin 0 auto, text-align center. .final-cta-eyebrow: font JetBrains Mono 11px, letter-spacing 0.28em, color var(--nucleo-eletrico), margin-bottom 16px, text-transform uppercase. h2.final-cta-headline: font Chakra Petch, font-size clamp(28px,3vw,44px), font-weight 600, text-wrap balance, margin-bottom 32px, color var(--texto-principal). :global(.flame) em FinalCTA: color var(--nucleo-eletrico), font-style italic, text-shadow 0 0 32px rgba(0,255,255,0.6). .final-cta-actions: display flex, justify-content center. @media (max-width: 720px) { .final-cta-actions :global(.btn) { width: 100% } }.

    Passo 2 — Criar src/pages/deep-dive-python-neurodivergentes/index.astro (LP Python):
    Criar o diretório se não existir. Seguir exatamente o padrão de deep-dive-vm/index.astro para imports e estrutura. Imports necessários: Layout, UrgencyBar, NavBar, Footer, StickyCta, Hero, TrustBand, PainPoints, Method, Curriculum, Mentor, ForWho, Bonuses, Pricing, Faq, FinalCTA, LegalModals, claudio2 (ImageMetadata), PYTHON_COURSE de src/data/python-course.ts. Layout props: title="Python para Neurodivergentes · O Sertao será Cloud", description (ver <interfaces>), url="https://mentoria.sertaoseracloud.com/deep-dive-python-neurodivergentes/", ogImage="/python-neurodivergentes-og.png", jsonLd com Course schema (ver <interfaces>). Composição do page body: skip-link `<a href="#main" class="skip-link">Pular para o conteúdo</a>`, `<UrgencyBar content="<strong>60H DE CONTEÚDO</strong> · <strong>6 SESSÕES 1:1</strong> · <strong>BODY DOUBLING SEMANAL</strong> · <strong>ACESSO 12 MESES</strong>" />`, NavBar, `<main id="main" tabindex="-1">`, depois as seções na ordem exata: Hero → TrustBand → PainPoints → Method → Curriculum → Mentor → ForWho → Bonuses → Pricing → Faq → FinalCTA → Footer, depois StickyCta e LegalModals. Passar todos os props tipados com os valores Python do wireframe (ver <interfaces> acima para Hero, Pricing, FinalCTA — para os demais componentes usar os valores do D-06 / UI-SPEC). NÃO incluir `<Testimonials />`. CTA primary em Hero usa `data-testid="hero-cta-primary"`, ghost usa `data-testid="hero-cta-ghost"`. StickyCta recebe: `priceLabel="DESDE 12× R$ 78,92"`, `ctaHref="#investimento"`, `ctaText="Quero começar →"`.

    Passo 3 — Atualizar src/pages/deep-dive-vm/index.astro com props explícitas:
    Para CADA componente refatorado, adicionar as props com os valores VM (extraídos na Tarefa 1 antes do refator). Os valores hardcoded que eram no componente agora vão para o caller. Pricing recebe os 8 includes VM extraídos, todos os textos VM. Hero recebe import claudio1 (que a VM LP usa) como portrait prop — `import claudio1 from "../../assets/claudio1.png"` no index.astro da VM, então `<Hero portrait={{ src: claudio1, alt: "..." }} .../>`. UrgencyBar: já atualizado no plano 09-01. StickyCta: já atualizado no plano 09-01. Mentor: primary com claudio2 (ou claudio1 se VM usa esse), guest com marcelo.png — verificar no arquivo atual o que estava hardcoded.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node -e "const fs=require('fs'); if(!fs.existsSync('dist/deep-dive-python-neurodivergentes/index.html')) throw new Error('LP Python not built'); const h=fs.readFileSync('dist/deep-dive-python-neurodivergentes/index.html','utf-8'); if(!h.includes('python-neurodivergentes-og.png')) throw new Error('og:image missing'); if(!h.includes('PY-ND-DEEP-DIVE')) throw new Error('JSON-LD courseCode missing'); console.log('LP Python build OK')"</automated>
    <automated>node -e "const fs=require('fs'); if(!fs.existsSync('dist/deep-dive-vm/index.html')) throw new Error('VM LP regression: not built'); const h=fs.readFileSync('dist/deep-dive-vm/index.html','utf-8'); if(!h.includes('TURMA CHAMA AZUL')) throw new Error('VM LP regression: Pricing content missing'); console.log('VM LP OK — no regression')"</automated>
    <automated>node -e "const fs=require('fs'); if(!fs.existsSync('dist/index.html')) throw new Error('Hub missing'); const h=fs.readFileSync('dist/index.html','utf-8'); if(!h.includes('deep-dive-python-neurodivergentes')) throw new Error('Hub missing Python link'); console.log('Hub OK')"</automated>
    <automated>node -e "const fs=require('fs'); const f=fs.readFileSync('src/components/sections/FinalCTA.astro','utf-8'); if(!f.includes('FinalCtaProps')&&!f.includes('interface Props')) throw new Error('FinalCTA: Props interface missing'); if(!f.includes('data-reveal')) throw new Error('FinalCTA: data-reveal missing'); if(!f.includes('final-cta')) throw new Error('FinalCTA: class missing'); console.log('FinalCTA OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/FinalCTA.astro` existe com interface Props, data-reveal, CSS com radial-gradient e border-top: 1px solid var(--hairline-strong), :global(.flame) rule
    - `src/pages/deep-dive-python-neurodivergentes/index.astro` existe com ogImage="/python-neurodivergentes-og.png", url com o domínio correto, JSON-LD com courseCode "PY-ND-DEEP-DIVE", SEM import de Testimonials, skip-link com href="#main"
    - `dist/deep-dive-python-neurodivergentes/index.html` existe após build
    - `dist/deep-dive-python-neurodivergentes/index.html` contém `python-neurodivergentes-og.png` na meta og:image
    - `dist/deep-dive-python-neurodivergentes/index.html` contém `PY-ND-DEEP-DIVE` no JSON-LD
    - `dist/deep-dive-vm/index.html` contém "TURMA CHAMA AZUL" (VM pricing sem regressão)
    - `dist/index.html` contém `/deep-dive-python-neurodivergentes/` (hub card Python ativo)
    - `npm run build` sai com código 0 — nenhum erro TypeScript
  </acceptance_criteria>
  <done>FinalCTA.astro criado, LP Python disponível em /deep-dive-python-neurodivergentes/, VM LP sem regressão, build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Props → template Astro | Props são tipadas com TypeScript — erros de tipo falham no build antes de chegar ao browser |
| set:html em props | Conteúdo HTML nos props (titleHtml, headlineHtml, includes[].html) vem de arquivos de dados em código — não de input de usuário |
| JSON-LD inline | Schema.org Course — dados estáticos, sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------|-----------------|
| T-09-03 | Tampering | set:html props em componentes | accept | Conteúdo vem de arquivos .ts em código-fonte, não de usuário final. TypeScript valida shape dos objetos no build. |
| T-09-04 | Information Disclosure | JSON-LD offers.url | accept | URL pública do produto, sem PII ou dados sensíveis |
| T-09-05 | Spoofing | ctaHref PYTHON_COURSE.hotmartUrl | accept | Placeholder explicitamente documentado; usuário responsável por substituir antes do deploy |
| T-09-SC | Tampering | npm installs | accept | Nenhum novo pacote instalado — stack existente apenas |
</threat_model>

<verification>
Verificação final do Wave 2:
1. `npm run build` → exit 0
2. `ls dist/deep-dive-python-neurodivergentes/index.html` → existe
3. `grep -c "python-neurodivergentes-og.png" dist/deep-dive-python-neurodivergentes/index.html` → ≥1
4. `grep -c "TURMA CHAMA AZUL" dist/deep-dive-vm/index.html` → ≥1 (VM LP sem regressão)
5. `grep -c "deep-dive-python-neurodivergentes" dist/index.html` → ≥1 (hub card ativo)
</verification>

<success_criteria>
- 10 componentes de seção + ModuleDetails refatorados para data-driven
- FinalCTA.astro criado (único componente novo)
- LP Python disponível em /deep-dive-python-neurodivergentes/ após build
- VM LP sem nenhuma regressão visual ou de conteúdo
- Hub exibe card Python ativo com link correto
- og:image na LP Python aponta para /python-neurodivergentes-og.png
- npm run build: exit 0
</success_criteria>

<output>
Criar `.planning/phases/09-python-neurodivergentes/09-02-SUMMARY.md` quando concluído.
</output>
