---
phase: 09-python-neurodivergentes
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - tests/e2e/python-lp.spec.ts
  - tests/e2e/hub.spec.ts
  - tests/seo/seo-meta.test.ts
autonomous: true
requirements:
  - PY-05

must_haves:
  truths:
    - "tests/e2e/python-lp.spec.ts existe e passa no Playwright (HTTP 200, h1, CTA, a11y)"
    - "tests/e2e/hub.spec.ts passa com 3 cards ao invés de 2"
    - "tests/seo/seo-meta.test.ts passa com teste 17 (og:image Python) e teste 18 (sitemap Python)"
    - "npm run test:all e npx playwright test saem ambos com código 0"
    - "Zero regressão nas LPs existentes (deep-dive-vm, hub)"
  artifacts:
    - path: "tests/e2e/python-lp.spec.ts"
      provides: "E2E spec para LP Python cobrindo HTTP 200, seções, CTA, a11y, responsivo"
    - path: "tests/e2e/hub.spec.ts"
      provides: "hub.spec.ts atualizado com toHaveCount(3) e asserção de link Python"
      contains: "toHaveCount(3)"
    - path: "tests/seo/seo-meta.test.ts"
      provides: "Teste 17 og:image Python e teste 18 sitemap Python adicionados"
      contains: "deep-dive-python-neurodivergentes"
  key_links:
    - from: "tests/e2e/hub.spec.ts"
      to: "src/data/courses.ts"
      via: "hub renderiza courses array — 3 entries"
      pattern: "toHaveCount\\(3\\)"
    - from: "tests/seo/seo-meta.test.ts"
      to: "dist/deep-dive-python-neurodivergentes/index.html"
      via: "readFileSync + extractMetaContent"
      pattern: "python-neurodivergentes-og.png"
---

<objective>
Criar o E2E spec da LP Python, atualizar hub.spec.ts (count 2→3), e adicionar testes SEO 17 e 18 ao Vitest — garantindo suite completa verde sem nenhuma regressão.

Purpose: PY-05 exige suite verde após a adição da LP Python. hub.spec.ts atual quebraria com `toHaveCount(2)` porque courses.ts agora tem 3 entradas. Os testes devem ser adicionados e corrigidos antes de qualquer merge.
Output: python-lp.spec.ts novo, hub.spec.ts corrigido, seo-meta.test.ts com testes 17-18, CI verde.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-python-neurodivergentes/09-CONTEXT.md
@.planning/phases/09-python-neurodivergentes/09-RESEARCH.md
@.planning/phases/09-python-neurodivergentes/09-PATTERNS.md
@.planning/phases/09-python-neurodivergentes/09-02-SUMMARY.md

<interfaces>
<!-- Padrões extraídos dos arquivos de teste existentes — o executor usa estes sem re-explorar. -->

De tests/e2e/hub.spec.ts (estrutura atual):
```typescript
import { test, expect } from "@playwright/test";
import AxeBuilder from "@axe-core/playwright";

test.describe("Hub load", () => {
  test("2 course cards are present", async ({ page }) => {
    await page.goto("./");
    await expect(page.locator(".course-card")).toHaveCount(2);  // LINHA A CORRIGIR → 3
  });
  // ... outros testes que permanecem inalterados
  test("active course card link href contains /deep-dive-vm/", async ({ page }) => { ... });
});
```

Linha exata a alterar em hub.spec.ts:
- Encontrar a asserção `toHaveCount(2)` no describe "Hub load"
- Alterar para `toHaveCount(3)`
- Adicionar novo test após o bloco dos testes existentes de active cards:
```typescript
test("Python active course card link href contains /deep-dive-python-neurodivergentes/", async ({ page }) => {
  await page.goto("./");
  const courseLinks = page.locator(".course-card.active .course-link");
  await expect(courseLinks).toHaveCount(2); // VM + Python ambos active
  const hrefs = await courseLinks.evaluateAll(links => links.map(l => l.getAttribute("href")));
  expect(hrefs.some(h => h?.includes("/deep-dive-python-neurodivergentes/"))).toBe(true);
});
```

De tests/seo/seo-meta.test.ts (estrutura e padrão para testes novos):
```typescript
// Variáveis de módulo existentes:
const DIST_DIR = join(process.cwd(), "dist");
// extractMetaContent helper já existe

// Padrão do teste 16 (modelo — NÃO reusar DIST_INDEX, declarar local):
it("16. ...", () => {
  const ec2IndexPath = join(DIST_DIR, "deep-dive-ec2/index.html");
  expect(existsSync(ec2IndexPath), `...`).toBe(true);
  const ec2Html = readFileSync(ec2IndexPath, "utf-8");
  // assertions...
});

// Teste 17 a adicionar (após teste 16, antes do fechamento do describe):
it("17. dist/deep-dive-python-neurodivergentes/index.html og:image points to python-neurodivergentes-og.png", () => {
  const pythonIndexPath = join(DIST_DIR, "deep-dive-python-neurodivergentes/index.html");
  expect(
    existsSync(pythonIndexPath),
    `dist/deep-dive-python-neurodivergentes/index.html not found. Run 'npm run build' first.`
  ).toBe(true);
  const pythonHtml = readFileSync(pythonIndexPath, "utf-8");
  const ogImage = extractMetaContent(pythonHtml, "og:image");
  expect(ogImage).toBeTruthy();
  expect(ogImage).toContain("python-neurodivergentes-og.png");
});

// Teste 18 a adicionar (após teste 17):
it("18. sitemap-0.xml contains /deep-dive-python-neurodivergentes/", () => {
  const sitemapPath = join(DIST_DIR, "sitemap-0.xml");
  expect(existsSync(sitemapPath), "sitemap-0.xml not found. Run 'npm run build' first.").toBe(true);
  const sitemap = readFileSync(sitemapPath, "utf-8");
  expect(sitemap).toContain("deep-dive-python-neurodivergentes");
});
```

Padrão do E2E spec da LP Python (de PATTERNS.md §python-lp.spec.ts):
```typescript
import { test, expect } from "@playwright/test";
import AxeBuilder from "@axe-core/playwright";

test.describe("Python LP load", () => {
  test("GET /deep-dive-python-neurodivergentes/ returns HTTP 200", async ({ page }) => {
    const response = await page.goto("./deep-dive-python-neurodivergentes/");
    expect(response?.status()).toBe(200);
  });

  test("<h1> is visible and contains Python copy", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    await expect(page.locator("h1")).toBeVisible();
    await expect(page.locator("h1")).toContainText("Python");
  });

  test("primary CTA links to #investimento", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    const cta = page.locator('[data-testid="hero-cta-primary"]');
    await expect(cta).toBeVisible();
    const href = await cta.getAttribute("href");
    expect(href).toContain("#investimento");
  });

  test("ghost CTA links to #ementa", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    const cta = page.locator('[data-testid="hero-cta-ghost"]');
    await expect(cta).toBeVisible();
    const href = await cta.getAttribute("href");
    expect(href).toContain("#ementa");
  });

  test("#investimento section is present", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    await expect(page.locator("#investimento")).toBeAttached();
  });

  test("#ementa section is present", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    await expect(page.locator("#ementa")).toBeAttached();
  });

  test("sticky CTA is present in DOM", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    await expect(page.locator('[data-testid="sticky-cta"]')).toBeAttached();
  });
});

test.describe("Python LP accessibility", () => {
  test("skip link has href='#main'", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    await expect(page.locator("a.skip-link")).toHaveAttribute("href", "#main");
  });

  test("no critical axe-core violations (WCAG 2.0 A/AA)", async ({ page }) => {
    await page.goto("./deep-dive-python-neurodivergentes/");
    const results = await new AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa"]).analyze();
    const criticalViolations = results.violations.filter(v => v.impact === "critical");
    expect(criticalViolations).toHaveLength(0);
  });
});

test.describe("Python LP responsive", () => {
  test("mobile 375x812: h1 visible, no horizontal overflow", async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 812 });
    await page.goto("./deep-dive-python-neurodivergentes/");
    await expect(page.locator("h1")).toBeVisible();
    const hasOverflow = await page.evaluate(() => document.documentElement.scrollWidth > window.innerWidth);
    expect(hasOverflow).toBe(false);
  });
});
```

IMPORTANTE sobre seo-meta.test.ts — Pitfall 5 (RESEARCH.md):
NUNCA usar a variável `DIST_INDEX` ou `html` de escopo de módulo para os novos testes.
Sempre declarar `const pythonIndexPath = join(DIST_DIR, ...)` LOCAL dentro do `it()` block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar python-lp.spec.ts + atualizar hub.spec.ts</name>
  <files>
    tests/e2e/python-lp.spec.ts
    tests/e2e/hub.spec.ts
  </files>
  <read_first>
    - tests/e2e/hub.spec.ts — ler COMPLETO para encontrar a linha exata com toHaveCount(2) e a estrutura dos describe blocks (para saber onde inserir o novo test de link Python)
    - tests/e2e/ec2-coming-soon.spec.ts — ler para confirmar padrão de E2E spec para LP simples (confirmação do padrão)
    - playwright.config.ts — ler para confirmar baseURL e configuração (webServer command, timeout)
  </read_first>
  <action>
    Passo 1 — Criar tests/e2e/python-lp.spec.ts:
    Criar arquivo novo seguindo exatamente o padrão da seção <interfaces> acima. O arquivo cobre 3 describe blocks: "Python LP load" (6 testes), "Python LP accessibility" (2 testes), "Python LP responsive" (1 teste). Total: 9 testes. NÃO adicionar testes adicionais além dos especificados — manter o spec focado. Os testids `data-testid="hero-cta-primary"` e `data-testid="hero-cta-ghost"` devem corresponder ao que foi adicionado no Hero.astro no plano 09-02 (verificar se foram adicionados — se não, corrigir em Hero.astro também). `data-testid="sticky-cta"` deve corresponder ao StickyCta (verificar que já existe — era hardcoded no componente antes do refator).

    Passo 2 — Atualizar tests/e2e/hub.spec.ts:
    Após ler o arquivo completo, fazer duas alterações cirúrgicas:
    a) Encontrar a linha com `toHaveCount(2)` dentro do test "course cards are present" (ou nome similar) — alterar APENAS o número: `toHaveCount(2)` → `toHaveCount(3)`. Não alterar mais nada neste test.
    b) Adicionar o novo test de link Python conforme o template da seção <interfaces>. Inserir após os testes de "active course card" existentes, dentro do mesmo describe block "Hub load" (ou equivalente).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npx playwright test tests/e2e/python-lp.spec.ts --project=chromium</automated>
    <automated>npx playwright test tests/e2e/hub.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/python-lp.spec.ts` existe com 3 describe blocks e pelo menos 9 testes
    - `tests/e2e/hub.spec.ts` contém `toHaveCount(3)` e NÃO contém `toHaveCount(2)` (no contexto de course cards)
    - `tests/e2e/hub.spec.ts` contém assertion com `/deep-dive-python-neurodivergentes/`
    - `npx playwright test tests/e2e/python-lp.spec.ts --project=chromium` sai com código 0 (todos passam)
    - `npx playwright test tests/e2e/hub.spec.ts --project=chromium` sai com código 0
    - Nenhum test file existente foi deletado ou teve testes removidos
  </acceptance_criteria>
  <done>python-lp.spec.ts criado (9 testes passando), hub.spec.ts corrigido (count 3 + link Python).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Adicionar testes SEO 17 e 18 + validação suite completa</name>
  <files>
    tests/seo/seo-meta.test.ts
  </files>
  <read_first>
    - tests/seo/seo-meta.test.ts — ler COMPLETO para: (a) identificar o número do último teste existente (deve ser 16 após o plano 08 — confirmar), (b) localizar onde adicionar os novos testes dentro do describe block correto, (c) confirmar as variáveis de módulo disponíveis (DIST_DIR, extractMetaContent helper, existsSync, readFileSync), (d) verificar se existe import de `existsSync` — adicionar se não existir
  </read_first>
  <action>
    Adicionar testes 17 e 18 a tests/seo/seo-meta.test.ts:

    Localizar o primeiro `describe("SEO meta-tag static assertions...")` block e encontrar o último `it()` dentro dele (deve ser o teste 16 sobre EC2 og:image ou similar). Inserir dois novos `it()` blocks APÓS o teste 16, ANTES do fechamento `}` do describe block.

    Teste 17 — Python og:image:
    ```
    it("17. dist/deep-dive-python-neurodivergentes/index.html og:image points to python-neurodivergentes-og.png", () => {
      const pythonIndexPath = join(DIST_DIR, "deep-dive-python-neurodivergentes/index.html");
      expect(existsSync(pythonIndexPath), `dist/deep-dive-python-neurodivergentes/index.html not found. Run 'npm run build' first.`).toBe(true);
      const pythonHtml = readFileSync(pythonIndexPath, "utf-8");
      const ogImage = extractMetaContent(pythonHtml, "og:image");
      expect(ogImage).toBeTruthy();
      expect(ogImage).toContain("python-neurodivergentes-og.png");
    });
    ```

    Teste 18 — sitemap Python:
    ```
    it("18. sitemap-0.xml contains /deep-dive-python-neurodivergentes/", () => {
      const sitemapPath = join(DIST_DIR, "sitemap-0.xml");
      expect(existsSync(sitemapPath), "sitemap-0.xml not found. Run 'npm run build' first.").toBe(true);
      const sitemap = readFileSync(sitemapPath, "utf-8");
      expect(sitemap).toContain("deep-dive-python-neurodivergentes");
    });
    ```

    CRÍTICO — Pitfall 5: As variáveis `pythonIndexPath` e `pythonHtml` devem ser declaradas DENTRO do `it()` block, não no escopo de módulo. NÃO usar a variável `DIST_INDEX` (se existir no módulo) para a LP Python — ela aponta para outro dist/index.html.

    Após adicionar os testes, verificar se `existsSync` está no import do arquivo. Se não estiver (pode ser que apenas `readFileSync` estava importado), adicionar ao import de 'fs'.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npx vitest run tests/seo/seo-meta.test.ts</automated>
    <automated>npx playwright test --project=chromium</automated>
    <automated>npm run test:all</automated>
  </verify>
  <acceptance_criteria>
    - `tests/seo/seo-meta.test.ts` contém strings "17." e "18." indicando os novos testes
    - `tests/seo/seo-meta.test.ts` contém "python-neurodivergentes-og.png" como assertion
    - `tests/seo/seo-meta.test.ts` contém "deep-dive-python-neurodivergentes" como assertion do sitemap
    - `npx vitest run tests/seo/seo-meta.test.ts` sai com código 0 (todos os 18 testes passam)
    - `npx playwright test --project=chromium` sai com código 0 (nenhuma regressão em LP VM, hub, EC2)
    - `npm run test:all` sai com código 0 (suite Vitest completa verde)
    - Os testes 1-16 existentes CONTINUAM passando — nenhuma regressão introduzida
  </acceptance_criteria>
  <done>Testes SEO 17 e 18 adicionados, suite completa verde, zero regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Playwright → built static files | Testes acessam arquivos em dist/ via servidor preview — sem input de usuário |
| Vitest → dist/ HTML files | Leitura estática de arquivos compilados — sem execução de código externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | seo-meta.test.ts DIST_INDEX variable reuse | mitigate | Pitfall 5 explicitamente evitado: usar variável local `pythonIndexPath` dentro do `it()` block — jamais referenciar `DIST_INDEX` do módulo |
| T-09-07 | Repudiation | hub.spec.ts count assertion | accept | toHaveCount(3) é uma asserção determinística — qualquer mudança em courses.ts causará falha explícita no CI |
| T-09-SC | Tampering | npm installs | accept | Nenhum novo pacote — @axe-core/playwright já instalado |
</threat_model>

<verification>
Verificação final do Wave 3 (suite completa):
1. `npm run build` → exit 0
2. `npx vitest run tests/seo/seo-meta.test.ts` → 18 testes passando
3. `npx playwright test tests/e2e/python-lp.spec.ts --project=chromium` → 9 testes passando
4. `npx playwright test tests/e2e/hub.spec.ts --project=chromium` → todos passando
5. `npx playwright test --project=chromium` → zero falhas (incluindo LP VM, EC2)
6. `npm run test:all` → exit 0
</verification>

<success_criteria>
- tests/e2e/python-lp.spec.ts: 9 testes, todos passando no Playwright Chromium
- tests/e2e/hub.spec.ts: toHaveCount(3), link Python assertion, todos passando
- tests/seo/seo-meta.test.ts: testes 17 (og:image Python) e 18 (sitemap) adicionados e passando
- npm run test:all: exit 0
- npx playwright test --project=chromium: exit 0
- Zero regressão: deep-dive-vm, hub, EC2 testes todos continuam passando
</success_criteria>

<output>
Criar `.planning/phases/09-python-neurodivergentes/09-03-SUMMARY.md` quando concluído.
</output>
