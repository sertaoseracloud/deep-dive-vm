---
phase: 09-python-neurodivergentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/python-neurodivergentes-og.png
  - src/data/python-course.ts
  - src/data/courses.ts
  - src/components/layout/UrgencyBar.astro
  - src/components/layout/StickyCta.astro
autonomous: true
requirements:
  - PY-03
  - PY-04

must_haves:
  truths:
    - "public/python-neurodivergentes-og.png existe com dimensões 1200×630 antes de qualquer build"
    - "src/data/courses.ts tem 3 entradas, Python como status 'active'"
    - "UrgencyBar.astro aceita prop content: string e não usa conteúdo hardcoded"
    - "StickyCta.astro aceita props priceLabel, ctaHref e ctaText tipados"
    - "src/data/python-course.ts exporta PYTHON_COURSE com hotmartUrl, route e ogImage"
  artifacts:
    - path: "public/python-neurodivergentes-og.png"
      provides: "OG image 1200×630 base claudio2.png"
    - path: "src/data/python-course.ts"
      provides: "PYTHON_COURSE const com hotmartUrl placeholder, route e ogImage"
    - path: "src/data/courses.ts"
      provides: "Array de 3 cursos com Python como 3ª entrada status active"
    - path: "src/components/layout/UrgencyBar.astro"
      provides: "Componente data-driven — props content: string"
    - path: "src/components/layout/StickyCta.astro"
      provides: "Componente data-driven — props priceLabel, ctaHref, ctaText"
  key_links:
    - from: "src/data/courses.ts"
      to: "src/pages/index.astro"
      via: "import courses array"
      pattern: "status.*active"
    - from: "src/data/python-course.ts"
      to: "src/pages/deep-dive-python-neurodivergentes/index.astro"
      via: "import PYTHON_COURSE"
      pattern: "PYTHON_COURSE.hotmartUrl"
---

<objective>
Criar todos os pré-requisitos de infraestrutura da LP Python antes de qualquer build: OG image, data files e refatorar os dois componentes de layout reutilizáveis (UrgencyBar e StickyCta) para data-driven.

Purpose: OG image deve existir em public/ ANTES de qualquer build (anti-pattern #3 do HOWTO). Sem este plano, Wave 2 não pode criar index.astro nem executar npm run build.
Output: python-neurodivergentes-og.png, python-course.ts, courses.ts atualizado, UrgencyBar e StickyCta props-based.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-python-neurodivergentes/09-CONTEXT.md
@.planning/phases/09-python-neurodivergentes/09-RESEARCH.md
@.planning/phases/09-python-neurodivergentes/09-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos das fontes de verdade. -->

De src/data/courses.ts (estado atual — 2 entradas):
```typescript
export type CourseStatus = 'active' | 'coming-soon';

export interface Course {
  title: string;
  description: string;
  url: string;
  status: CourseStatus;
}

export const courses: Course[] = [
  { title: 'Deep Dive Azure VM', description: '...', url: '/deep-dive-vm/', status: 'active' },
  { title: 'Deep Dive EC2', description: '...', url: '/deep-dive-ec2/', status: 'coming-soon' },
];
```

De src/components/layout/UrgencyBar.astro (estado atual — hardcoded):
- Frontmatter atual: sem props (comentário `const { horas = "54h", sessoes = "6" } = Astro.props` desativado)
- Conteúdo hardcoded: "54h de imersão + 6 sessões 1:1 + Toolkit Terraform · acesso por 12 meses"
- Render: `<div class="urgency-bar" role="status" aria-label="...">` com text inline

De src/components/layout/StickyCta.astro (estado atual — hardcoded):
- Frontmatter atual: sem props
- Conteúdo hardcoded: `divido em <b>12× R$ 78,92</b>`, href="#investimento", texto "Quero começar →"
- Nota: typo "divido em" (correto seria "desde") — o refator corrige via prop

HOWTO-new-landing-page.md — script sharp padrão:
```javascript
// gen-SLUG-og.mjs (deletar após executar)
import sharp from 'sharp';
import { fileURLToPath } from 'url';
import { dirname, join } from 'path';
const __dirname = dirname(fileURLToPath(import.meta.url));
await sharp(join(__dirname, 'src/assets/claudio2.png'))
  .resize(1200, 630, { fit: 'cover', position: 'top' })
  .png()
  .toFile(join(__dirname, 'public/python-neurodivergentes-og.png'));
console.log('Generated public/python-neurodivergentes-og.png');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Gerar OG image + criar python-course.ts + estender courses.ts</name>
  <files>
    public/python-neurodivergentes-og.png
    src/data/python-course.ts
    src/data/courses.ts
  </files>
  <read_first>
    - src/data/courses.ts — ler estado atual completo antes de modificar (CourseStatus union, Course interface, array atual de 2 entradas)
    - src/data/social-links.ts — ler padrão de arquivo de dados tipado com export `as const` (modelo para python-course.ts)
    - HOWTO-new-landing-page.md — ler seção do sharp script para confirmar o padrão exato (anti-pattern #3 documentado)
    - src/assets/claudio2.png — confirmar existência do arquivo base para o sharp script
  </read_first>
  <action>
    Passo 1 — Gerar OG image (PRIMEIRO de tudo):
    Criar o arquivo temporário gen-python-neurodivergentes-og.mjs na raiz do repo com o script sharp exato do HOWTO: `sharp(join(__dirname, 'src/assets/claudio2.png')).resize(1200, 630, { fit: 'cover', position: 'top' }).png().toFile(join(__dirname, 'public/python-neurodivergentes-og.png'))`. Executar com `node gen-python-neurodivergentes-og.mjs`. Verificar que public/python-neurodivergentes-og.png existe e tem 1200×630. Deletar o arquivo .mjs temporário.

    Passo 2 — Criar src/data/python-course.ts:
    Novo arquivo seguindo o padrão `as const` de social-links.ts. Conteúdo: `export const PYTHON_COURSE = { hotmartUrl: "https://pay.hotmart.com/", route: "/deep-dive-python-neurodivergentes/", ogImage: "/python-neurodivergentes-og.png" } as const;`. Adicionar comentário inline: `// placeholder — substitua pela URL real antes do deploy`.

    Passo 3 — Atualizar src/data/courses.ts:
    Adicionar 3ª entrada ao array `courses` após a entrada do EC2 (per D-01). Valores exatos: `title: 'Python para Neurodivergentes'`, `description: 'Deep Dive em Python para devs com TDAH, autismo, dislexia ou ansiedade. 60h em micro-aulas, 5 frentes, Body Doubling semanal.'`, `url: '/deep-dive-python-neurodivergentes/'`, `status: 'active'`. Não alterar entradas existentes (VM e EC2). Não alterar CourseStatus union nem Course interface — ambas já cobrem o novo status 'active'.
  </action>
  <verify>
    <automated>node -e "import('sharp').then(s=>s.default('public/python-neurodivergentes-og.png').metadata().then(m=>{ if(m.width!==1200||m.height!==630||m.format!=='png') throw new Error('Dimensoes incorretas: '+JSON.stringify(m)); console.log('OK',m.width,m.height,m.format); }))"</automated>
    <automated>node -e "import('./src/data/courses.js').catch(()=>import('./src/data/courses.ts')).catch(()=>{ const fs=require('fs'); const c=fs.readFileSync('src/data/courses.ts','utf-8'); const matches=c.match(/status:\s*'active'/g); if(!matches||matches.length<2) throw new Error('Python active entry missing'); console.log('courses.ts OK — active entries: '+matches.length); })"</automated>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/data/courses.ts','utf-8'); if(!c.includes('deep-dive-python-neurodivergentes')) throw new Error('Python url missing'); if(!c.includes(\"status: 'active'\")) throw new Error('Python status missing'); console.log('courses.ts assertions OK')"</automated>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('src/data/python-course.ts','utf-8'); if(!p.includes('hotmartUrl')) throw new Error('hotmartUrl missing'); if(!p.includes('/python-neurodivergentes-og.png')) throw new Error('ogImage missing'); console.log('python-course.ts OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `public/python-neurodivergentes-og.png` existe, width=1200, height=630, format=png
    - `src/data/python-course.ts` exporta `PYTHON_COURSE` com campos `hotmartUrl`, `route`, `ogImage` e usa `as const`
    - `src/data/courses.ts` tem exatamente 3 entradas no array `courses`; a 3ª tem `url: '/deep-dive-python-neurodivergentes/'` e `status: 'active'`
    - Entradas existentes de VM e EC2 permanecem inalteradas
    - O arquivo .mjs temporário NÃO existe na raiz após a execução
  </acceptance_criteria>
  <done>OG image gerada (1200×630), python-course.ts criado, courses.ts com 3ª entrada Python ativa.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Refatorar UrgencyBar.astro e StickyCta.astro para data-driven</name>
  <files>
    src/components/layout/UrgencyBar.astro
    src/components/layout/StickyCta.astro
    src/pages/deep-dive-vm/index.astro
  </files>
  <read_first>
    - src/components/layout/UrgencyBar.astro — ler estado atual completo (frontmatter, template, style block inteiro)
    - src/components/layout/StickyCta.astro — ler estado atual completo (hardcoded priceLabel, href, button text, CSS)
    - src/pages/deep-dive-vm/index.astro — ler invocações atuais de UrgencyBar e StickyCta para extrair valores VM que precisam ser passados como props após o refator
  </read_first>
  <action>
    UrgencyBar.astro — refatorar:
    Adicionar `interface Props { content: string; }` e `const { content } = Astro.props;` no frontmatter. Substituir o texto hardcoded no template por `<Fragment set:html={content} />`. O bloco `<style>` permanece COMPLETAMENTE INALTERADO — nenhuma linha de CSS muda.

    StickyCta.astro — refatorar:
    Adicionar `interface Props { priceLabel: string; ctaHref: string; ctaText: string; }` e `const { priceLabel, ctaHref, ctaText } = Astro.props;` no frontmatter. No template substituir o texto hardcoded "divido em..." por `<Fragment set:html={priceLabel} />` (ou `set:html` no elemento que renderiza o label). Substituir `href="#investimento"` hardcoded por `href={ctaHref}`. Substituir o texto do botão hardcoded por `{ctaText}`. O bloco `<style>` permanece COMPLETAMENTE INALTERADO.

    src/pages/deep-dive-vm/index.astro — atualizar chamadas:
    Após ler o arquivo, adicionar as props necessárias nas invocações dos componentes refatorados:
    - `<UrgencyBar content="54h de imersão + 6 sessões 1:1 + Toolkit Terraform · acesso por 12 meses" />`
    - `<StickyCta priceLabel="DESDE 12× R$ 78,92" ctaHref="#investimento" ctaText="Quero começar →" />`
    (Verificar os valores exatos no arquivo antes de escrever — usar o que estiver hardcoded atualmente no componente.)

    Importante: VM LP não pode quebrar. O refator é backward-compatible porque apenas adiciona props obrigatórias e atualiza o caller.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node -e "const fs=require('fs'); const u=fs.readFileSync('src/components/layout/UrgencyBar.astro','utf-8'); if(!u.includes('interface Props')) throw new Error('UrgencyBar: interface Props missing'); if(u.includes('54h de imersão')) throw new Error('UrgencyBar: hardcoded VM content still present'); console.log('UrgencyBar OK')"</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/layout/StickyCta.astro','utf-8'); if(!s.includes('interface Props')) throw new Error('StickyCta: interface Props missing'); if(s.includes('divido em')) throw new Error('StickyCta: typo hardcoded still present'); if(s.includes('Quero comecar') && !s.includes('ctaText')) throw new Error('StickyCta: hardcoded text still present'); console.log('StickyCta OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `UrgencyBar.astro` frontmatter contém `interface Props { content: string; }` e `const { content } = Astro.props;`
    - `UrgencyBar.astro` NÃO contém a string "54h de imersão" no template (removida do hardcode)
    - `StickyCta.astro` frontmatter contém `interface Props` com campos `priceLabel`, `ctaHref`, `ctaText`
    - `StickyCta.astro` NÃO contém a string "divido em" (typo corrigido pelo refator)
    - `src/pages/deep-dive-vm/index.astro` passa props explícitas para `UrgencyBar` e `StickyCta`
    - `npm run build` sai com código 0 — dist/ criado sem erros TypeScript
    - `dist/deep-dive-vm/index.html` existe após o build
  </acceptance_criteria>
  <done>UrgencyBar e StickyCta data-driven, VM LP atualizada com props explícitas, build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Script node → filesystem | Script sharp escreve em public/ — conteúdo controlado (arquivo local) |
| Data files → build Astro | courses.ts e python-course.ts são consumidos em build-time — sem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | courses.ts | accept | Arquivo tipado, TypeScript valida Course interface no build — erros de tipo geram falha no build |
| T-09-02 | Information Disclosure | python-course.ts hotmartUrl | accept | URL é placeholder público, sem secrets — comentário inline adverte sobre substituição pré-deploy |
| T-09-SC | Tampering | npm/pip/cargo | accept | Nenhum novo pacote instalado neste plano — sharp já está em package.json |
</threat_model>

<verification>
Verificação final do Wave 1:
1. `node -e "import('sharp').then(s=>s.default('public/python-neurodivergentes-og.png').metadata().then(m=>console.log(m.width,m.height,m.format)))"` → `1200 630 png`
2. `grep -c "deep-dive-python-neurodivergentes" src/data/courses.ts` → 1
3. `npm run build` → exit code 0
4. `ls dist/deep-dive-vm/index.html` existe
</verification>

<success_criteria>
- public/python-neurodivergentes-og.png: 1200×630, format png
- src/data/courses.ts: 3 entradas, Python como status 'active'
- src/data/python-course.ts: PYTHON_COURSE exportado com hotmartUrl, route, ogImage
- UrgencyBar.astro: aceita `content: string`, sem hardcode VM
- StickyCta.astro: aceita priceLabel, ctaHref, ctaText; sem typo "divido em"
- npm run build: exit 0
</success_criteria>

<output>
Criar `.planning/phases/09-python-neurodivergentes/09-01-SUMMARY.md` quando concluído.
</output>
