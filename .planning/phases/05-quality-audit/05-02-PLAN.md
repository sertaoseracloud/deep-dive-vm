---
phase: 05-quality-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/e2e/motion-accessibility.spec.ts
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "Com prefers-reduced-motion: reduce ativo, [data-reveal] tem opacity:1 e transition:none imediatamente"
    - "Com prefers-reduced-motion: reduce ativo, [data-stagger] tem animation-name igual a 'none'"
    - "Com prefers-reduced-motion: reduce ativo, .hero-stagger-item tem animation-name igual a 'none'"
    - "Com prefers-reduced-motion: reduce ativo, motion.span do SettingsToggle não exibe transição de opacity animada"
    - "Os testes Playwright do gate QUAL-02 passam no Chromium com build prévio"
  artifacts:
    - path: "tests/e2e/motion-accessibility.spec.ts"
      provides: "Testes E2E de reduced-motion para os 4 seletores alvo"
      contains: "reducedMotion"
  key_links:
    - from: "tests/e2e/motion-accessibility.spec.ts"
      to: "page.emulateMedia"
      via: "{ reducedMotion: 'reduce' } passado antes de page.goto"
      pattern: "emulateMedia.*reducedMotion"
    - from: "tests/e2e/motion-accessibility.spec.ts"
      to: "getComputedStyle"
      via: "verify animation-name === 'none' para data-stagger e hero-stagger-item"
      pattern: "animationName|animation-name"
---

<objective>
Executar o gate QUAL-02: verificação de conformidade total com prefers-reduced-motion para todas as animações adicionadas na fase v1.2.

Conforme D-AUDIT-02: usar `page.emulateMedia({ reducedMotion: 'reduce' })` do Playwright. Adicionar testes ao arquivo EXISTENTE `tests/e2e/motion-accessibility.spec.ts`. Os 4 seletores alvo são: `[data-reveal]`, `[data-stagger]`, `.hero-stagger-item`, e `motion.span` do SettingsToggle.

Purpose: Garantir que nenhuma animação v1.2 persiste quando o usuário ativa prefers-reduced-motion — pré-requisito para fechar a fase 05.

Output: Testes Playwright no arquivo existente que cobrem os 4 seletores, todos passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quality-audit/05-01-SUMMARY.md

<interfaces>
Da src/layouts/Layout.astro (branch main):

CSS relevante para os seletores alvo:

[data-reveal]: opacity:0, transform:translateY(20px), transition opacity+transform
[data-reveal][data-revealed]: opacity:1, transform:none
@media (prefers-reduced-motion:reduce) [data-reveal]: opacity:1, transform:none, transition:none

[data-stagger]: animation: fade-up 400ms forwards, animation-play-state: paused
[data-revealed] [data-stagger]: animation-play-state: running
@media (prefers-reduced-motion:reduce) [data-stagger]: animation:none, opacity:1, transform:none

.hero-stagger-item (adicionada via JS em HeroMotionSingle):
@media (prefers-reduced-motion:reduce) .hero-stagger-item: animation:none, opacity:1, transform:none

Do src/components/SettingsToggle.tsx:
motion.span data-testid="motion-label" com animate={{ opacity: motionEnabled ? 1 : 0.4 }}
MotionConfig reducedMotion="user" — suprime automaticamente via matchMedia

Do src/components/HeroMotion.tsx HeroMotionSingle:
useEffect checa: window.matchMedia("(prefers-reduced-motion: reduce)").matches
Se prefersReduced === true: return; (sem aplicar hero-stagger-item)
Logo: com reduced-motion ativo, .hero-stagger-item NÃO existe no DOM

Pitfall documentado no RESEARCH.md:
- getComputedStyle(el).transform retorna "matrix(1,0,0,1,0,0)" quando transform:none — verificar ambos
- Verificar animationName (não animation shorthand) — shorthand inclui timing
- [data-stagger] paused por default — com reduced-motion, animation:none → animationName === "none"

API Playwright:
page.emulateMedia({ reducedMotion: 'reduce' }) — deve ser chamado ANTES de page.goto()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar testes reduced-motion ao motion-accessibility.spec.ts existente</name>
  <files>tests/e2e/motion-accessibility.spec.ts</files>
  <read_first>
    - tests/e2e/motion-accessibility.spec.ts (ler o arquivo COMPLETO — adicionar ao final, não substituir)
    - src/layouts/Layout.astro (confirmar seletores CSS para [data-reveal], [data-stagger], hero-stagger-item)
    - src/components/HeroMotion.tsx (confirmar lógica prefersReduced no HeroMotionSingle)
    - src/components/SettingsToggle.tsx (confirmar data-testid="motion-label" e MotionConfig)
    - playwright.config.ts (confirmar baseURL e webServer config)
  </read_first>
  <behavior>
    - Test: [data-reveal] com reduced-motion → opacity computed é "1" e transitionProperty é "none" ou ""
    - Test: [data-stagger] com reduced-motion → animationName computado é "none"
    - Test: .hero-stagger-item com reduced-motion → elemento NÃO existe no DOM (HeroMotionSingle retorna antes de adicionar a classe)
    - Test: motion.span[data-testid="motion-label"] com reduced-motion → MotionConfig suprime transição; verificar que não há animação CSS ativa (ou que o elemento tem opacity computada não-animada)
  </behavior>
  <action>
    Ler o arquivo tests/e2e/motion-accessibility.spec.ts completo primeiro. Adicionar um novo describe block ao final do arquivo (não substituir nenhum teste existente). O novo bloco deve se chamar "QUAL-02: reduced-motion compliance" e conter 4 testes:

    TESTE 1 — [data-reveal] reduced-motion:
    Chamar page.emulateMedia({ reducedMotion: 'reduce' }) ANTES de page.goto('./').
    Localizar o primeiro elemento com seletor '[data-reveal]'.
    Se não existir na página (pode não estar visível sem scroll), usar page.evaluate para localizar via document.querySelector('[data-reveal]').
    Verificar via getComputedStyle:
      - opacity === "1" (não "0")
      - transitionProperty === "none" ou transitionDuration === "0s"
    Usar page.evaluate para acessar window.getComputedStyle(el).

    TESTE 2 — [data-stagger] reduced-motion:
    Chamar page.emulateMedia({ reducedMotion: 'reduce' }) ANTES de page.goto('./').
    Localizar o primeiro elemento com seletor '[data-stagger]' via page.evaluate.
    Verificar via getComputedStyle que animationName === "none".
    Nota: getComputedStyle(el).animationName (não .animation que inclui timing).

    TESTE 3 — .hero-stagger-item ausente com reduced-motion:
    Chamar page.emulateMedia({ reducedMotion: 'reduce' }) ANTES de page.goto('./').
    Verificar que document.querySelector('.hero-stagger-item') retorna null — o HeroMotionSingle não adiciona a classe quando prefersReduced é true.
    Usar expect(count).toBe(0) onde count = await page.locator('.hero-stagger-item').count().

    TESTE 4 — motion.span SettingsToggle com reduced-motion:
    Chamar page.emulateMedia({ reducedMotion: 'reduce' }) ANTES de page.goto('./').
    Localizar span[data-testid="motion-label"].
    Verificar que animationName === "none" (MotionConfig reducedMotion="user" suprime animação CSS gerada pelo Motion).
    Alternativa se o componente não for hidratado no preview: usar test.skip com mensagem "SettingsToggle requires client:load hydration".

    Restrições de implementação:
    - page.emulateMedia SEMPRE antes de page.goto — a ordem importa
    - Usar page.evaluate(() => { const el = document.querySelector('[data-reveal]'); return el ? window.getComputedStyle(el).opacity : null }) para acessar computed styles
    - Para verificar transitionProperty nulo/none: aceitar "all" como fail e "" ou "none" como pass NÃO — verificar que opacity:1 (estado final imediato) é o comportamento observável
    - NÃO usar fenced code blocks na action — a implementação fica no executor
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/motion-accessibility.spec.ts --project=chromium --grep "QUAL-02" 2>&1 | tail -20</automated>
  </verify>
  <done>Os 4 testes do describe "QUAL-02: reduced-motion compliance" passam no Chromium. O arquivo motion-accessibility.spec.ts tem o novo describe block ao final sem alterar nenhum teste existente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Playwright → preview server | Testes E2E consomem a build estática via astro preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | page.evaluate expõe window/document | accept | Ambiente local controlado, sem PII em jogo |
| T-05-03 | Denial of Service | webServer timeout no preview | mitigate | playwright.config.ts já tem timeout: 60000 para o webServer |
</threat_model>

<verification>
Gate QUAL-02 passa quando:
1. npx playwright test tests/e2e/motion-accessibility.spec.ts --project=chromium --grep "QUAL-02" reporta 4 testes passando
2. Nenhum teste existente no arquivo foi quebrado
3. Todos os 4 seletores alvo ([data-reveal], [data-stagger], .hero-stagger-item, motion.span) estão cobertos
</verification>

<success_criteria>
- 4 testes novos no describe "QUAL-02: reduced-motion compliance" passando
- Arquivo motion-accessibility.spec.ts preserva todos os testes anteriores
- Gate QUAL-02 documentado como PASS
</success_criteria>

<output>
Criar `.planning/phases/05-quality-audit/05-02-SUMMARY.md` quando concluído
</output>
