---
phase: 05-quality-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - lighthouse-report.json
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "lighthouse-report.json contém audits['cumulative-layout-shift'].numericValue <= 0.1"
    - "O relatório foi gerado contra http://localhost:4321/deep-dive-vm/ com a build local (não produção)"
    - "O gate QUAL-03 está documentado com o valor numérico de CLS e veredicto PASS/FAIL"
  artifacts:
    - path: "lighthouse-report.json"
      provides: "Relatório Lighthouse com campo CLS numericValue"
      contains: "cumulative-layout-shift"
    - path: ".planning/phases/05-quality-audit/QUAL-03-audit.md"
      provides: "Resultado documentado do gate CLS"
      contains: "QUAL-03 PASS"
  key_links:
    - from: "lighthouse-report.json"
      to: "audits.cumulative-layout-shift.numericValue"
      via: "Valor deve ser <= 0.1 conforme D-AUDIT-03"
      pattern: "cumulative-layout-shift"
---

<objective>
Executar o gate QUAL-03: verificar que o Cumulative Layout Shift (CLS) está dentro do orçamento (<=0.1) após todas as animações da fase v1.2 estarem presentes no output buildado.

Conforme D-AUDIT-03: usar o script Lighthouse diretamente contra http://localhost:4321/deep-dive-vm/ (NÃO usar npm run lighthouse:ci que aponta para produção). Verificar lighthouse-report.json campo audits['cumulative-layout-shift'].numericValue <= 0.1.

Purpose: Confirmar que as animações CSS (data-reveal, data-stagger, hero-stagger-item) e os componentes Motion (HeroMotion, SettingsToggle) não causam layout shift mensurável — fechando o último gate técnico da fase 05.

Output: lighthouse-report.json atualizado com build local + QUAL-03-audit.md com veredicto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quality-audit/05-01-SUMMARY.md
@.planning/phases/05-quality-audit/05-02-SUMMARY.md

<interfaces>
Do package.json (scripts existentes):
  "build": "astro build"
  "preview": "astro preview"
  "lighthouse": "lighthouse https://mentoria.sertaoseracloud.com/deep-dive-vm/ --output json --output-path lighthouse-report.json --chrome-flags=\"--headless --no-sandbox\""
  "lighthouse:ci": "npm run build && npx astro preview & sleep 3 && npm run lighthouse"

ATENÇÃO: npm run lighthouse aponta para produção (mentoria.sertaoseracloud.com) — NÃO usar.
ATENÇÃO: npm run lighthouse:ci usa o npm run lighthouse que aponta para produção — NÃO usar.

Comando correto conforme D-AUDIT-03:
  npm run build
  npx astro preview &
  sleep 5
  npx lighthouse http://localhost:4321/deep-dive-vm/ --output json --output-path lighthouse-report.json --chrome-flags="--headless --no-sandbox"

Verificação do resultado:
  node -e "const r=require('./lighthouse-report.json'); const cls=r.audits['cumulative-layout-shift'].numericValue; console.log('CLS:', cls, cls <= 0.1 ? 'PASS' : 'FAIL')"

Campo esperado no JSON:
  audits -> cumulative-layout-shift -> numericValue (número float)
  audits -> cumulative-layout-shift -> score (1 = CLS <= 0.1)

Contexto histórico: lighthouse-report.json existente mostra CLS = 0 (score 1) antes das animações v1.2.
Expectativa: gate provavelmente passa sem correções — animações CSS via opacity+transform não causam CLS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Gerar relatório Lighthouse contra build local</name>
  <files>lighthouse-report.json</files>
  <read_first>
    - package.json (confirmar scripts build e preview existentes)
    - lighthouse-report.json (se existir — ler o valor CLS atual antes de sobrescrever)
  </read_first>
  <action>
    Executar a sequência de comandos na ordem exata, conforme D-AUDIT-03:

    Passo 1 — Build do projeto:
    npm run build
    Aguardar conclusão sem erros. Se o build falhar, parar e reportar o erro — não prosseguir com Lighthouse.

    Passo 2 — Iniciar preview server em background:
    npx astro preview &
    Aguardar 5 segundos para o servidor iniciar completamente.

    Passo 3 — Executar Lighthouse contra localhost:
    npx lighthouse http://localhost:4321/deep-dive-vm/ --output json --output-path lighthouse-report.json --chrome-flags="--headless --no-sandbox"
    Este comando sobrescreve lighthouse-report.json com dados da build local.

    Passo 4 — Encerrar o preview server:
    kill %1 ou pkill -f "astro preview"

    URL alvo obrigatória: http://localhost:4321/deep-dive-vm/
    NÃO usar: https://mentoria.sertaoseracloud.com/deep-dive-vm/
    NÃO usar: npm run lighthouse ou npm run lighthouse:ci

    Se o Chromium não estiver disponível no ambiente, tentar:
    npx lighthouse http://localhost:4321/deep-dive-vm/ --output json --output-path lighthouse-report.json --chrome-flags="--headless --no-sandbox --disable-gpu"
  </action>
  <verify>
    <automated>node -e "const r=require('./lighthouse-report.json'); const url=r.finalUrl||r.requestedUrl; console.log('URL:', url); if(!url.includes('localhost')) process.exit(1); console.log('PASS: report is from localhost')" 2>&1</automated>
  </verify>
  <done>lighthouse-report.json existe, contém a chave audits.cumulative-layout-shift, e o campo requestedUrl ou finalUrl contém "localhost:4321" (não a URL de produção)</done>
</task>

<task type="auto">
  <name>Tarefa 2: Verificar CLS e documentar gate QUAL-03</name>
  <files>.planning/phases/05-quality-audit/QUAL-03-audit.md</files>
  <read_first>
    - lighthouse-report.json (ler o valor exato de audits.cumulative-layout-shift.numericValue)
    - .planning/phases/05-quality-audit/QUAL-01-audit.md (referenciar resultados dos gates anteriores)
  </read_first>
  <action>
    Passo 1 — Extrair o valor CLS do JSON:
    Executar: node -e "const r=require('./lighthouse-report.json'); const a=r.audits['cumulative-layout-shift']; console.log(JSON.stringify({numericValue: a.numericValue, score: a.score, displayValue: a.displayValue}))"

    Passo 2 — Avaliar o threshold:
    CLS numericValue <= 0.1 → QUAL-03 PASS
    CLS numericValue > 0.1 → QUAL-03 FAIL → identificar qual elemento causa o shift

    Para identificar causas de CLS em caso de FAIL:
    Verificar lighthouse-report.json em audits['layout-shift-elements'] ou audits['non-composited-animations'].
    As animações v1.2 usam opacity + transform — não causam CLS por definição.
    CLS seria causado por: imagens sem dimensões, fontes com swap, conteúdo inserido acima do viewport.

    Passo 3 — Criar .planning/phases/05-quality-audit/QUAL-03-audit.md com:
    - URL auditada: http://localhost:4321/deep-dive-vm/
    - Data: data da execução
    - CLS numericValue: valor exato do JSON
    - CLS score: valor exato do JSON (1.0 = perfeito)
    - CLS displayValue: string do JSON (ex: "0")
    - Threshold: <= 0.1
    - Veredicto: QUAL-03 PASS ou QUAL-03 FAIL
    - Resumo dos 3 gates: QUAL-01, QUAL-02, QUAL-03 — todos PASS = fase 05 fechada

    Se QUAL-03 FAIL: aplicar correção antes de documentar o resultado final.
    Causas prováveis de CLS > 0.1 com animações v1.2:
    - Elemento com opacity:0 inicial causa shift quando torna-se opacity:1 E tem margin/padding que expande a área de layout — verificar se [data-reveal] tem qualquer propriedade não-compositor
    - Fix: garantir que elementos com data-reveal ocupam o mesmo espaço com opacity:0 (não alteram height ou width ao revelar)
  </action>
  <verify>
    <automated>node -e "const r=require('./lighthouse-report.json'); const cls=r.audits['cumulative-layout-shift'].numericValue; const pass=cls<=0.1; console.log('CLS:', cls, pass?'PASS':'FAIL'); process.exit(pass?0:1)" 2>&1</automated>
  </verify>
  <done>QUAL-03-audit.md existe com o valor numérico exato de CLS e veredicto "QUAL-03 PASS". O comando node retorna "PASS" e exit code 0. Os 3 gates (QUAL-01, QUAL-02, QUAL-03) estão todos documentados como PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Lighthouse CLI → preview server localhost | Lighthouse acessa a build local via HTTP |
| node -e → sistema de arquivos | Lê lighthouse-report.json para validação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | URL do Lighthouse | mitigate | Verificar requestedUrl/finalUrl no JSON contém "localhost" antes de aceitar resultado |
| T-05-05 | Denial of Service | Preview server não inicia | mitigate | sleep 5 após iniciar background process; verificar com curl http://localhost:4321/deep-dive-vm/ antes do Lighthouse |
</threat_model>

<verification>
Gate QUAL-03 passa quando:
1. lighthouse-report.json gerado com requestedUrl contendo "localhost:4321"
2. node -e extrai CLS numericValue <= 0.1 e imprime PASS
3. QUAL-03-audit.md existe com veredicto "QUAL-03 PASS"
4. QUAL-03-audit.md referencia os 3 gates completos como PASS — fase 05 fechada
</verification>

<success_criteria>
- CLS numericValue <= 0.1 na build local com animações v1.2 presentes
- QUAL-03-audit.md documenta o valor exato e o veredicto
- Os 3 gates (QUAL-01, QUAL-02, QUAL-03) estão todos PASS — fase 05 pode ser encerrada
</success_criteria>

<output>
Criar `.planning/phases/05-quality-audit/05-03-SUMMARY.md` quando concluído
</output>
