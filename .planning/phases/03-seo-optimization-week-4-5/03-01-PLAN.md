---
phase: 03-seo-optimization-week-4-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .lighthouserc.json
  - package.json
  - astro.config.mjs
autonomous: true
requirements:
  - seo-performance-gate
  - seo-sitemap

must_haves:
  truths:
    - "CI now blocks (error) on Performance < 80 and Accessibility < 90"
    - "@astrojs/sitemap is installed and available to the build"
    - "astro.config.mjs declares the sitemap() integration"
    - ".lighthouserc.json has four assertion entries, two promoted/added vs. current"
  artifacts:
    - path: ".lighthouserc.json"
      provides: "Lighthouse CI gate configuration"
      contains: "categories:performance"
    - path: "package.json"
      provides: "Installed dependency list"
      contains: "@astrojs/sitemap"
    - path: "astro.config.mjs"
      provides: "Astro build configuration with sitemap integration"
      contains: "sitemap()"
  key_links:
    - from: "astro.config.mjs"
      to: "@astrojs/sitemap"
      via: "integrations: [sitemap()]"
      pattern: "sitemap\\(\\)"
    - from: ".lighthouserc.json"
      to: "LHCI runner"
      via: "categories:performance [error, minScore: 0.8]"
      pattern: "categories:performance"
---

<objective>
Install @astrojs/sitemap and harden Lighthouse CI gates. This is the Wave 1 prerequisite that unblocks TDD RED tests in Wave 2 (test 13 needs the package installed) and fulfils decisions D-01, D-02, and D-07.

Purpose: Gate CI on performance (new) and promote accessibility from warn to error; install sitemap integration so `npm run build` emits `dist/sitemap-index.xml`.
Output: Updated `.lighthouserc.json`, `package.json` with `@astrojs/sitemap`, patched `astro.config.mjs`.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-seo-optimization-week-4-5/03-CONTEXT.md
@.planning/phases/03-seo-optimization-week-4-5/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Lighthouse CI assertion gates (D-01, D-02)</name>
  <files>.lighthouserc.json</files>
  <read_first>
    - .lighthouserc.json — read current state before editing (3 assertions: seo/error, accessibility/warn, best-practices/warn)
  </read_first>
  <action>
    Replace the contents of `.lighthouserc.json` with the following exact JSON (per D-01 and D-02):

    {
      "ci": {
        "collect": {
          "staticDistDir": "./dist",
          "numberOfRuns": 3
        },
        "assert": {
          "assertions": {
            "categories:seo": ["error", { "minScore": 0.9 }],
            "categories:accessibility": ["error", { "minScore": 0.9 }],
            "categories:best-practices": ["warn", { "minScore": 0.8 }],
            "categories:performance": ["error", { "minScore": 0.8 }]
          }
        },
        "upload": {
          "target": "temporary-public-storage"
        }
      }
    }

    Changes from current:
    - `categories:accessibility`: "warn" -> "error" (D-02 — Phase 2 a11y tests pass, skip-link is implemented)
    - `categories:performance`: new entry at ["error", { "minScore": 0.8 }] (D-01)
    - All other entries: unchanged

    Do NOT touch `numberOfRuns` (stays at 3) or `staticDistDir` (stays at "./dist").
    The `aggregationMethod` is intentionally omitted — the LHCI default is "optimistic" (best of 3 runs), which provides flap protection for the new performance gate.
  </action>
  <verify>
    <automated>node -e "const j = JSON.parse(require('fs').readFileSync('.lighthouserc.json','utf8')); const a = j.ci.assert.assertions; if (a['categories:performance']?.[0] !== 'error') throw new Error('performance gate missing'); if (a['categories:accessibility']?.[0] !== 'error') throw new Error('accessibility not promoted to error'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `.lighthouserc.json` has exactly 4 entries under `ci.assert.assertions`
    - `categories:performance` is present with value `["error", { "minScore": 0.8 }]`
    - `categories:accessibility` is `["error", { "minScore": 0.9 }]` (was "warn")
    - `categories:seo` is `["error", { "minScore": 0.9 }]` (unchanged)
    - `categories:best-practices` is `["warn", { "minScore": 0.8 }]` (unchanged)
    - `numberOfRuns` is still `3`
    - The node verify command above exits 0
  </acceptance_criteria>
  <done>`.lighthouserc.json` contains the four-gate assertion block with performance as error and accessibility promoted to error.</done>
</task>

<task type="auto">
  <name>Task 2: Install @astrojs/sitemap and wire into astro.config.mjs (D-07)</name>
  <files>package.json, astro.config.mjs</files>
  <read_first>
    - astro.config.mjs — read before editing; note existing `site`, `base`, `outDir` values that must be preserved
    - package.json — verify @astrojs/sitemap is not already present before installing
  </read_first>
  <action>
    Step 1 — Install the package:
      npm install @astrojs/sitemap

    This adds `@astrojs/sitemap` to `dependencies` in `package.json`. Version will resolve to 3.x (latest compatible with Astro 6).

    Step 2 — Edit `astro.config.mjs` manually (do NOT use `npx astro add sitemap` — the existing trailing comment `// ... outras configurações` may confuse the auto-patcher per RESEARCH.md open question 2).

    Replace the entire contents of `astro.config.mjs` with:

      import { defineConfig } from 'astro/config';
      import sitemap from '@astrojs/sitemap';

      export default defineConfig({
        site: 'https://sertaoseracloud.github.io',
        base: '/deep-dive-vm',
        outDir: 'dist',
        integrations: [sitemap()],
      });

    Verify the three existing config values are preserved:
    - `site: 'https://sertaoseracloud.github.io'` (required by @astrojs/sitemap — without it, sitemap generation silently skips)
    - `base: '/deep-dive-vm'` (required for GitHub Pages deployment)
    - `outDir: 'dist'` (required by LHCI staticDistDir)

    After editing, run `npm run build` to confirm the config parses and the build succeeds.
    Confirm `dist/sitemap-index.xml` is generated by the build.
  </action>
  <verify>
    <automated>npm run build && node -e "const {existsSync} = require('fs'); if (!existsSync('dist/sitemap-index.xml')) throw new Error('sitemap-index.xml not generated'); console.log('OK: sitemap-index.xml exists');"</automated>
  </verify>
  <acceptance_criteria>
    - `npm install @astrojs/sitemap` exits 0
    - `package.json` contains `"@astrojs/sitemap"` in dependencies
    - `astro.config.mjs` imports `sitemap from '@astrojs/sitemap'` and has `integrations: [sitemap()]`
    - `astro.config.mjs` still has `site: 'https://sertaoseracloud.github.io'`, `base: '/deep-dive-vm'`, `outDir: 'dist'`
    - `npm run build` exits 0 with no errors
    - `dist/sitemap-index.xml` exists after build
    - `dist/sitemap-0.xml` exists after build (secondary output file)
  </acceptance_criteria>
  <done>@astrojs/sitemap is installed, astro.config.mjs declares `integrations: [sitemap()]`, and `npm run build` produces `dist/sitemap-index.xml`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Package downloaded from npmjs.com; untrusted supply chain |
| static config files → LHCI runner | `.lighthouserc.json` is consumed by `@lhci/cli` in CI; incorrect JSON would silently disable gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `.lighthouserc.json` | mitigate | Verify JSON is valid and contains exactly the 4 expected assertion keys via the node one-liner in Task 1 verify step |
| T-03-01-02 | Denial of Service | `@astrojs/sitemap` supply chain | accept | Official Astro integration maintained by the Astro core team; npm provenance attestation available; risk is low for a build-time devDependency |
| T-03-01-03 | Information Disclosure | `dist/sitemap-index.xml` | accept | The sitemap exposes the site URL structure — acceptable for a public landing page with a single route |
| T-03-01-04 | Tampering | `astro.config.mjs` | mitigate | Verify `site`, `base`, `outDir` values are preserved after edit; `npm run build` exits 0 confirms config parses correctly |
</threat_model>

<verification>
After both tasks complete:
- `cat .lighthouserc.json` shows 4 assertion entries
- `grep sitemap astro.config.mjs` returns the import and integrations lines
- `grep @astrojs/sitemap package.json` returns a line
- `ls dist/sitemap-index.xml` exits 0
</verification>

<success_criteria>
- `.lighthouserc.json` blocks CI on Performance < 80 and Accessibility < 90 (both as "error")
- `@astrojs/sitemap` is installed and `astro.config.mjs` uses it
- `npm run build` produces `dist/sitemap-index.xml`
- No regressions in existing build output (`dist/index.html` still present)
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-optimization-week-4-5/03-01-SUMMARY.md`
</output>
