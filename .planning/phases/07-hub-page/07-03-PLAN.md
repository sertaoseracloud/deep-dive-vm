---
phase: 07-hub-page
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - tests/unit/components/Layout.test.ts
  - tests/seo/seo-meta.test.ts
  - tests/e2e/hub.spec.ts
autonomous: true
requirements: [HUB-01, HUB-02, HUB-03, HUB-04]
tags: [vitest, playwright, seo, e2e]

must_haves:
  truths:
    - "Vitest unit suite asserts that `dist/index.html` has `og:image` containing `hub-og.png` and that the LP `dist/deep-dive-vm/index.html` still has `og:image` containing `claudio1` (no cross-contamination)"
    - "Vitest SEO suite asserts `dist/sitemap-0.xml` contains both `https://mentoria.sertaoseracloud.com/` and `https://mentoria.sertaoseracloud.com/deep-dive-vm/`"
    - "Vitest SEO suite asserts `dist/index.html` `og:image` content includes `hub-og.png`"
    - "Playwright `tests/e2e/hub.spec.ts` covers: HTTP 200 on `./`, single visible `<h1>`, 4 social-icon-link anchors with aria-label, 2 course-card elements (one active visible), skip link to #conteudo-principal, no critical axe violations on `./`"
    - "Coverage gate of 95% remains green (data files exporting only arrays achieve 100% on import; .astro files excluded from coverage)"
  artifacts:
    - path: "tests/e2e/hub.spec.ts"
      provides: "End-to-end smoke + a11y for the hub root"
      min_lines: 40
    - path: "tests/unit/components/Layout.test.ts"
      provides: "Updated unit tests including ogImage hub assertion"
      contains: "hub-og.png"
    - path: "tests/seo/seo-meta.test.ts"
      provides: "Updated SEO suite with sitemap and hub og:image tests (14, 15)"
      contains: "sitemap-0.xml"
  key_links:
    - from: "tests/unit/components/Layout.test.ts"
      to: "dist/index.html and dist/deep-dive-vm/index.html"
      via: "readFileSync in beforeAll"
      pattern: "readFileSync.*dist/index\\.html"
    - from: "tests/seo/seo-meta.test.ts"
      to: "dist/sitemap-0.xml"
      via: "existsSync + readFileSync"
      pattern: "sitemap-0\\.xml"
    - from: "tests/e2e/hub.spec.ts"
      to: "Playwright baseURL http://localhost:4321/"
      via: "page.goto(\"./\")"
      pattern: "page\\.goto\\(\"\\./\"\\)"
---

<objective>
Add the verification surface for the hub. This plan extends `tests/unit/components/Layout.test.ts` with hub-side OG assertions, extends `tests/seo/seo-meta.test.ts` with sitemap-content and hub-og.png assertions (new tests 14 and 15), and creates `tests/e2e/hub.spec.ts` covering smoke load, structural assertions for HUB-01..HUB-04, skip-link wiring, responsive viewport, and axe-core a11y smoke check on the root `./`.

Purpose: Lock the hub behavior under CI so future changes that regress the OG image, sitemap inclusion, or accessibility wiring fail fast. All requirements (HUB-01..HUB-04) gain at least one automated check.
Output: Two modified test files plus one new E2E spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hub-page/07-PATTERNS.md
@.planning/phases/07-hub-page/07-RESEARCH.md
@.planning/phases/07-hub-page/07-02-SUMMARY.md
@tests/unit/components/Layout.test.ts
@tests/seo/seo-meta.test.ts
@tests/e2e/homepage.spec.ts
@tests/e2e/accessibility.spec.ts
@playwright.config.ts

<interfaces>
<!-- Existing test scaffolding to reuse — do not redefine: -->

<!-- Vitest unit/Layout: -->
import { describe, it, expect, beforeAll } from "vitest";
import { readFileSync } from "node:fs";
const PROJECT_ROOT = join(__dirname, "../../..");
// beforeAll currently reads dist/deep-dive-vm/index.html into `builtHtml`.

<!-- Vitest SEO: -->
const DIST_DIR = join(__dirname, "../../dist");
function extractMetaContent(h: string, name: string): string | null; // available, lines 39-50
// Tests numbered 1..13 already exist. Add 14 and 15.

<!-- Playwright baseURL: http://localhost:4321/ — page.goto("./") hits the hub. -->

<!-- Skip-link selector: `a.skip-link` with `href="#conteudo-principal"` for hub (vs `#main` for LP). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Layout.test.ts with hub OG image assertion and LP regression guard</name>
  <files>tests/unit/components/Layout.test.ts</files>
  <read_first>
    - tests/unit/components/Layout.test.ts (current 10 tests, lines 1-63)
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "tests/unit/components/Layout.test.ts")
    - dist/index.html (after running `npm run build` — to verify the assertion patterns work against real output)
  </read_first>
  <behavior>
    - After this change, `npm run test:unit` continues to pass
    - A new `it("hub og:image points to hub-og.png (not claudio1)")` reads `dist/index.html` and asserts `og:image` content includes `hub-og.png` and does NOT include `claudio1`
    - A new `it("LP og:image still uses claudio1 (not hub-og.png)")` asserts the existing `builtHtml` (LP) contains `og:image` and does NOT contain `hub-og.png`
    - A new `it("LP does not contain noindex meta")` asserts the LP `builtHtml` does NOT match `/<meta[^>]+name=["']robots["'][^>]+content=["'][^"']*noindex/i`
  </behavior>
  <action>
    Edit `tests/unit/components/Layout.test.ts`. Add a `let hubHtml = ""` declaration alongside the existing `let builtHtml = ""`. Extend the existing `beforeAll` callback so it ALSO reads `dist/index.html` into `hubHtml` (use `readFileSync(join(PROJECT_ROOT, "dist/index.html"), "utf-8")`). Inside the existing `describe("Layout component", ...)`, append three new `it(...)` blocks: (1) `"hub og:image points to hub-og.png (not claudio1)"` — extract og:image content from `hubHtml` using a regex that handles both `property` ordering (mirror lines 78-93 of `tests/seo/seo-meta.test.ts`'s `extractMetaContent` style — inline a local helper or use a simple regex pair), assert the captured value contains `"hub-og.png"` and does NOT contain `"claudio1"`; (2) `"LP og:image still uses claudio1 (not hub-og.png)"` — assert `builtHtml` matches `/og:image/` and does NOT match `/hub-og\.png/`; (3) `"LP does not contain noindex meta"` — assert `builtHtml` does NOT match `/name=["']robots["'][^>]+noindex/i`. Do not modify or remove any existing assertion.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run tests/unit/components/Layout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/unit/components/Layout.test.ts` contains the substring `hub-og.png`
    - Source assertion: `tests/unit/components/Layout.test.ts` contains the substring `let hubHtml`
    - Source assertion: `tests/unit/components/Layout.test.ts` contains `readFileSync(join(PROJECT_ROOT, "dist/index.html"`
    - CLI: `npx vitest run tests/unit/components/Layout.test.ts` exits 0 and reports >= 13 passing tests (10 existing + 3 new)
  </acceptance_criteria>
  <done>Layout.test.ts asserts hub uses hub-og.png and LP retains claudio1 fallback with no noindex regression.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEO tests 14 and 15 for sitemap content and hub OG image</name>
  <files>tests/seo/seo-meta.test.ts</files>
  <read_first>
    - tests/seo/seo-meta.test.ts (current 13 tests; helper `extractMetaContent`; `DIST_DIR` constant)
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "tests/seo/seo-meta.test.ts")
    - dist/sitemap-0.xml (after running `npm run build` — to confirm the expected URLs are present so the assertion matches real output)
    - dist/index.html (to confirm `og:image` content format)
  </read_first>
  <behavior>
    - `npm run test:unit` continues to pass
    - New test 14 reads `dist/sitemap-0.xml` and asserts presence of `https://mentoria.sertaoseracloud.com/` AND `https://mentoria.sertaoseracloud.com/deep-dive-vm/`
    - New test 15 reads `dist/index.html` and asserts `og:image` content contains `hub-og.png`
    - The existing 13 tests continue to pass unchanged
  </behavior>
  <action>
    Edit `tests/seo/seo-meta.test.ts`. Append two new `it(...)` blocks immediately after the existing test 13 (line 165-167), still inside the same `describe(...)`. Test 14, titled `"14. sitemap-0.xml contains root / and /deep-dive-vm/"`: construct `const sitemapPath = join(DIST_DIR, "sitemap-0.xml");` — if `!existsSync(sitemapPath)` then early return (skip pattern matches existing style), else read with `readFileSync(sitemapPath, "utf-8")` and assert `expect(sitemap).toContain("https://mentoria.sertaoseracloud.com/")` and `expect(sitemap).toContain("https://mentoria.sertaoseracloud.com/deep-dive-vm/")`. Test 15, titled `"15. dist/index.html og:image points to hub-og.png"`: construct `const hubIndexPath = join(DIST_DIR, "index.html");` — if `!existsSync(hubIndexPath)` early return, else read it, call `extractMetaContent(hubHtml, "og:image")`, assert the result is truthy and contains `hub-og.png`. Do not introduce new imports — `readFileSync` and `existsSync` are already imported on line 2. Do not rename or renumber existing tests.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run tests/seo/seo-meta.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/seo/seo-meta.test.ts` contains the substring `14. sitemap-0.xml contains`
    - Source assertion: `tests/seo/seo-meta.test.ts` contains the substring `15. dist/index.html og:image`
    - Source assertion: `tests/seo/seo-meta.test.ts` contains `sitemap-0.xml` and `hub-og.png`
    - Source assertion: the file still contains tests `"1. <title>"` through `"13. dist/sitemap-index.xml"` unchanged
    - CLI: `npx vitest run tests/seo/seo-meta.test.ts` exits 0 with at least 15 passing tests
  </acceptance_criteria>
  <done>SEO suite has 15 tests; sitemap and hub OG image are locked by CI.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/e2e/hub.spec.ts covering HUB-01..HUB-04 and a11y</name>
  <files>tests/e2e/hub.spec.ts</files>
  <read_first>
    - tests/e2e/homepage.spec.ts (analog — copy structural patterns for HTTP 200, h1 visibility, responsive viewport, axe smoke)
    - tests/e2e/accessibility.spec.ts (skip-link test pattern — lines 59-79)
    - playwright.config.ts (confirm baseURL is `http://localhost:4321/` so `page.goto("./")` hits hub)
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "tests/e2e/hub.spec.ts" — exact assertions to add)
  </read_first>
  <behavior>
    - `npx playwright test tests/e2e/hub.spec.ts` passes on Chromium, Firefox, WebKit, and Mobile Chromium against `npm run preview`
    - Tests cover: (a) HTTP 200 on `./`, (b) `<h1>` visible, (c) 4 `.social-icon-link` anchors present with aria-label, (d) 2 `.course-card` elements visible, (e) `.course-card.active` is visible and renders a clickable course link, (f) `.course-card.coming-soon` is visible with no clickable link, (g) skip-link `a.skip-link` has `href="#conteudo-principal"` and text `Pular para o conteúdo`, (h) responsive 375x812: no horizontal overflow, (i) axe-core analyse with tags `wcag2a` + `wcag2aa` returns zero `critical` violations
  </behavior>
  <action>
    Create `tests/e2e/hub.spec.ts`. Imports: `import { test, expect } from "@playwright/test"` and `import AxeBuilder from "@axe-core/playwright"` (mirror the homepage spec exactly). Organize tests under three describes: `test.describe("Hub load", ...)` covering HTTP 200 (call `await page.goto("./")` and assert `response?.status()` equals 200), h1 visibility (`await expect(page.locator("h1")).toBeVisible()`), social link count (`await expect(page.locator(".social-icon-link")).toHaveCount(4)` plus a loop asserting each `aria-label` is non-empty), course card count (`await expect(page.locator(".course-card")).toHaveCount(2)`), active course is visible (`await expect(page.locator(".course-card.active")).toBeVisible()`), coming-soon card is visible (`await expect(page.locator(".course-card.coming-soon")).toBeVisible()`) and has no `.course-link` descendant inside it (`await expect(page.locator(".course-card.coming-soon .course-link")).toHaveCount(0)`); `test.describe("Hub accessibility", ...)` covering skip link href (`await expect(page.locator("a.skip-link")).toHaveAttribute("href", "#conteudo-principal")` and `await expect(page.locator("a.skip-link")).toHaveText("Pular para o conteúdo")`), and a "no critical axe violations" test that runs `new AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa"]).analyze()` and filters `results.violations` by `impact === "critical"` and asserts the filtered length is 0; `test.describe("Hub responsive", ...)` setting viewport `{ width: 375, height: 812 }` then navigating to `./`, asserting h1 visible, and asserting `document.documentElement.scrollWidth > window.innerWidth` evaluates to false. Every `page.goto` uses `"./"` (relative — the baseURL handles host). Do not add tests for the LP (those already live in homepage.spec.ts).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/hub.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `tests/e2e/hub.spec.ts` exists
    - Source assertion: file contains `page.goto("./")` (literal — not `./deep-dive-vm/`)
    - Source assertion: file contains `.social-icon-link` and `.course-card` selectors
    - Source assertion: file contains `#conteudo-principal` literal
    - Source assertion: file contains `AxeBuilder` import and `withTags(["wcag2a", "wcag2aa"])`
    - CLI: `npx playwright test tests/e2e/hub.spec.ts --project=chromium` exits 0
    - CLI: `npx playwright test tests/e2e/hub.spec.ts` (all projects) exits 0 — coverage across Chromium, Firefox, WebKit, Mobile Chromium
  </acceptance_criteria>
  <done>hub.spec.ts locks structural and a11y behavior of the hub across all configured Playwright projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → test runner | Tests parse build output (`dist/`) and exercise the previewed site. Failures here are CI signal, not production exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Repudiation | Future regressions in OG/sitemap/a11y | mitigate | Tests 14, 15, and `hub.spec.ts` make the behavior auditable — any silent regression fails CI; assertions are concrete (URL strings, counts, aria attributes) so they cannot be vacuously satisfied |
| T-07-10 | Denial of Service | Long-running E2E suite | accept | The hub spec adds ~10 assertions across 3 describes; runtime stays under existing budget for `npx playwright test` (the homepage spec is the closest analog and runs in <30s per project) |
| T-07-11 | Tampering | npm/pip/cargo package installs | n/a | No package installs in this plan. `@axe-core/playwright` and `@playwright/test` already present in package.json. |
</threat_model>

<verification>
- `npm run build && npm run test:unit` exits 0 (unit + SEO)
- `npx playwright test tests/e2e/hub.spec.ts` exits 0 across all configured projects
- Coverage gate 95% still met (the new test files do not bring in new production code paths)
- All 4 phase requirements have at least one passing automated assertion attributable to them
</verification>

<success_criteria>
- Test counts: Layout unit tests >= 13, SEO tests >= 15, new hub.spec.ts with at least 8 distinct assertions
- Every requirement HUB-01..HUB-04 traceable to a specific test:
  - HUB-01: hub.spec.ts h1 visibility + responsive viewport
  - HUB-02: hub.spec.ts `.course-card` count + active/coming-soon visibility
  - HUB-03: hub.spec.ts `.social-icon-link` count + aria-label loop
  - HUB-04: Layout.test.ts hub og:image + seo-meta.test.ts test 15
- Sitemap test (test 14) confirms the hub is discoverable
</success_criteria>

<output>
Create `.planning/phases/07-hub-page/07-03-SUMMARY.md` when done.
</output>
