---
phase: 07-hub-page
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: [HUB-01, HUB-02, HUB-03, HUB-04]
tags: [astro, hub, linktree, open-graph]

must_haves:
  truths:
    - "Visiting `/` displays the mentor circular photo (claudio2), the h1 `Cláudio Filipe Lima Raposo`, and a one-line bio"
    - "The hub renders exactly 2 course cards (Deep Dive Azure VM active, Deep Dive EC2 coming-soon) sourced from src/data/courses.ts"
    - "The hub renders 4 social icon anchors (Instagram, YouTube, WhatsApp, LinkedIn) sourced from src/data/social-links.ts, each with target=\"_blank\" rel=\"noopener noreferrer\" and aria-label"
    - "The active course card links to /deep-dive-vm/; the coming-soon card is non-clickable (pointer-events: none on the inner link, cursor: default on the card, opacity 0.7)"
    - "The Layout receives ogImage=\"/hub-og.png\" and url=\"https://mentoria.sertaoseracloud.com/\"; the placeholder noindex meta is fully removed"
    - "The hub uses `<main id=\"conteudo-principal\" tabindex=\"-1\">` and a skip-link with href=\"#conteudo-principal\""
  artifacts:
    - path: "src/pages/index.astro"
      provides: "The hub Linktree page"
      contains: "id=\"conteudo-principal\""
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/Layout.astro"
      via: "ogImage prop"
      pattern: "ogImage=\"/hub-og.png\""
    - from: "src/pages/index.astro"
      to: "src/data/courses.ts"
      via: "import { courses }"
      pattern: "from\\s+['\"]\\.\\./data/courses['\"]"
    - from: "src/pages/index.astro"
      to: "src/data/social-links.ts"
      via: "import { socialLinks }"
      pattern: "from\\s+['\"]\\.\\./data/social-links['\"]"
    - from: "src/pages/index.astro"
      to: "src/components/ui/SocialIcon.astro"
      via: "<SocialIcon name={link.icon} ariaLabel={link.ariaLabel} />"
      pattern: "<SocialIcon\\s"
---

<objective>
Rewrite `src/pages/index.astro` from the noindex placeholder into the full Linktree-style hub. The hub renders the mentor's profile (circular photo, h1, bio, 4 social icons), a course section (2 cards using `src/data/courses.ts`), and uses `Layout.astro` with the new `ogImage` and `url` props. The current `<meta slot="head" name="robots" content="noindex" />` line is removed entirely so the hub becomes indexable. All visual rules — colors, spacing, typography, hover states, reduced-motion — follow the design tokens already declared globally in `Layout.astro`.

Purpose: Deliver HUB-01 (mentor identity), HUB-02 (course cards), HUB-03 (social links from configurable data), and HUB-04 (Open Graph) in a single coherent page rewrite that consumes the contracts plan 01 already established.
Output: A complete `src/pages/index.astro` with scoped CSS implementing the hub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hub-page/07-CONTEXT.md
@.planning/phases/07-hub-page/07-UI-SPEC.md
@.planning/phases/07-hub-page/07-PATTERNS.md
@.planning/phases/07-hub-page/07-RESEARCH.md
@.planning/phases/07-hub-page/07-01-SUMMARY.md
@src/pages/index.astro
@src/pages/deep-dive-vm/index.astro
@src/layouts/Layout.astro
@src/components/sections/Mentor.astro

<interfaces>
<!-- Layout.astro Props after plan 01: -->
interface LayoutProps {
  title: string;
  description?: string;
  url?: string;
  offersUrl?: string;
  ogImage?: string;
  noindex?: boolean;
}

<!-- src/data/social-links.ts exports: -->
export type SocialIcon = 'instagram' | 'youtube' | 'whatsapp' | 'linkedin';
export interface SocialLink { name: string; url: string; icon: SocialIcon; ariaLabel: string; }
export const socialLinks: SocialLink[];

<!-- src/data/courses.ts exports: -->
export type CourseStatus = 'active' | 'coming-soon';
export interface Course { title: string; description: string; url: string; status: CourseStatus; }
export const courses: Course[];

<!-- src/components/ui/SocialIcon.astro Props: -->
interface Props { name: 'instagram'|'youtube'|'whatsapp'|'linkedin'; ariaLabel: string; }
<!-- Renders an SVG only; aria-label belongs on the wrapping <a> -->

<!-- Skip-link CSS class `.skip-link` is already declared in Layout.astro `<style is:global>` (lines 131-146). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite src/pages/index.astro as the Linktree hub</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (the current placeholder being replaced)
    - src/pages/deep-dive-vm/index.astro (skip-link pattern, Layout invocation pattern, main element pattern)
    - src/layouts/Layout.astro (confirm Props now include ogImage and noindex; confirm `.skip-link` global style)
    - src/data/social-links.ts (verify exports — created in plan 01)
    - src/data/courses.ts (verify exports — created in plan 01)
    - src/components/ui/SocialIcon.astro (confirm component interface)
    - src/components/sections/Mentor.astro (bio text reference for condensing)
    - .planning/phases/07-hub-page/07-UI-SPEC.md (full visual contract — colors, spacing, typography, interaction states)
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "src/pages/index.astro" — full structural pattern)
  </read_first>
  <behavior>
    - GET / responds with HTTP 200 and renders one `<h1>` element with the text `Cláudio Filipe Lima Raposo`
    - `dist/index.html` contains `og:image` content `https://mentoria.sertaoseracloud.com/hub-og.png`, `og:url` `https://mentoria.sertaoseracloud.com/`, and NO `<meta name="robots" content="noindex">`
    - `dist/index.html` contains exactly 4 anchors with class `social-icon-link`, each carrying `target="_blank"`, `rel="noopener noreferrer"`, and a non-empty `aria-label`
    - `dist/index.html` contains exactly 2 elements with class `course-card`; one has class `active` with a clickable `<a>` to `/deep-dive-vm/`; the other has class `coming-soon` with no clickable link (or with `pointer-events:none` link)
    - `dist/index.html` contains `<main id="conteudo-principal" tabindex="-1">` and `<a class="skip-link" href="#conteudo-principal">Pular para o conteúdo</a>`
    - Heading order is `<h1>` (mentor name) followed by `<h2>` (Cursos) followed by `<h3>` (per card title), no skipped levels
    - In a `prefers-reduced-motion: reduce` viewport, no `transform` or non-instant `transition` is applied to card or icon hover
  </behavior>
  <action>
    Replace the entire content of `src/pages/index.astro`. Frontmatter imports: `Layout` from `../layouts/Layout.astro`, `Image` from `astro:assets`, `claudio2` from `../assets/claudio2.png`, `socialLinks` from `../data/social-links`, `courses` from `../data/courses`, `SocialIcon` from `../components/ui/SocialIcon.astro`. Use Layout with these exact props: `title="Cláudio Rapôso — O Sertão será Cloud"`, `description="Formação técnica de alto impacto em cloud. Azure VM, EC2 e mais. Acesse os cursos e redes do mentor."`, `url="https://mentoria.sertaoseracloud.com/"`, `ogImage="/hub-og.png"`. DO NOT pass `noindex` — leave the Layout default (false). DELETE the existing line `<meta slot="head" name="robots" content="noindex" />` entirely. Inside the Layout slot, render: (1) `<a href="#conteudo-principal" class="skip-link">Pular para o conteúdo</a>`; (2) `<main id="conteudo-principal" tabindex="-1" aria-label="Hub de cursos e redes sociais">` containing a `<div class="hub-container">` with two sections — `<section class="hub-profile" aria-label="Perfil do mentor">` with `<Image src={claudio2} alt="Cláudio Filipe Lima Raposo — mentor" width={128} height={128} class="mentor-photo" loading="eager" />`, `<h1>Cláudio Filipe Lima Raposo</h1>`, `<p class="bio">Systems Architect · 2× MVP Microsoft · Docker Captain</p>`, and a `<div class="social-links" role="list">` mapping `socialLinks` to `<a href={link.url} aria-label={link.ariaLabel} target="_blank" rel="noopener noreferrer" role="listitem" class="social-icon-link"><SocialIcon name={link.icon} ariaLabel={link.ariaLabel} /></a>`; followed by `<hr class="hub-divider" aria-hidden="true" />`; followed by `<section class="hub-courses" aria-label="Cursos disponíveis">` containing `<h2>Cursos</h2>` and a `<div class="course-list">` mapping `courses` to a `<div class={` `course-card ${course.status}` `} data-status={course.status}>` containing `<div class="course-header"><h3 class="course-title">{course.title}</h3><span class={` `badge badge-${course.status}` `}>{course.status === 'active' ? 'ATIVO' : 'EM BREVE'}</span></div>`, `<p class="course-description">{course.description}</p>`, and conditionally — only when `course.status === 'active'` — `<a href={course.url} class="course-link">Acessar curso →</a>`. Add a scoped `<style>` block implementing the UI-SPEC: `.hub-container` max-width 480px, margin 0 auto, padding 48px 32px; `.hub-profile` and `.hub-courses` as flex/column with the spacing tokens in UI-SPEC §Spacing; `.mentor-photo` border-radius 50%, 128px desktop / 96px under 480px, border 2px solid var(--nucleo-eletrico), box-shadow 0 0 16px rgba(0,255,255,0.3); `h1` Chakra Petch 28px 700; `.bio` Space Grotesk 16px 400 color var(--texto-secundario); `.social-links` flex row gap 16px justify-content center; `.social-icon-link` padding 10px (achieves 44px touch target), color var(--texto-terciario), transition `color 0.2s ease, filter 0.2s ease`, on hover color var(--nucleo-eletrico) and filter drop-shadow(0 0 6px rgba(0,255,255,0.6)); `.hub-divider` border-top 1px solid var(--hairline-strong) border-bottom none margin 24px 0; `.hub-courses h2` Space Grotesk 20px 600; `.course-list` flex column gap 24px; `.course-card` background rgba(27,41,60,0.6) border 1px solid var(--hairline) border-radius 4px padding 24px cursor pointer transition `border-color 0.2s ease, background 0.2s ease, transform 0.2s ease`; `.course-card.active:hover` border var(--hairline-strong) background rgba(27,41,60,0.85) transform translateY(-2px); `.course-card.coming-soon` opacity 0.7 cursor default; `.course-card.coming-soon .course-link` pointer-events none (note: no link is rendered for coming-soon per template logic — but include the rule defensively); `.badge` JetBrains Mono 11px uppercase letter-spacing 0.18em padding 2px 8px border-radius 2px; `.badge-active` color var(--sucesso) border 1px solid var(--sucesso); `.badge-coming-soon` color var(--alerta) border 1px solid var(--alerta); `.course-title` Space Grotesk 20px 600 color var(--texto-principal); `.course-description` Space Grotesk 16px color var(--texto-secundario); `.course-link` color var(--nucleo-eletrico) text-decoration none. Add a `@media (prefers-reduced-motion: reduce)` block that sets `transition: none` and clears `transform` on `.social-icon-link`, `.course-card`, `.course-card.active:hover`. Add a `@media (max-width: 480px)` block that reduces `.mentor-photo` to 96px width/height. Do NOT inline the SVG paths here — use the SocialIcon component. Do NOT pass any `slot="head"` content.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');function fail(m){throw new Error(m)}if(!/<h1[^>]*>Cláudio Filipe Lima Raposo<\/h1>/.test(h))fail('h1');const og=h.match(/property=\"og:image\"[^>]+content=\"([^\"]+)\"/)||h.match(/content=\"([^\"]+)\"[^>]+property=\"og:image\"/);if(!og||!/hub-og\.png/.test(og[1]))fail('og:image '+(og&&og[1]));const ou=h.match(/property=\"og:url\"[^>]+content=\"([^\"]+)\"/)||h.match(/content=\"([^\"]+)\"[^>]+property=\"og:url\"/);if(!ou||!/mentoria\.sertaoseracloud\.com\/?\"?$/.test(ou[1]+''))fail('og:url '+(ou&&ou[1]));if(/robots[^>]*noindex/i.test(h))fail('noindex must not be present');const social=(h.match(/class=\"social-icon-link\"/g)||[]).length;if(social!==4)fail('social-icon-link count '+social);const cards=(h.match(/class=\"[^\"]*course-card[^\"]*\"/g)||[]).length;if(cards!==2)fail('course-card count '+cards);if(!h.includes('id=\"conteudo-principal\"'))fail('main id');if(!h.includes('href=\"#conteudo-principal\"'))fail('skip-link href');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/pages/index.astro` does NOT contain the substring `slot="head"`
    - Source assertion: `src/pages/index.astro` does NOT contain the substring `noindex`
    - Source assertion: `src/pages/index.astro` contains `ogImage="/hub-og.png"` and `url="https://mentoria.sertaoseracloud.com/"`
    - Source assertion: `src/pages/index.astro` contains `id="conteudo-principal"` and `href="#conteudo-principal"`
    - Source assertion: `src/pages/index.astro` imports from `../data/social-links`, `../data/courses`, `../components/ui/SocialIcon.astro`, and `../assets/claudio2.png`
    - Source assertion: `src/pages/index.astro` contains the literal bio string `Systems Architect · 2× MVP Microsoft · Docker Captain`
    - Source assertion: `src/pages/index.astro` contains `loading="eager"` on the Image component
    - Source assertion: `src/pages/index.astro` contains a `@media (prefers-reduced-motion: reduce)` block in its scoped style
    - Behavior: `dist/index.html` contains exactly 4 occurrences of `class="social-icon-link"` and exactly 2 elements with class containing `course-card`
    - Behavior: `dist/index.html` `og:image` content ends with `/hub-og.png`; `og:url` equals `https://mentoria.sertaoseracloud.com/`
    - Behavior: `dist/index.html` contains no `<meta name="robots" content="noindex">`
    - Behavior: `dist/deep-dive-vm/index.html` continues to build (LP not regressed) — verified by `npm run build` exit 0
  </acceptance_criteria>
  <done>Hub page renders the full Linktree layout with data-driven cards and social links, OG tags correct, skip link wired to `#conteudo-principal`, and reduced-motion respected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub → external networks | Social anchors open external URLs in a new tab (Instagram, YouTube, WhatsApp, LinkedIn). |
| Static data → emitted HTML | URL strings from `src/data/social-links.ts` and `src/data/courses.ts` are interpolated directly into `href` attributes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Spoofing | Social anchors `target="_blank"` | mitigate | Every external anchor includes `rel="noopener noreferrer"` (enforced in the template and asserted in acceptance criteria via the count of social-icon-link entries) |
| T-07-06 | Tampering | Coming-soon course card | mitigate | The template renders NO `<a>` for `coming-soon` cards; defensive CSS rule `.course-card.coming-soon .course-link { pointer-events: none }` covers the case if a future data entry adds a link |
| T-07-07 | Information Disclosure | Indexability switch | accept | Hub is intentionally indexable per CONTEXT.md D-07 — removal of the noindex meta is the documented intent; verified via dist/index.html assertion |
| T-07-08 | Tampering | npm/pip/cargo package installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
- `npm run build` exits 0
- `dist/index.html` contains the asserted hub structure
- `dist/deep-dive-vm/index.html` is unchanged compared to plan-01 baseline (LP regression check)
- Lighthouse / axe smoke checks deferred to plan 03 (tests)
</verification>

<success_criteria>
- Hub built successfully with all four requirements observable in `dist/index.html`
- LP build unchanged
- Single atomic commit rewriting `src/pages/index.astro`
</success_criteria>

<output>
Create `.planning/phases/07-hub-page/07-02-SUMMARY.md` when done.
</output>
