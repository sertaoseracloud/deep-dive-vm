---
phase: 07-hub-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Layout.astro
  - src/data/social-links.ts
  - src/data/courses.ts
  - src/components/ui/SocialIcon.astro
  - public/hub-og.png
autonomous: true
requirements: [HUB-03, HUB-04]
tags: [astro, ssg, open-graph, svg-icons]

must_haves:
  truths:
    - "Layout.astro accepts an optional ogImage prop and renders the resolved URL in og:image while preserving the claudio1.png fallback for the LP"
    - "Layout.astro accepts an optional noindex prop and forwards it to astro-seo (default false)"
    - "src/data/social-links.ts exports a typed socialLinks array with 4 entries (Instagram, YouTube, WhatsApp, LinkedIn) each containing name, url, icon, ariaLabel"
    - "src/data/courses.ts exports a typed courses array with 2 entries (Deep Dive Azure VM active, Deep Dive EC2 coming-soon)"
    - "SocialIcon.astro renders inline SVG (viewBox 0 0 16 16, width 24, height 24, fill currentColor, aria-hidden true) for the requested icon name"
    - "public/hub-og.png exists as a 1200x630 PNG before any npm run build runs"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "ogImage and noindex props plumbed into astro-seo"
      contains: "ogImage?: string"
    - path: "src/data/social-links.ts"
      provides: "Typed socialLinks export"
      exports: ["SocialIcon", "SocialLink", "socialLinks"]
    - path: "src/data/courses.ts"
      provides: "Typed courses export"
      exports: ["CourseStatus", "Course", "courses"]
    - path: "src/components/ui/SocialIcon.astro"
      provides: "Inline SVG icon component for 4 networks"
      contains: "viewBox=\"0 0 16 16\""
    - path: "public/hub-og.png"
      provides: "Open Graph image placeholder served by GitHub Pages"
      min_lines: 0
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "astro-seo SEO component"
      via: "noindex prop and openGraph.basic.image with conditional ogImageUrl"
      pattern: "noindex=\\{noindex"
    - from: "src/data/social-links.ts"
      to: "SocialIcon.astro consumer in src/pages/index.astro (next plan)"
      via: "icon discriminator field"
      pattern: "icon: 'instagram'"
---

<objective>
Build the foundations required by the hub page without touching `src/pages/index.astro` yet. This plan adds `ogImage?` and `noindex?` props to `Layout.astro` (zero breaking changes for the existing LP), creates the new `src/data/` directory with two typed data files, ships the `SocialIcon.astro` component with inline SVG paths for Instagram/YouTube/WhatsApp/LinkedIn, and creates the `public/hub-og.png` 1200x630 placeholder via `sharp`. These artifacts are pure additions/surgical modifications — the LP continues to build and pass tests unchanged.

Purpose: Decouple the foundational interface contract (Layout props, data shape, icon component, OG image asset) from the page rewrite so both can be verified independently, and so the hub rewrite in plan 02 reads from real exports rather than placeholders.
Output: Modified `Layout.astro`, two new `src/data/*.ts` files, new `SocialIcon.astro`, and `public/hub-og.png`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-hub-page/07-CONTEXT.md
@.planning/phases/07-hub-page/07-RESEARCH.md
@.planning/phases/07-hub-page/07-PATTERNS.md
@.planning/phases/07-hub-page/07-UI-SPEC.md
@src/layouts/Layout.astro
@src/components/ui/Button.astro
@src/components/layout/Footer.astro

<interfaces>
<!-- Existing Layout.astro Props (lines 5-10, BEFORE this plan): -->
interface Props {
  title: string;
  description?: string;
  url?: string;
  offersUrl?: string;
}

<!-- After this plan: -->
interface Props {
  title: string;
  description?: string;
  url?: string;
  offersUrl?: string;
  ogImage?: string;    // path from root, e.g. "/hub-og.png"
  noindex?: boolean;   // default false; forwarded to astro-seo noindex prop
}

<!-- LP at src/pages/deep-dive-vm/index.astro calls Layout without ogImage/noindex — fallback to claudio1.src and noindex=false must be preserved. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ogImage and noindex props to Layout.astro</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>
    - src/layouts/Layout.astro (full file — current Props interface, ogImageUrl computation, and SEO invocation)
    - .planning/phases/07-hub-page/07-RESEARCH.md (section "Adicionando ogImage? string ao Layout.astro")
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "src/layouts/Layout.astro (layout, request-response — surgical modification)")
    - src/pages/deep-dive-vm/index.astro (verify it calls Layout WITHOUT ogImage/noindex so fallback must remain intact)
  </read_first>
  <behavior>
    - When Layout is invoked without ogImage prop, og:image content equals `https://mentoria.sertaoseracloud.com` + claudio1.src (existing LP behavior preserved)
    - When Layout is invoked with ogImage="/hub-og.png", og:image content equals `https://mentoria.sertaoseracloud.com/hub-og.png`
    - When Layout is invoked without noindex prop (or noindex=false), no `<meta name="robots" content="noindex">` is emitted
    - When Layout is invoked with noindex=true, astro-seo emits `<meta name="robots" content="noindex,follow">` (or equivalent)
    - LP build at `dist/deep-dive-vm/index.html` continues to contain `og:image` referencing claudio1 (NOT hub-og.png)
  </behavior>
  <action>
    Modify the Props interface in Layout.astro (lines 5-10) to add two optional fields: `ogImage?: string` (path relative to site root, must start with `/`) and `noindex?: boolean`. Extend the destructuring on line 12 to include both new props. Change the `ogImageUrl` computation on line 15 from the unconditional `${siteOrigin}${claudio1.src}` to a conditional: when `ogImage` is provided, use `${siteOrigin}${ogImage}`; otherwise use `${siteOrigin}${claudio1.src}`. Inside the `<SEO ...>` invocation (lines 23-70), add a `noindex={noindex ?? false}` prop as a sibling of `title`, `description`, `openGraph`, `twitter`, and `extend`. Do not change `openGraph.image.alt`, the twitter block, the extend block, the offersUrl JSON-LD logic, the ambient div, or any global style. The LP must remain bit-for-bit unchanged in its emitted og:image and robots tags.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs');const lp=fs.readFileSync('dist/deep-dive-vm/index.html','utf8');if(!/og:image/.test(lp))throw new Error('LP og:image missing');if(/hub-og\.png/.test(lp))throw new Error('LP must not reference hub-og.png');if(/robots[^>]*noindex/i.test(lp))throw new Error('LP must not contain noindex');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/layouts/Layout.astro` contains the exact substring `ogImage?: string` inside the Props interface
    - Source assertion: `src/layouts/Layout.astro` contains the exact substring `noindex?: boolean` inside the Props interface
    - Source assertion: `src/layouts/Layout.astro` contains the substring `noindex={noindex ?? false}` inside the `<SEO` invocation
    - Source assertion: `src/layouts/Layout.astro` contains the substring `ogImage ?` and the substring `${siteOrigin}${ogImage}` (template literal — escape backticks as needed)
    - Source assertion: `src/layouts/Layout.astro` retains `claudio1.src` as the fallback branch
    - CLI: `npm run build` exits 0
    - Behavior: `dist/deep-dive-vm/index.html` contains `og:image` but does NOT contain `hub-og.png`
    - Behavior: `dist/deep-dive-vm/index.html` does NOT contain `robots` meta with `noindex`
  </acceptance_criteria>
  <done>Layout.astro accepts both new props; LP build emits identical og:image and no noindex meta; new props default safely.</done>
</task>

<task type="auto">
  <name>Task 2: Create typed data files src/data/social-links.ts and src/data/courses.ts</name>
  <files>src/data/social-links.ts, src/data/courses.ts</files>
  <read_first>
    - .planning/phases/07-hub-page/07-CONTEXT.md (D-03 and D-04 — exact URLs and statuses)
    - .planning/phases/07-hub-page/07-RESEARCH.md (section "Padrão para src/data/social-links.ts e src/data/courses.ts")
    - .planning/phases/07-hub-page/07-PATTERNS.md (sections "src/data/social-links.ts" and "src/data/courses.ts")
    - tsconfig.json (verify strict mode / module resolution before exporting)
  </read_first>
  <behavior>
    - Importing `socialLinks` from `src/data/social-links` yields an array of length 4 with icon discriminators `instagram`, `youtube`, `whatsapp`, `linkedin` in that order
    - Importing `courses` from `src/data/courses` yields an array of length 2 with statuses `active` then `coming-soon`
    - TypeScript compiles cleanly (no type errors) when running `npm run astro check` or equivalent
    - The WhatsApp URL contains the literal substring `PLACEHOLDER` (executor must NOT invent a phone number)
  </behavior>
  <action>
    Create `src/data/social-links.ts` exporting three names: `SocialIcon` (string literal union `'instagram' | 'youtube' | 'whatsapp' | 'linkedin'`), `SocialLink` (interface with fields `name: string`, `url: string`, `icon: SocialIcon`, `ariaLabel: string`), and `socialLinks: SocialLink[]` with these exact four entries in order — `{name:'Instagram', url:'https://instagram.com/sertaoseracloud', icon:'instagram', ariaLabel:'Seguir no Instagram'}`, `{name:'YouTube', url:'https://youtube.com/@sertaoseracloud', icon:'youtube', ariaLabel:'Assistir no YouTube'}`, `{name:'WhatsApp', url:'https://wa.me/PLACEHOLDER', icon:'whatsapp', ariaLabel:'Contato via WhatsApp'}`, `{name:'LinkedIn', url:'https://linkedin.com/in/cfraposo/', icon:'linkedin', ariaLabel:'Conectar no LinkedIn'}`. Create `src/data/courses.ts` exporting `CourseStatus` (`'active' | 'coming-soon'`), `Course` (interface with `title: string`, `description: string`, `url: string`, `status: CourseStatus`), and `courses: Course[]` with two entries in order — `{title:'Deep Dive Azure VM', description:'Formação técnica de 54h — Azure VMs, Terraform e Well-Architected Framework.', url:'/deep-dive-vm/', status:'active'}` then `{title:'Deep Dive EC2', description:'Formação técnica focada em AWS EC2 — em preparação.', url:'/deep-dive-ec2/', status:'coming-soon'}`. Do not add helper functions, mappers, or default exports — only the named exports listed.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/data/social-links.ts').catch(()=>import('./src/data/social-links.js')).then(m=>{if(m.socialLinks.length!==4)throw new Error('social count');if(m.socialLinks.map(s=>s.icon).join(',')!=='instagram,youtube,whatsapp,linkedin')throw new Error('order');if(!m.socialLinks[2].url.includes('PLACEHOLDER'))throw new Error('placeholder');});import('./src/data/courses.ts').catch(()=>import('./src/data/courses.js')).then(m=>{if(m.courses.length!==2)throw new Error('courses count');if(m.courses[0].status!=='active'||m.courses[1].status!=='coming-soon')throw new Error('status');});console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/data/social-links.ts` contains `export const socialLinks: SocialLink[]`
    - Source assertion: `src/data/social-links.ts` contains the exact strings `'https://instagram.com/sertaoseracloud'`, `'https://youtube.com/@sertaoseracloud'`, `'https://wa.me/PLACEHOLDER'`, `'https://linkedin.com/in/cfraposo/'`
    - Source assertion: `src/data/courses.ts` contains `export const courses: Course[]` and both `'active'` and `'coming-soon'` literal strings
    - Source assertion: `src/data/courses.ts` contains the exact strings `'/deep-dive-vm/'` and `'/deep-dive-ec2/'`
    - CLI: `npx astro check` exits 0 (or `npm run build` exits 0 if astro check is not configured)
  </acceptance_criteria>
  <done>Both data files exist with exact entries and order, types compile, WhatsApp uses PLACEHOLDER literal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create SocialIcon.astro with inline SVG paths for 4 networks</name>
  <files>src/components/ui/SocialIcon.astro</files>
  <read_first>
    - .planning/phases/07-hub-page/07-RESEARCH.md (section "SVG Paths para Ícones Sociais" — contains the full path d attributes for all 4 icons)
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "src/components/ui/SocialIcon.astro")
    - src/components/ui/Button.astro (Props interface pattern reference)
    - src/components/layout/Footer.astro (inline SVG style reference)
  </read_first>
  <behavior>
    - Calling `<SocialIcon name="instagram" ariaLabel="..." />` renders an `<svg>` with `viewBox="0 0 16 16"`, `width="24"`, `height="24"`, `fill="currentColor"`, `aria-hidden="true"`, `focusable="false"`, containing a `<path d="...">` whose d attribute matches the Instagram path from RESEARCH.md
    - Same for `name="youtube" | "whatsapp" | "linkedin"` — each name maps to its corresponding RESEARCH.md path
    - The component does NOT emit `aria-label` on the SVG element (per Pitfall in RESEARCH.md — aria-label belongs on the wrapping `<a>` in the consumer)
  </behavior>
  <action>
    Create `src/components/ui/SocialIcon.astro` with an Astro frontmatter block defining `interface Props { name: 'instagram' | 'youtube' | 'whatsapp' | 'linkedin'; ariaLabel: string; }`, destructuring `const { name, ariaLabel } = Astro.props;`, and declaring a `paths: Record<Props['name'], string>` constant whose values are the exact path-d strings from RESEARCH.md section "SVG Paths para Ícones Sociais" for each of `instagram`, `youtube`, `whatsapp`, `linkedin`. The template body must be a single `<svg>` element with attributes `viewBox="0 0 16 16"`, `width="24"`, `height="24"`, `fill="currentColor"`, `aria-hidden="true"`, `focusable="false"`, containing exactly one `<path d={paths[name]} />`. Do not declare scoped styles. Do not add a wrapper `<a>`. The `ariaLabel` prop is part of the contract but is not rendered inside this component — the consumer in plan 02 applies it to the surrounding anchor; keep the prop in the interface for type safety.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('src/components/ui/SocialIcon.astro','utf8');for(const k of ['instagram','youtube','whatsapp','linkedin']){if(!f.includes(\"'\"+k+\"'\"))throw new Error('missing '+k);}if(!/viewBox=\"0 0 16 16\"/.test(f))throw new Error('viewBox');if(!/aria-hidden=\"true\"/.test(f))throw new Error('aria-hidden');if(!/fill=\"currentColor\"/.test(f))throw new Error('fill');if(/aria-label=/.test(f))throw new Error('aria-label must not be on svg');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file `src/components/ui/SocialIcon.astro` exists
    - Source assertion: file contains the substring `Record<Props['name'], string>` (or equivalent typed map)
    - Source assertion: file contains the literal SVG path d-strings starting with `M8 0C5.829` (instagram), `M8.051 1.999h` (youtube), `M13.601 2.326` (whatsapp), `M0 1.146C0 .513` (linkedin)
    - Source assertion: file contains `viewBox="0 0 16 16"` and `fill="currentColor"` and `aria-hidden="true"` and `focusable="false"`
    - Source assertion: file does NOT contain `aria-label=` anywhere
    - CLI: `npm run build` exits 0 (component must compile even though no page imports it yet — it will be wired in plan 02)
  </acceptance_criteria>
  <done>SocialIcon.astro renders 4 distinct icon paths per the name prop with the exact SVG attributes from UI-SPEC; aria-label is reserved for consumer.</done>
</task>

<task type="auto">
  <name>Task 4: Generate public/hub-og.png placeholder (1200x630) via sharp</name>
  <files>public/hub-og.png</files>
  <read_first>
    - .planning/phases/07-hub-page/07-RESEARCH.md (section "Criação do public/hub-og.png placeholder")
    - .planning/phases/07-hub-page/07-PATTERNS.md (section "public/hub-og.png")
    - package.json (confirm `sharp` is present in dependencies before invoking it)
  </read_first>
  <behavior>
    - `public/hub-og.png` exists as a valid PNG file
    - Image dimensions are exactly 1200x630 pixels (verifiable via `sharp(...).metadata()` or `file` command)
    - The image is derived from `src/assets/claudio1.png` resized with `fit: 'cover'`, `position: 'top'`
  </behavior>
  <action>
    Write a one-shot Node ESM script (do NOT commit it as a permanent artifact — execute and discard, or place it under a scratch path and delete after run) that imports `sharp`, loads `src/assets/claudio1.png`, calls `.resize(1200, 630, { fit: 'cover', position: 'top' })`, calls `.png()`, and writes to `public/hub-og.png`. Use `import { fileURLToPath } from 'url'` + `dirname(...)` to derive `__dirname`. Run the script with `node <scriptpath>`. After the run, verify the output file exists and has the correct dimensions before deleting the script. Commit ONLY `public/hub-og.png` (binary) — not the script. Do not add `hub-og.png` to .gitignore.
  </action>
  <verify>
    <automated>node -e "const sharp=require('sharp');sharp('public/hub-og.png').metadata().then(m=>{if(m.width!==1200||m.height!==630)throw new Error('size '+m.width+'x'+m.height);if(m.format!=='png')throw new Error('format '+m.format);console.log('OK');});"</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `public/hub-og.png` exists
    - Behavior: `sharp('public/hub-og.png').metadata()` returns `width === 1200`, `height === 630`, `format === 'png'`
    - File assertion: no Node script artifact left in repo root or `scripts/` related to hub-og generation
  </acceptance_criteria>
  <done>public/hub-og.png is a real 1200x630 PNG resized from claudio1; ready to be referenced by ogImage="/hub-og.png" in plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → static asset directory | `public/` is copied verbatim into `dist/` and served by GitHub Pages CDN. Files placed here are exposed publicly. |
| Source TypeScript → emitted HTML | Data file string literals (URLs in `social-links.ts`, `courses.ts`) flow directly into anchor `href` attributes in the next plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | `src/data/social-links.ts` WhatsApp URL | mitigate | Hardcode `https://wa.me/PLACEHOLDER` literal; do NOT invent a phone number; document the placeholder requirement so the developer replaces it before deploy (per CONTEXT.md D-03) |
| T-07-02 | Tampering | `public/hub-og.png` (binary asset) | accept | Static file, regenerated deterministically from claudio1.png via sharp; risk of tampering equals risk to any other asset in `public/` |
| T-07-03 | Information Disclosure | New `noindex` prop on Layout.astro | mitigate | Default `noindex ?? false` keeps existing LP indexable; only callers that explicitly pass `noindex={true}` opt out — verified in task 1 acceptance criterion |
| T-07-04 | Tampering | npm/pip/cargo package installs | n/a | No package installs in this plan (zero new dependencies — sharp and astro-seo already pinned). No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

<verification>
- `npm run build` succeeds
- `dist/deep-dive-vm/index.html` retains pre-plan og:image (claudio1) and no robots noindex meta
- `public/hub-og.png` is a 1200x630 PNG
- `src/components/ui/SocialIcon.astro` and both `src/data/*.ts` files compile under astro check
- No new entries added to `package.json` dependencies / devDependencies
</verification>

<success_criteria>
- All four tasks complete with their automated verify commands passing
- LP build output unchanged in og:image and robots meta
- Foundation artifacts exist and are typed correctly so plan 02 can import them with zero added discovery
- Git history shows surgical Layout.astro diff (Props + ogImageUrl + SEO noindex prop only)
</success_criteria>

<output>
Create `.planning/phases/07-hub-page/07-01-SUMMARY.md` when done.
</output>
