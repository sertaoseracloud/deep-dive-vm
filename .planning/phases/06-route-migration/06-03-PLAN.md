---
phase: 06-route-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - tests/unit/components/Button.test.ts
  - tests/unit/components/Faq.test.ts
  - tests/unit/components/Footer.test.ts
  - tests/unit/components/Hero.test.ts
  - tests/unit/components/Layout.test.ts
  - tests/unit/components/NavBar.test.ts
  - tests/unit/components/Pricing.test.ts
  - tests/unit/components/SectionHead.test.ts
  - tests/unit/components/StickyCta.test.ts
  - tests/unit/components/UrgencyBar.test.ts
  - tests/seo/seo-meta.test.ts
  - playwright.config.ts
  - tests/e2e/homepage.spec.ts
  - tests/e2e/accessibility.spec.ts
  - tests/e2e/journeys.spec.ts
  - tests/e2e/motion-accessibility.spec.ts
  - .lhcirc.json
autonomous: true
requirements:
  - MIGR-04

must_haves:
  truths:
    - "Cada um dos 10 arquivos tests/unit/components/*.test.ts contém a string 'dist/deep-dive-vm/index.html' e não contém 'dist/index.html' em chamadas readFileSync"
    - "tests/seo/seo-meta.test.ts contém 'dist/deep-dive-vm/index.html' na variável DIST_INDEX"
    - "playwright.config.ts contém 'baseURL: \"http://localhost:4321/\"' e 'url: \"http://localhost:4321/\"' no webServer — sem '/deep-dive-vm/' nessas propriedades"
    - "Os 4 arquivos E2E (homepage, accessibility, journeys, motion-accessibility) contêm 'deep-dive-vm' em todas as chamadas page.goto que navegam para a landing page"
    - ".lhcirc.json contém a propriedade ci.collect.url como array com pelo menos um elemento contendo 'deep-dive-vm'"
  artifacts:
    - path: "tests/unit/components/Button.test.ts"
      provides: "Unit test corrigido para novo path de dist"
      contains: "dist/deep-dive-vm/index.html"
    - path: "tests/seo/seo-meta.test.ts"
      provides: "SEO assertions verificando a LP da VM (não o hub)"
      contains: "dist/deep-dive-vm/index.html"
    - path: "playwright.config.ts"
      provides: "Config E2E com baseURL na raiz do servidor"
      contains: "http://localhost:4321/"
    - path: ".lhcirc.json"
      provides: "LHCI auditando apenas /deep-dive-vm/ — hub placeholder excluído do gate de SEO"
      contains: "deep-dive-vm"
  key_links:
    - from: "playwright.config.ts baseURL=http://localhost:4321/"
      to: "tests/e2e/*.spec.ts page.goto('./deep-dive-vm/')"
      via: "Playwright resolve baseURL + path relativo"
      pattern: "deep-dive-vm"
    - from: ".lhcirc.json url ou autodiscoverUrlBlocklist"
      to: "gate categories:seo minScore 0.9"
      via: "LHCI coleta apenas /deep-dive-vm/ — hub com noindex excluído"
      pattern: "deep-dive-vm"
---

<objective>
Atualizar todos os 17 arquivos de teste e configuração para refletir a nova estrutura de dist
e o novo baseURL do servidor de preview. Corrigir o LHCI para não auditar o hub placeholder.

Purpose: Com dist/ reestruturado pelo Plan 02, todos os testes que liam dist/index.html agora
precisam de dist/deep-dive-vm/index.html. O Playwright precisa de baseURL na raiz. O LHCI
precisa excluir o hub placeholder (noindex) do gate de SEO 90%.

Output: Suite de testes completa verde; LHCI auditando apenas a landing page.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-route-migration/06-CONTEXT.md
@.planning/phases/06-route-migration/06-RESEARCH.md
@.planning/phases/06-route-migration/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar 10 component tests + seo-meta.test.ts para dist/deep-dive-vm/ (per D-04, D-05, MIGR-04)</name>
  <files>
    tests/unit/components/Button.test.ts,
    tests/unit/components/Faq.test.ts,
    tests/unit/components/Footer.test.ts,
    tests/unit/components/Hero.test.ts,
    tests/unit/components/Layout.test.ts,
    tests/unit/components/NavBar.test.ts,
    tests/unit/components/Pricing.test.ts,
    tests/unit/components/SectionHead.test.ts,
    tests/unit/components/StickyCta.test.ts,
    tests/unit/components/UrgencyBar.test.ts,
    tests/seo/seo-meta.test.ts
  </files>
  <read_first>
    - tests/unit/components/Button.test.ts — confirmar padrão exato de leitura do dist/ (readFileSync path)
    - tests/seo/seo-meta.test.ts — confirmar variáveis DIST_INDEX e DIST_DIR (linhas 18-19)
    - .planning/phases/06-route-migration/06-RESEARCH.md — seções "Code Examples 7 e 8" (padrão de mudança)
  </read_first>
  <action>
    Para cada um dos 10 arquivos em tests/unit/components/*.test.ts:
    Localizar a linha com readFileSync que referencia "dist/index.html" e alterar para
    "dist/deep-dive-vm/index.html". O restante de cada arquivo permanece inalterado.
    O padrão de mudança é uniforme: uma substituição de string por arquivo.

    Para tests/seo/seo-meta.test.ts:
    Alterar a variável DIST_INDEX de:
    join(__dirname, "../../dist/index.html")
    para:
    join(__dirname, "../../dist/deep-dive-vm/index.html")

    A variável DIST_DIR permanece join(__dirname, "../../dist") — o sitemap ainda está em dist/.
    Nenhuma outra linha de seo-meta.test.ts deve ser alterada.

    Ler cada arquivo antes de editar para confirmar a linha exata a ser alterada.
    Não abstrair o path em uma constante compartilhada — D-04 determina uma mudança direta por arquivo.
    Não alterar imports, describes, its, expects — apenas a linha do readFileSync path.
  </action>
  <verify>
    <automated>node -e "
const {readFileSync, readdirSync} = require('fs');
const {join} = require('path');
const unitFiles = readdirSync('tests/unit/components').filter(f => f.endsWith('.test.ts'));
let errors = [];
unitFiles.forEach(f => {
  const s = readFileSync(join('tests/unit/components', f), 'utf-8');
  if (s.includes('dist/index.html') && !s.includes('dist/deep-dive-vm/index.html'))
    errors.push(f + ': ainda usa dist/index.html');
});
const seo = readFileSync('tests/seo/seo-meta.test.ts', 'utf-8');
if (!seo.includes('dist/deep-dive-vm/index.html')) errors.push('seo-meta.test.ts: DIST_INDEX não atualizado');
if (errors.length) throw new Error(errors.join('\n'));
console.log('Todos os 11 arquivos de test atualizados OK');
"</automated>
  </verify>
  <done>
    Todos os 10 component tests e seo-meta.test.ts referenciam dist/deep-dive-vm/index.html.
    Nenhum arquivo ainda referencia dist/index.html no readFileSync da landing page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar playwright.config.ts + 4 arquivos E2E (per D-06, D-07, MIGR-04)</name>
  <files>
    playwright.config.ts,
    tests/e2e/homepage.spec.ts,
    tests/e2e/accessibility.spec.ts,
    tests/e2e/journeys.spec.ts,
    tests/e2e/motion-accessibility.spec.ts
  </files>
  <read_first>
    - playwright.config.ts — ler o arquivo completo (baseURL e webServer.url atuais)
    - tests/e2e/homepage.spec.ts — ler completo (identificar todos os page.goto)
    - tests/e2e/accessibility.spec.ts — ler completo
    - tests/e2e/journeys.spec.ts — ler completo (incluindo gotos com fragmentos como #investimento)
    - tests/e2e/motion-accessibility.spec.ts — ler completo
    - .planning/phases/06-route-migration/06-RESEARCH.md — seção "Code Examples 6" e "Pitfall 4"
  </read_first>
  <action>
    Em playwright.config.ts:
    Alterar use.baseURL de "http://localhost:4321/deep-dive-vm/" para "http://localhost:4321/"
    Alterar webServer.url de "http://localhost:4321/deep-dive-vm/" para "http://localhost:4321/"
    Nenhuma outra linha de playwright.config.ts deve ser alterada.

    Nos 4 arquivos E2E, para cada page.goto que navega para a landing page da VM:
    - page.goto("./") → page.goto("./deep-dive-vm/")
    - page.goto("./#investimento") → page.goto("./deep-dive-vm/#investimento")
    - page.goto("./#top") → page.goto("./deep-dive-vm/#top")
    - Qualquer outro goto com fragmento de anchor da LP → prefixar com ./deep-dive-vm/

    Ler cada arquivo E2E para mapear TODOS os gotos antes de editar.
    Um goto que navegar para uma URL externa (https://...) não deve ser alterado.
    Um goto que explicitamente navegar para o hub (se existir) não deve receber /deep-dive-vm/.

    Atualizar também os comentários de cada E2E file que mencionam
    "http://localhost:4321/deep-dive-vm/" para "http://localhost:4321/deep-dive-vm/"
    (os testes de LP continuam testando a landing page, mas via baseURL raiz).

    IMPORTANTE: após as mudanças, o servidor de preview precisa estar rodando em 4321/
    para que os testes funcionem. O webServer block no playwright.config.ts garante isso
    automaticamente via npm run preview.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const pc = fs.readFileSync('playwright.config.ts', 'utf-8');
if (pc.includes('localhost:4321/deep-dive-vm/')) throw new Error('playwright.config.ts: ainda tem /deep-dive-vm/ em baseURL ou webServer.url');
if (!pc.includes('localhost:4321/')) throw new Error('playwright.config.ts: baseURL raiz ausente');
const e2eFiles = ['homepage','accessibility','journeys','motion-accessibility'].map(f => 'tests/e2e/' + f + '.spec.ts');
let e2eErrors = [];
e2eFiles.forEach(f => {
  const s = fs.readFileSync(f, 'utf-8');
  const lines = s.split('\n');
  lines.forEach((l, i) => {
    if (l.includes('page.goto') && (l.includes('\"./\"') || l.includes(\"'./'\")) && !l.includes('deep-dive-vm'))
      e2eErrors.push(f + ' linha ' + (i+1) + ': page.goto sem deep-dive-vm: ' + l.trim());
  });
});
if (e2eErrors.length) throw new Error('D-07 não implementado — gotos sem deep-dive-vm encontrados:\n' + e2eErrors.join('\n'));
console.log('playwright.config.ts OK; todos os E2E gotos para LP usam deep-dive-vm');
"</automated>
  </verify>
  <done>
    playwright.config.ts usa baseURL "http://localhost:4321/" e webServer.url "http://localhost:4321/".
    Todos os page.goto que navegam para a LP usam "./deep-dive-vm/" ou "./deep-dive-vm/#fragmento".
  </done>
</task>

<task type="auto">
  <name>Task 3: Corrigir .lhcirc.json para excluir hub do audit SEO + validação final CI (per MIGR-04)</name>
  <files>.lhcirc.json</files>
  <read_first>
    - .lhcirc.json — ler o arquivo completo antes de editar
    - .lighthouserc.json — verificar se também precisa de ajuste (mesmo padrão staticDistDir)
    - .planning/phases/06-route-migration/06-RESEARCH.md — seções "Pitfall 2", "Code Examples 9", "Open Questions 2"
  </read_first>
  <action>
    Em .lhcirc.json, substituir a estratégia de autodiscovery por url explícita:
    No objeto ci.collect, remover staticDistDir e adicionar url como array com um elemento.

    A porta do servidor LHCI com staticDistDir é variável — estratégia mais robusta é manter
    staticDistDir e adicionar url explícita substituindo o autodiscovery. Usar:

    ci.collect.staticDistDir: "./dist" (manter)
    ci.collect.numberOfRuns: 3 (manter)
    Adicionar: ci.collect.url: ["http://localhost/deep-dive-vm/"]

    Com url explícita, o LHCI ignora autodiscovery e audita apenas a URL especificada.
    O hub placeholder (dist/index.html) não será auditado.
    O gate de SEO categories:seo minScore 0.9 se aplica apenas à landing page da VM.

    Manter ci.assert e ci.upload sem alterações.

    Para .lighthouserc.json (verificar): aplicar a mesma mudança de url explícita se
    o arquivo também usa staticDistDir sem url. Caso já tenha url explícita, não alterar.

    Após editar .lhcirc.json, executar a suite completa de validação local:
    npm run build && npm run test:all
    (npx playwright test --project=chromium requer servidor preview — executar separadamente
    ou verificar que o webServer block do playwright.config.ts sobe o servidor automaticamente)
  </action>
  <verify>
    <automated>node -e "
const lhci = JSON.parse(require('fs').readFileSync('.lhcirc.json', 'utf-8'));
const collect = lhci.ci.collect;
if (!collect.url) throw new Error('.lhcirc.json: url explícita ausente');
if (!collect.url.some(u => u.includes('deep-dive-vm'))) throw new Error('.lhcirc.json: url nao aponta para /deep-dive-vm/');
console.log('.lhcirc.json OK — url:', collect.url);
" && npm run build && npm run test:all && npx lhci autorun</automated>
  </verify>
  <done>
    .lhcirc.json possui ci.collect.url apontando para http://localhost/deep-dive-vm/.
    npm run build conclui sem erros.
    npm run test:all (Vitest) passa sem falhas.
    npx lhci autorun passa com score SEO >= 90% para /deep-dive-vm/ (gate MIGR-04 confirmado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LHCI audit → gate de SEO | LHCI coleta HTML estático — sem input de usuário; risco é de auditoria incorreta (hub com noindex penaliza gate) |
| Playwright baseURL → testes E2E | Mudança de baseURL pode fazer testes navegarem para página errada (hub vs LP) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | .lhcirc.json sem url explícita | mitigate | Task 3 verify confirma presença de url com deep-dive-vm antes de finalizar |
| T-06-07 | Denial of Service | gate CI falha por hub com noindex no LHCI | mitigate | url explícita no .lhcirc.json exclui o hub do audit — npx lhci autorun no verify da Task 3 confirma gate SEO passa |
| T-06-08 | Tampering | E2E navegando para hub em vez da LP | mitigate | Task 2 verify usa throw new Error para todo page.goto sem deep-dive-vm — falha com exit code != 0 se D-07 não estiver implementado |
| T-06-SC | Tampering | npm installs | accept | Fase não instala novos pacotes |
</threat_model>

<verification>
Após completar as três tasks, executar a validação final completa:

1. Vitest — todos os 11 arquivos de unit/seo:
   npm run test:all
   Esperado: 0 falhas

2. Playwright chromium — testes E2E:
   npx playwright test --project=chromium
   Esperado: 0 falhas (servidor sobe via webServer block)

3. LHCI — confirmar gate SEO 90% passa para /deep-dive-vm/ apenas:
   npx lhci autorun
   Esperado: exit code 0, score SEO >= 0.9 para http://localhost/deep-dive-vm/

4. Verificação de CNAME no build:
   node -e "const c=require('fs').readFileSync('dist/CNAME','utf-8').trim(); if(c!=='mentoria.sertaoseracloud.com') throw new Error(c); console.log('CNAME OK')"
</verification>

<success_criteria>
- Todos os 10 component tests Vitest leem dist/deep-dive-vm/index.html (MIGR-04)
- seo-meta.test.ts usa dist/deep-dive-vm/index.html como DIST_INDEX (MIGR-04)
- playwright.config.ts: baseURL = "http://localhost:4321/", webServer.url = "http://localhost:4321/" (MIGR-04)
- 4 arquivos E2E: page.goto para LP usa "./deep-dive-vm/" (MIGR-04)
- .lhcirc.json: ci.collect.url aponta exclusivamente para /deep-dive-vm/ (gate SEO protegido)
- npm run test:all passa (exit code 0)
- npx playwright test --project=chromium passa (exit code 0)
- npx lhci autorun passa com SEO >= 90% (exit code 0)
- dist/CNAME contém mentoria.sertaoseracloud.com (MIGR-03 — verificação final)
</success_criteria>

<output>
Criar `.planning/phases/06-route-migration/06-03-SUMMARY.md` quando concluído.
</output>
