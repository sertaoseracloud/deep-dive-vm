---
phase: 06-route-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - astro.config.mjs
  - src/pages/deep-dive-vm/index.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02

must_haves:
  truths:
    - "astro.config.mjs não possui a propriedade base"
    - "src/pages/deep-dive-vm/index.astro existe com todo o conteúdo da landing page + offersUrl explícito"
    - "src/pages/index.astro é um placeholder mínimo com title, description e noindex"
    - "npm run build conclui sem erros após as mudanças"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Configuração Astro sem base — site servido da raiz"
      contains: "site: 'https://mentoria.sertaoseracloud.com'"
    - path: "src/pages/deep-dive-vm/index.astro"
      provides: "Landing page VM em file-based routing nativo"
      contains: "offersUrl=\"https://mentoria.sertaoseracloud.com/deep-dive-vm#investimento\""
    - path: "src/pages/index.astro"
      provides: "Hub placeholder mínimo com noindex"
      contains: "Mentoria Sertão Será Cloud"
  key_links:
    - from: "src/pages/deep-dive-vm/index.astro"
      to: "dist/deep-dive-vm/index.html"
      via: "Astro file-based routing: src/pages/[slug]/index.astro → dist/[slug]/index.html"
      pattern: "deep-dive-vm"
    - from: "src/pages/index.astro"
      to: "dist/index.html"
      via: "Astro src/pages/index.astro → dist/index.html"
      pattern: "noindex"
    - from: "src/pages/index.astro (<meta slot='head' name='robots' content='noindex'>)"
      to: "dist/index.html (meta[name='robots'][content='noindex'] presente no HTML gerado)"
      via: "Astro SSG processa slot 'head' e injeta a meta tag no <head> do HTML estático"
      pattern: "noindex"
---

<objective>
Executar a migração de roteamento central: remover base do config Astro, mover a landing page
da VM para seu path definitivo, e criar o hub placeholder mínimo.

Purpose: Com Layout.astro já corrigido (Plan 01), é seguro remover base e reposicionar as pages.
Após este plan, npm run build gera dist/deep-dive-vm/index.html (LP) e dist/index.html (hub).

Output: astro.config.mjs sem base; src/pages/deep-dive-vm/index.astro com landing page completa;
src/pages/index.astro com hub placeholder mínimo.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-route-migration/06-CONTEXT.md
@.planning/phases/06-route-migration/06-RESEARCH.md
@.planning/phases/06-route-migration/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que o executor precisa respeitar — extraídos do Plan 01 -->
<!-- src/layouts/Layout.astro após Plan 01: -->

interface Props {
  title: string;
  description?: string;
  url?: string;
  offersUrl?: string;  // opcional — JSON-LD de Course renderiza APENAS se passado
}

<!-- favicon: href="/favicon.ico" (public/favicon.ico existe) -->
<!-- JSON-LD de Course: envolvido em {offersUrl && (...)} -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remover base de astro.config.mjs (per D-01, MIGR-01)</name>
  <files>astro.config.mjs</files>
  <read_first>
    - astro.config.mjs — ler o arquivo completo antes de editar
    - .planning/phases/06-route-migration/06-RESEARCH.md — seção "Code Examples 1" (astro.config.mjs após remoção de base)
  </read_first>
  <action>
    Em astro.config.mjs, remover a linha inteira que contém base: '/deep-dive-vm/'.
    O arquivo resultante deve conter apenas: defineConfig com site, outDir e integrations.
    NÃO substituir por base: '/' — a decisão D-01 é remover completamente, não substituir.
    NÃO alterar a propriedade site (deve permanecer 'https://mentoria.sertaoseracloud.com').
    NÃO alterar outDir ou integrations.
    Após a edição, o arquivo deve ter 9 linhas ou menos.
  </action>
  <verify>
    <automated>node -e "
const s = require('fs').readFileSync('astro.config.mjs', 'utf-8');
if (s.includes('base:')) throw new Error('base ainda presente em astro.config.mjs');
if (!s.includes('site:')) throw new Error('site foi removido incorretamente');
if (!s.includes('mentoria.sertaoseracloud.com')) throw new Error('site URL incorreta');
console.log('astro.config.mjs OK');
"</automated>
  </verify>
  <done>astro.config.mjs não contém base:; site permanece 'https://mentoria.sertaoseracloud.com'</done>
</task>

<task type="auto">
  <name>Task 2: Mover landing page para src/pages/deep-dive-vm/index.astro (per D-03, MIGR-02)</name>
  <files>src/pages/deep-dive-vm/index.astro</files>
  <read_first>
    - src/pages/index.astro — ler o arquivo COMPLETO antes de escrever o novo arquivo (é a fonte do conteúdo)
    - src/layouts/Layout.astro — confirmar assinatura de Props após Plan 01 (especialmente offersUrl?)
    - .planning/phases/06-route-migration/06-RESEARCH.md — seção "Code Examples 4" (VM page com offersUrl explícito)
  </read_first>
  <action>
    Criar o diretório src/pages/deep-dive-vm/ (se não existir) e criar o arquivo
    src/pages/deep-dive-vm/index.astro com o seguinte conteúdo:

    1. Todo o conteúdo atual de src/pages/index.astro deve ser copiado fielmente —
       todos os imports de componentes, todas as seções, todos os slots.

    2. Ajustar os caminhos de import do Layout: de "../layouts/Layout.astro" para
       "../../layouts/Layout.astro" (um nível a mais de profundidade).

    3. Ajustar os caminhos de import de todos os outros componentes do mesmo modo:
       "../components/..." vira "../../components/...".

    4. No componente <Layout>, adicionar a prop offersUrl com o valor exato:
       offersUrl="https://mentoria.sertaoseracloud.com/deep-dive-vm#investimento"
       (este valor é a URL canônica do bloco de pricing da landing page).

    5. Manter title, description e url={Astro.url.toString()} inalterados.

    NÃO remover ou simplificar nenhuma seção da landing page.
    NÃO alterar o conteúdo visual — apenas mover e corrigir imports + adicionar offersUrl.
    NÃO apagar src/pages/index.astro nesta task — ele será substituído na próxima task.
  </action>
  <verify>
    <automated>node -e "
const s = require('fs').readFileSync('src/pages/deep-dive-vm/index.astro', 'utf-8');
if (!s.includes('offersUrl=')) throw new Error('offersUrl prop ausente na VM page');
if (!s.includes('deep-dive-vm#investimento')) throw new Error('offersUrl URL incorreta');
if (s.includes('../layouts/Layout.astro') && !s.includes('../../layouts/Layout.astro')) throw new Error('import path de Layout incorreto');
if (!s.includes('Hero') || !s.includes('Pricing') || !s.includes('Faq')) throw new Error('componentes da LP ausentes');
console.log('deep-dive-vm/index.astro OK');
"</automated>
  </verify>
  <done>
    src/pages/deep-dive-vm/index.astro existe com todo o conteúdo da LP, imports corrigidos
    para ../../, e offersUrl="https://mentoria.sertaoseracloud.com/deep-dive-vm#investimento" passado ao Layout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar hub placeholder src/pages/index.astro + build de validação (per D-10, D-11, MIGR-01)</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro — ler o arquivo atual (será substituído pelo placeholder)
    - src/layouts/Layout.astro — confirmar como injetar noindex (slot head ou prop)
    - .planning/phases/06-route-migration/06-RESEARCH.md — seções "Code Examples 5" e "Open Questions 1" (API de noindex do astro-seo)
  </read_first>
  <action>
    Substituir src/pages/index.astro pelo hub placeholder mínimo com as seguintes características:

    Frontmatter Astro (entre --- e ---):
    - Importar Layout de "../layouts/Layout.astro"
    - Sem outros imports (sem componentes da LP)

    Componente Layout recebe:
    - title="Mentoria Sertão Será Cloud"
    - description="Formações técnicas de Azure para engenheiros. Microsoft MVP."
    - SEM offersUrl (não passar a prop — hub não é um curso, JSON-LD de Course não deve renderizar)

    Injeção de noindex:
    Verificar se astro-seo versão ^1.1.0 aceita prop noindex={true}. Se aceitar, usar a prop.
    Caso contrário (API não confirmada — A1 no RESEARCH.md), injetar diretamente no slot head:
    <meta slot="head" name="robots" content="noindex" />

    Conteúdo do body (mínimo para LHCI não travar em page vazia):
    - <main id="main" tabindex="-1"> com <h1>Mentoria Sertão Será Cloud</h1>
    - <p>Em breve: hub de cursos.</p>
    - <a href="/deep-dive-vm/">Deep Dive Azure VM →</a>

    NÃO incluir design definitivo do hub (foto, bio, cards) — isso é escopo da Fase 7.
    NÃO copiar componentes da landing page para o hub placeholder.

    Após criar o arquivo, executar npm run build para validar que:
    - dist/index.html é gerado (hub placeholder)
    - dist/deep-dive-vm/index.html é gerado (landing page)
    - dist/CNAME é gerado (copiado de public/CNAME)
    - O build não termina com código de saída diferente de 0
  </action>
  <verify>
    <automated>npm run build && node -e "
const fs = require('fs');
['dist/index.html','dist/deep-dive-vm/index.html','dist/CNAME'].forEach(f => {
  if (!fs.existsSync(f)) throw new Error('Ausente: ' + f);
});
const hub = fs.readFileSync('dist/index.html', 'utf-8');
if (!hub.includes('noindex')) throw new Error('noindex ausente no hub dist/index.html');
const lp = fs.readFileSync('dist/deep-dive-vm/index.html', 'utf-8');
if (!lp.includes('Deep Dive Azure VM')) throw new Error('LP title ausente em dist/deep-dive-vm/index.html');
const cname = fs.readFileSync('dist/CNAME', 'utf-8').trim();
if (cname !== 'mentoria.sertaoseracloud.com') throw new Error('CNAME incorreto: ' + cname);
console.log('Build OK — todos os artefatos presentes');
"</automated>
  </verify>
  <done>
    src/pages/index.astro é o hub placeholder com title="Mentoria Sertão Será Cloud", description, noindex.
    npm run build passa sem erros.
    dist/index.html contém noindex.
    dist/deep-dive-vm/index.html contém "Deep Dive Azure VM".
    dist/CNAME contém mentoria.sertaoseracloud.com.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/pages/ → dist/ | Build-time SSG — zero input de usuário; ameaças são de supply chain (não aplicável: sem novos pacotes) |
| dist/CNAME → GitHub Pages | Arquivo de texto plano copiado pelo build — conteúdo fixo e conhecido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | astro.config.mjs (remoção de base) | mitigate | Verificação automatizada pós-edição: node -e confirma ausência de base: antes de prosseguir |
| T-06-04 | Information Disclosure | Hub placeholder sem noindex no built HTML | mitigate | Task 3 verify confirma que dist/index.html contém noindex antes de finalizar |
| T-06-05 | Denial of Service | Build quebrado por imports relativos errados | mitigate | npm run build em Task 3 valida toda a cadeia de imports — falha antes do commit |
| T-06-SC | Tampering | npm installs | accept | Fase não instala novos pacotes |
</threat_model>

<verification>
Após completar as três tasks, verificar:

1. node -e "const s=require('fs').readFileSync('astro.config.mjs','utf-8'); if(s.includes('base:')) throw new Error('base presente'); console.log('config OK')"
2. node -e "require('fs').existsSync('src/pages/deep-dive-vm/index.astro') && console.log('VM page existe')"
3. node -e "const s=require('fs').readFileSync('src/pages/index.astro','utf-8'); if(s.includes('Hero') || s.includes('Pricing')) throw new Error('componentes LP no hub'); console.log('hub placeholder OK')"
4. npm run build (verificação de que o build completo funciona com as três mudanças juntas)
5. node -e "['dist/index.html','dist/deep-dive-vm/index.html','dist/CNAME'].forEach(f=>{if(!require('fs').existsSync(f))throw new Error('Ausente: '+f)}); console.log('dist OK')"
</verification>

<success_criteria>
- astro.config.mjs não possui a propriedade base (MIGR-01)
- src/pages/deep-dive-vm/index.astro existe com conteúdo completo da LP e offersUrl explícito (MIGR-02)
- src/pages/index.astro é hub placeholder com title, description e noindex (D-10, D-11)
- npm run build conclui com exit code 0
- dist/deep-dive-vm/index.html existe e contém "Deep Dive Azure VM"
- dist/index.html existe e contém noindex
- dist/CNAME existe e contém mentoria.sertaoseracloud.com
</success_criteria>

<output>
Criar `.planning/phases/06-route-migration/06-02-SUMMARY.md` quando concluído.
</output>
