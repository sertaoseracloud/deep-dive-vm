---
phase: 06-route-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/CNAME
  - src/layouts/Layout.astro
autonomous: true
requirements:
  - MIGR-03
  - MIGR-04

must_haves:
  truths:
    - "public/CNAME existe com o conteúdo exato mentoria.sertaoseracloud.com"
    - "Layout.astro aceita offersUrl como prop opcional — JSON-LD de Course renderiza apenas quando offersUrl é passado"
    - "Layout.astro aponta favicon para /favicon.ico (não /deep-dive-vm/favicon.svg)"
  artifacts:
    - path: "public/CNAME"
      provides: "Custom domain survival após deploy GitHub Pages"
      contains: "mentoria.sertaoseracloud.com"
    - path: "src/layouts/Layout.astro"
      provides: "Layout reutilizável sem paths hardcoded de /deep-dive-vm/"
      exports: []
  key_links:
    - from: "public/CNAME"
      to: "dist/CNAME"
      via: "Astro build copia public/ para dist/ verbatim"
      pattern: "mentoria\\.sertaoseracloud\\.com"
    - from: "src/layouts/Layout.astro"
      to: "offersUrl prop"
      via: "condicional {offersUrl && <script>...JSON-LD...</script>}"
      pattern: "offersUrl\\?"
---

<objective>
Criar o safety net de domínio (CNAME) e remover os dois paths hardcoded em Layout.astro
antes de qualquer mudança de roteamento.

Purpose: D-02 exige CNAME como primeira ação para garantir que o domínio personalizado
sobrevive ao deploy. D-08 e D-09 eliminam as referências /deep-dive-vm/ do Layout antes
de remover o base — caso contrário o Layout ficaria com paths quebrados após a remoção.

Output: public/CNAME criado; Layout.astro com offersUrl prop opcional e favicon corrigido.
</objective>

<execution_context>
@C:\Users\HP\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HP\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-route-migration/06-CONTEXT.md
@.planning/phases/06-route-migration/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar public/CNAME (per D-02, MIGR-03)</name>
  <files>public/CNAME</files>
  <read_first>
    - public/ — verificar se CNAME já existe antes de criar
    - .planning/phases/06-route-migration/06-CONTEXT.md — confirmar valor exato do domínio (D-02)
  </read_first>
  <action>
    Criar o arquivo public/CNAME com uma única linha de conteúdo: mentoria.sertaoseracloud.com
    Sem quebra de linha adicional no final. O arquivo deve conter exatamente esse hostname.
    Arquivos em public/ são copiados verbatim para dist/ pelo Astro build — nenhum passo de CI
    adicional é necessário. Não criar subdiretórios, não usar aspas, não adicionar protocolo (http/https).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('public/CNAME','utf-8').trim(); if(c!=='mentoria.sertaoseracloud.com') throw new Error('CNAME incorreto: '+c); console.log('OK:', c)"</automated>
  </verify>
  <done>public/CNAME existe e contém exatamente mentoria.sertaoseracloud.com (trim OK)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar Layout.astro — offersUrl prop opcional + favicon /favicon.ico (per D-08, D-09, MIGR-04)</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>
    - src/layouts/Layout.astro — ler o arquivo completo antes de editar (interface Props, linha 16 offersUrl, linha 48 favicon)
    - .planning/phases/06-route-migration/06-RESEARCH.md — seções "Code Examples 2, 3" (padrão offersUrl prop, favicon corrigido)
  </read_first>
  <behavior>
    - Layout.astro interface Props inclui offersUrl?: string (opcional)
    - Quando offersUrl é undefined, o bloco inteiro <script type="application/ld+json"> de Course não é renderizado
    - Quando offersUrl é uma string, o JSON-LD renderiza com offers.url igual ao valor recebido
    - O link de favicon aponta para /favicon.ico (não /deep-dive-vm/favicon.svg)
    - A constante offersUrl hardcoded (linha 16) é removida — substituta é a prop desestruturada
  </behavior>
  <action>
    Em src/layouts/Layout.astro, realizar as três mudanças abaixo. Ler o arquivo completo primeiro.

    Mudança 1 — interface Props (linha 5-9):
    Adicionar offersUrl?: string como quarto campo opcional. A interface passa a ser:
    { title: string; description?: string; url?: string; offersUrl?: string }

    Mudança 2 — desestruturação e remoção da constante hardcoded (linha 11-16):
    Na linha da desestruturação, incluir offersUrl na lista: const { title, description, url, offersUrl } = Astro.props;
    Remover a linha que define const offersUrl = ... hardcoded com /deep-dive-vm#investimento.

    Mudança 3 — bloco JSON-LD condicional (linha 86-118 aprox):
    Envolver o bloco <script type="application/ld+json" ...> em um condicional Astro:
    {offersUrl && (<script type="application/ld+json" set:html={JSON.stringify({...})} />)}
    O objeto JSON-LD interno permanece idêntico ao atual, exceto que a prop offers.url usa
    a variável offersUrl (que agora vem da prop, não da constante hardcoded).

    Mudança 4 — favicon (linha 48 aprox, dentro de extend.link):
    Mudar href: "/deep-dive-vm/favicon.svg" para href: "/favicon.ico"
    Mudar type: "image/svg+xml" para type: "image/x-icon"

    NUNCA colocar blocos de código fenced (```) no corpo do arquivo de layout.
    NUNCA alterar os estilos CSS globais abaixo da linha <style is:global>.
    NUNCA alterar as fontes Google Fonts ou o markup do <body>.
    Preservar todos os outros campos do SEO component intactos.
  </action>
  <verify>
    <automated>node -e "
const s = require('fs').readFileSync('src/layouts/Layout.astro', 'utf-8');
if (!s.includes('offersUrl?: string')) throw new Error('prop offersUrl? ausente');
if (s.includes('/deep-dive-vm/favicon.svg')) throw new Error('favicon hardcoded ainda presente');
if (!s.includes('/favicon.ico')) throw new Error('favicon /favicon.ico ausente');
if (s.includes('siteOrigin}/deep-dive-vm#investimento')) throw new Error('offersUrl hardcoded ainda presente');
if (!s.includes('offersUrl &&')) throw new Error('condicional offersUrl && ausente');
console.log('Layout.astro OK');
"</automated>
  </verify>
  <done>
    Layout.astro: interface Props contém offersUrl?: string; favicon aponta para /favicon.ico;
    JSON-LD de Course renderiza apenas quando offersUrl prop é passado; constante hardcoded removida.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/ → dist/ | Conteúdo copiado verbatim — CNAME não é processado ou interpretado |
| Layout.astro → JSON-LD output | Dados do schema são serializados via JSON.stringify — sem input de usuário nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | public/CNAME / GitHub Pages | mitigate | CNAME aponta para domínio sob controle próprio; DNS CNAME record verificado via dig antes do deploy |
| T-06-02 | Information Disclosure | Hub placeholder com noindex | accept | Conteúdo é estático e público por design; noindex mitiga indexação prematura |
| T-06-SC | Tampering | npm/pip installs | accept | Esta fase não instala novos pacotes — Package Legitimacy Gate não aplicável |
</threat_model>

<verification>
Após completar as duas tasks:

1. node -e "require('fs').readFileSync('public/CNAME','utf-8').trim() === 'mentoria.sertaoseracloud.com' && console.log('CNAME OK')"
2. node -e "const s=require('fs').readFileSync('src/layouts/Layout.astro','utf-8'); ['offersUrl?: string','/favicon.ico','offersUrl &&'].forEach(p=>{if(!s.includes(p))throw new Error('Ausente: '+p)}); console.log('Layout OK')"
3. Verificar que public/favicon.ico existe: node -e "require('fs').existsSync('public/favicon.ico') && console.log('favicon.ico existe')"
</verification>

<success_criteria>
- public/CNAME contém mentoria.sertaoseracloud.com (uma linha, sem protocolo)
- Layout.astro interface Props possui offersUrl?: string
- Layout.astro const offersUrl hardcoded (/deep-dive-vm#investimento) foi removida
- Layout.astro bloco JSON-LD de Course está dentro de {offersUrl && (...)}
- Layout.astro favicon aponta para /favicon.ico com type image/x-icon
- Nenhuma referência /deep-dive-vm/ permanece em src/layouts/Layout.astro
</success_criteria>

<output>
Criar `.planning/phases/06-route-migration/06-01-SUMMARY.md` quando concluído.
</output>
