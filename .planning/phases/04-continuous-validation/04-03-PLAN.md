---
phase: 04-continuous-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/lighthouse-weekly.yml
autonomous: false
requirements:
  - D-04
  - D-05
  - D-06
must_haves:
  truths:
    - "A new workflow file `.github/workflows/lighthouse-weekly.yml` exists"
    - "The workflow has a `schedule` trigger with cron `0 0 * * 0` (Sunday midnight UTC)"
    - "The workflow has a `workflow_dispatch` trigger for manual testing"
    - "The weekly job runs `npm run build` then `npx lhci autorun` (build fresh from source per D-05)"
    - "The weekly workflow is separate from `test.yml` and does not gate pushes (D-06)"
    - "The weekly workflow uploads `.lighthouseci/` as an artifact for reference"
  artifacts:
    - path: ".github/workflows/lighthouse-weekly.yml"
      provides: "Sunday midnight UTC scheduled Lighthouse audit"
      contains: "cron: '0 0 * * 0'"
  key_links:
    - from: "GitHub Actions cron scheduler"
      to: ".github/workflows/lighthouse-weekly.yml"
      via: "schedule trigger"
      pattern: "schedule:\\s*\\n\\s*-\\s*cron:\\s*['\"]0 0 \\* \\* 0['\"]"
---

<objective>
Create a new GitHub Actions workflow that runs Lighthouse weekly on a Sunday midnight UTC cron schedule, independent of the push-triggered `test.yml` (per D-04, D-05, D-06).

Purpose: Continuous SEO health monitoring without gating developer pushes.
Output: New file `.github/workflows/lighthouse-weekly.yml` mirroring the existing `lighthouse` job pattern from `test.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-continuous-validation/04-CONTEXT.md
@.planning/phases/04-continuous-validation/04-RESEARCH.md
@.github/workflows/test.yml
@.lighthouserc.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/lighthouse-weekly.yml</name>
  <files>.github/workflows/lighthouse-weekly.yml</files>
  <read_first>
    - .github/workflows/test.yml (current `lighthouse` job at lines 76-96 — the template to mirror)
    - .lighthouserc.json (current LHCI config — must remain compatible with `startServerCommand: "npx astro preview --port 4321"`)
    - .planning/phases/04-continuous-validation/04-RESEARCH.md (Pattern 3 — Weekly Lighthouse Workflow, lines 236-276; Pitfall 6 — port conflict warning)
  </read_first>
  <action>
    Create the new file `.github/workflows/lighthouse-weekly.yml` with:
    - Top-level `name: Weekly Lighthouse Audit` (D-04).
    - `on:` block with TWO triggers: (a) `schedule:` with one entry `- cron: '0 0 * * 0'` (Sunday midnight UTC, D-04); (b) `workflow_dispatch:` (for manual testing, Claude's discretion noted in research Pattern 3).
    - One job named `lighthouse-weekly` on `runs-on: ubuntu-latest`.
    - Job steps in this exact order, mirroring the `lighthouse` job in `test.yml` (D-05 — same pattern: build fresh then autorun):
      1. `uses: actions/checkout@v4`
      2. `uses: actions/setup-node@v4` with `node-version: "22"` and `cache: "npm"`
      3. `run: npm ci`
      4. `run: npm run build`
      5. Step named `Lighthouse CI weekly audit` running `npx lhci autorun`
      6. `uses: actions/upload-artifact@v4` with `if: always()`, `name: lighthouse-weekly-report`, `path: .lighthouseci/`, `retention-days: 30` (mirrors existing pattern in test.yml line 95).
    - Do NOT include any LHCI server start commands or `lhci.db` commit steps in this plan — those are Plan 04 (Wave 2) per the wave separation.
    - Do NOT modify `test.yml` (D-06 — separate workflow).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('.github/workflows/lighthouse-weekly.yml')){console.error('file missing');process.exit(1)}const y=fs.readFileSync('.github/workflows/lighthouse-weekly.yml','utf8');const errs=[];if(!/name:\s*Weekly Lighthouse Audit/.test(y))errs.push('name wrong');if(!/cron:\s*['\"]0 0 \* \* 0['\"]/.test(y))errs.push('cron wrong');if(!/workflow_dispatch:/.test(y))errs.push('missing workflow_dispatch');if(!/actions\/checkout@v4/.test(y))errs.push('missing checkout');if(!/node-version:\s*['\"]?22['\"]?/.test(y))errs.push('node 22 missing');if(!/npm ci/.test(y))errs.push('missing npm ci');if(!/npm run build/.test(y))errs.push('missing npm run build');if(!/npx lhci autorun/.test(y))errs.push('missing lhci autorun');if(!/actions\/upload-artifact@v4/.test(y))errs.push('missing upload-artifact');if(!/retention-days:\s*30/.test(y))errs.push('missing retention-days 30');if(errs.length){console.error(errs.join('\n'));process.exit(1)}else console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file `.github/workflows/lighthouse-weekly.yml` exists.
    - Source assertion: `name: Weekly Lighthouse Audit` present.
    - Source assertion: cron expression is exactly `'0 0 * * 0'` (Sunday midnight UTC).
    - Source assertion: `workflow_dispatch:` trigger present.
    - Source assertion: job uses `actions/checkout@v4`, `actions/setup-node@v4` with node 22 and npm cache, runs `npm ci`, `npm run build`, then `npx lhci autorun`.
    - Source assertion: artifact upload with `path: .lighthouseci/` and `retention-days: 30`.
    - Source assertion: `test.yml` is NOT modified by this task (verify via `git diff --name-only` after edit shows only the new file).
    - Behavior assertion: workflow is valid YAML and parseable by GitHub Actions (verified at next manual dispatch, see Task 2).
  </acceptance_criteria>
  <done>
    `.github/workflows/lighthouse-weekly.yml` created with the schedule, manual dispatch, and the build+autorun+upload-artifact step sequence. `test.yml` is unchanged by this task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manually trigger the weekly workflow via workflow_dispatch to confirm it runs green</name>
  <files>(observation only — no edits)</files>
  <read_first>
    - .github/workflows/lighthouse-weekly.yml (the file produced in Task 1)
    - .planning/phases/04-continuous-validation/04-VALIDATION.md (Manual-Only Verifications — Weekly cron fires on schedule)
  </read_first>
  <what-built>
    Task 1 created `.github/workflows/lighthouse-weekly.yml`. Because cron schedule triggers can only be observed by waiting for Sunday midnight UTC, we use the `workflow_dispatch` trigger (also in the file) to manually validate the workflow runs end-to-end before relying on the cron.
  </what-built>
  <how-to-verify>
    1. Merge this plan to main (or push the file to a branch where Actions can run).
    2. Open `https://github.com/sertaoseracloud/deep-dive-vm/actions/workflows/lighthouse-weekly.yml`.
    3. Click `Run workflow` → select `main` branch → `Run workflow`.
    4. Wait for the run to complete. Confirm:
       - Job `lighthouse-weekly` exit status: success (green check).
       - Step `Lighthouse CI weekly audit` printed Lighthouse scores for 3 runs (numberOfRuns=3 in .lighthouserc.json).
       - Step `Upload a Build Artifact` succeeded; artifact `lighthouse-weekly-report` is downloadable from the run summary page.
    5. Download the artifact and confirm `.lighthouseci/` contains at least one `lhr-*.json` file.
  </how-to-verify>
  <resume-signal>Type "approved" if the manual run is green and the artifact is downloadable. If LHCI assertions fail (SEO/A11y/Perf scores below thresholds), this is a content regression, not a workflow defect — note it but it does not block this plan's sign-off.</resume-signal>
  <acceptance_criteria>
    - Behavior: `workflow_dispatch` run of `lighthouse-weekly.yml` on main completes with exit 0.
    - Behavior: Artifact `lighthouse-weekly-report` produced and downloadable.
    - Source: Downloaded artifact contains `.lighthouseci/lhr-*.json` Lighthouse report files.
  </acceptance_criteria>
  <done>
    Weekly workflow manually triggered, ran green, produced artifact. Cron schedule will fire next Sunday midnight UTC (unverifiable until then — accepted as deferred manual check).
  </done>
</task>

</tasks>

<verification>
- `cat .github/workflows/lighthouse-weekly.yml` shows the cron `'0 0 * * 0'`, workflow_dispatch, build+autorun+upload-artifact.
- `git diff --stat .github/workflows/test.yml` shows zero changes from this plan.
- `workflow_dispatch` run on main exits 0; artifact downloadable.
</verification>

<success_criteria>
- New workflow file exists and matches all regex checks.
- Manual `workflow_dispatch` run completes green.
- `test.yml` is untouched by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-continuous-validation/04-03-SUMMARY.md`.
</output>
