---
phase: 04-continuous-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test.yml
  - README.md
autonomous: false
requirements:
  - D-07
  - D-08
  - D-09
must_haves:
  truths:
    - "An orphan `badges` branch exists on origin with a seed `badges/coverage.json` file"
    - "After a CI run on main, `badges/coverage.json` on the badges branch is a valid shields.io endpoint JSON with schemaVersion, label, message, color"
    - "README.md contains a shields.io endpoint badge img tag pointing at raw.githubusercontent.com/sertaoseracloud/deep-dive-vm/badges/badges/coverage.json"
    - "The badge publish step runs only on push to main (not on PRs)"
    - "The badge commit message contains `[skip ci]` to prevent recursive CI runs"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "Coverage badge publish step in unit-and-integration job"
      contains: "Publish coverage badge"
    - path: "README.md"
      provides: "Coverage badge img link"
      contains: "img.shields.io/endpoint"
    - path: "badges branch / badges/coverage.json"
      provides: "Seed badge JSON readable by shields.io endpoint"
      contains: "schemaVersion"
  key_links:
    - from: ".github/workflows/test.yml badge step"
      to: "badges branch on origin"
      via: "git checkout badges + git push origin badges with [skip ci]"
      pattern: "git push origin badges"
    - from: "README.md badge img"
      to: "raw.githubusercontent.com/sertaoseracloud/deep-dive-vm/badges/badges/coverage.json"
      via: "shields.io endpoint URL"
      pattern: "img\\.shields\\.io/endpoint\\?url=https://raw\\.githubusercontent\\.com/sertaoseracloud/deep-dive-vm/badges/badges/coverage\\.json"
---

<objective>
Publish a live coverage badge from CI to a dedicated `badges` orphan branch and reference it in README.md (per D-07, D-08, D-09).

Purpose: Surface coverage percentage visually in the README without third-party services (Codecov rejected per D-09).
Output: Pre-created `badges` branch on origin, badge publish step in `test.yml`, README badge img tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-continuous-validation/04-CONTEXT.md
@.planning/phases/04-continuous-validation/04-RESEARCH.md
@.github/workflows/test.yml
@README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 (Wave 0): Pre-create the orphan `badges` branch on origin</name>
  <files>(remote-only — no files in working tree)</files>
  <read_first>
    - .planning/phases/04-continuous-validation/04-RESEARCH.md (Pitfall 2 — `badges` Branch Does Not Exist on First CI Run, lines 387-391)
  </read_first>
  <what-built>
    Claude attempts to create the orphan `badges` branch automatically using the following exact sequence:
    1. `git stash push -u -m "pre-badges-branch"` (preserve working tree)
    2. `git checkout --orphan badges`
    3. `git rm -rf .` (clear all files from the orphan branch's index)
    4. `mkdir -p badges`
    5. Write to `badges/coverage.json` the literal content: `{"schemaVersion":1,"label":"coverage","message":"N/A","color":"lightgrey"}`
    6. `git add badges/coverage.json`
    7. `git commit -m "chore: init badges branch [skip ci]"`
    8. `git push origin badges`
    9. `git checkout main`
    10. `git stash pop` (if step 1 created a stash entry)
    If `git push origin badges` returns a permission error, halt and ask user.
  </what-built>
  <how-to-verify>
    Run the following and confirm output:
    1. `git ls-remote origin badges` — must print a SHA followed by `refs/heads/badges` (non-empty output).
    2. `git fetch origin badges && git show origin/badges:badges/coverage.json` — must print `{"schemaVersion":1,"label":"coverage","message":"N/A","color":"lightgrey"}`.
    3. `git branch --show-current` — must print `main` (we are back on main after the operation).
    4. `git status` — must show no unintended deletions or modifications introduced by the orphan-branch sequence.
  </how-to-verify>
  <resume-signal>Type "approved" once `git ls-remote origin badges` returns a SHA and you are back on `main` with a clean working tree. If push fails, describe the error.</resume-signal>
  <acceptance_criteria>
    - CLI: `git ls-remote origin badges` returns one line ending in `refs/heads/badges`.
    - CLI: `git show origin/badges:badges/coverage.json` outputs the seed JSON.
    - CLI: `git branch --show-current` outputs `main`.
    - Behavior: subsequent `git checkout badges` in CI will succeed (no "pathspec did not match" error).
  </acceptance_criteria>
  <done>
    Orphan `badges` branch exists on origin with seed `badges/coverage.json`, working tree on `main` is unchanged from pre-task state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Add coverage badge publish step to test.yml unit-and-integration job</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (current state — confirm the `unit-and-integration` job at lines 10-29 and the `upload-artifact` step at lines 25-29)
    - .planning/phases/04-continuous-validation/04-RESEARCH.md (Pattern 2 — Shields.io Endpoint Badge, lines 184-227; Anti-Patterns lines 344-351)
  </read_first>
  <action>
    In `.github/workflows/test.yml`, modify the `unit-and-integration` job:
    1. Add a top-level `permissions:` block to that job with key `contents: write` (required to push to the `badges` branch per Pitfall 5).
    2. After the existing `actions/upload-artifact@v4` step (currently lines 25-29), append a new step named exactly `Publish coverage badge`.
    3. The new step MUST be gated with `if: github.ref == 'refs/heads/main'` (do NOT publish from PR branches — Anti-Pattern in research).
    4. The step's `run:` block must:
       - Use a `node -e` script that reads `coverage/coverage-summary.json` via `fs.readFileSync`, JSON-parses it, and guards `if (!json.total) { process.stdout.write('N/A'); process.exit(0); }` then prints `json.total.statements.pct.toFixed(1) + '%'` (per Pitfall 3).
       - Compute color: `brightgreen` if pct >= 90, `yellow` if >= 75, `red` if < 75, `lightgrey` if `N/A`.
       - Configure git user as `github-actions[bot]` with email `github-actions[bot]@users.noreply.github.com`.
       - Run `git fetch origin badges` then `git checkout badges`.
       - Write `badges/coverage.json` with exact shields.io endpoint schema: keys `schemaVersion` (number 1), `label` (string "coverage"), `message` (string with computed pct or "N/A"), `color` (string).
       - `git add badges/coverage.json` — stage ONLY this file (never `git add .` per Anti-Pattern).
       - `git diff --staged --quiet || git commit -m "chore: update coverage badge [skip ci]"` — `[skip ci]` is mandatory per Pitfall 4.
       - `git push origin badges`.
    5. Do NOT modify any other job (`e2e-chromium`, `e2e-cross-browser`, `lighthouse`).
    6. Do NOT change the existing coverage threshold step name or behavior; update the step name string `Unit tests (coverage gate >= 80%)` → `Unit tests (coverage gate >= 95%)` to reflect the Plan 01 change.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/test.yml','utf8'); const errs=[]; if(!/permissions:\s*\n\s+contents:\s*write/.test(y))errs.push('missing permissions: contents: write in unit-and-integration'); if(!/Publish coverage badge/.test(y))errs.push('missing Publish coverage badge step'); if(!/if:\s*github\.ref\s*==\s*'refs\/heads\/main'/.test(y))errs.push('badge step not gated on main'); if(!/\[skip ci\]/.test(y))errs.push('missing [skip ci] in commit message'); if(!/git push origin badges/.test(y))errs.push('missing git push origin badges'); if(!/coverage gate >= 95%/.test(y))errs.push('step name not updated to 95%'); if(errs.length){console.error(errs.join('\n'));process.exit(1)} else console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `unit-and-integration` job has `permissions: contents: write`.
    - Source assertion: A step literally named `Publish coverage badge` exists after the upload-artifact step.
    - Source assertion: That step has `if: github.ref == 'refs/heads/main'`.
    - Source assertion: Commit message string contains `[skip ci]`.
    - Source assertion: Step contains `git push origin badges`.
    - Source assertion: Step stages only `badges/coverage.json` (no `git add .` or `git add -A`).
    - Source assertion: Step name on coverage test updated to mention `>= 95%`.
    - CLI: `node -e "..."` verify command prints `OK`.
  </acceptance_criteria>
  <done>
    `test.yml` has a `Publish coverage badge` step gated on main, with permission to write, that produces shields.io-compliant JSON, commits with `[skip ci]`, and pushes to `badges`. No other jobs affected.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add coverage badge img link to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — confirm top-of-file structure; the badge belongs near the top under the H1)
    - .planning/phases/04-continuous-validation/04-RESEARCH.md (Pattern 2 — README badge tag, lines 230-232)
  </read_first>
  <action>
    Insert the badge markdown immediately after the H1 `# Astro Starter Kit: Basics` (line 1) with a blank line above and below. Exact line to insert:
    `![Coverage](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/sertaoseracloud/deep-dive-vm/badges/badges/coverage.json)`
    The repo owner/name `sertaoseracloud/deep-dive-vm` is verified from `git remote -v` output. The branch in the raw URL is `badges` and the path within that branch is `badges/coverage.json`.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('README.md','utf8'); const m=r.match(/!\[Coverage\]\(https:\/\/img\.shields\.io\/endpoint\?url=https:\/\/raw\.githubusercontent\.com\/sertaoseracloud\/deep-dive-vm\/badges\/badges\/coverage\.json\)/); if(!m){console.error('badge markdown missing or wrong URL');process.exit(1)} else console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `README.md` contains the literal string `![Coverage](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/sertaoseracloud/deep-dive-vm/badges/badges/coverage.json)`.
    - Source assertion: The badge appears after the H1 line and before the first paragraph of body content.
    - CLI: verify command prints `OK`.
  </acceptance_criteria>
  <done>
    README.md displays a coverage badge img referencing the shields.io endpoint URL bound to `badges/badges/coverage.json` on the `badges` branch of `sertaoseracloud/deep-dive-vm`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify badge renders in README after first CI run on main</name>
  <files>(observation only — no edits)</files>
  <read_first>
    - .planning/phases/04-continuous-validation/04-VALIDATION.md (Manual-Only Verifications, lines 64-68)
  </read_first>
  <what-built>
    The badge publish CI step (Task 1) has been merged. On the first push to `main` after merge, the step writes `badges/coverage.json` and pushes to the `badges` branch. shields.io reads that file via `raw.githubusercontent.com` and renders the badge in the README.
  </what-built>
  <how-to-verify>
    1. Wait for the first CI run on `main` after merging this plan to complete (check Actions tab).
    2. Verify the `Publish coverage badge` step is green in the `unit-and-integration` job log.
    3. Run `git fetch origin badges && git show origin/badges:badges/coverage.json` — confirm it is no longer the seed `N/A` JSON (or confirm it IS `N/A` if the coverable surface remains 0/0, per A1).
    4. Open `https://github.com/sertaoseracloud/deep-dive-vm` in a browser. The README badge must render as a coloured shields.io image (not a broken-image icon). If coverable surface is 0/0, the badge shows `coverage | N/A` in lightgrey — this is the correct guarded behaviour.
    5. Confirm no recursive CI run was triggered by the badge commit (Actions history shows the badge commit was skipped due to `[skip ci]`).
  </how-to-verify>
  <resume-signal>Type "approved" if the badge renders (any colour, including lightgrey N/A) and no infinite CI loop occurred. Describe any issues otherwise.</resume-signal>
  <acceptance_criteria>
    - Visual: Coverage badge image visible in GitHub-rendered README on `sertaoseracloud/deep-dive-vm`.
    - Behavior: No recursive CI run triggered by the `chore: update coverage badge [skip ci]` commit.
    - Source: `git show origin/badges:badges/coverage.json` is valid shields.io endpoint JSON (schemaVersion=1, has label, message, color).
  </acceptance_criteria>
  <done>
    Badge renders in README on GitHub; no CI loop; badges branch file is well-formed.
  </done>
</task>

</tasks>

<verification>
- `git ls-remote origin badges` returns a SHA (branch exists).
- `.github/workflows/test.yml` passes the regex verify (permissions, step name, gating, `[skip ci]`, push).
- README.md contains the exact shields.io endpoint markdown.
- After first CI run on main, badge renders and no CI loop occurs.
</verification>

<success_criteria>
- `badges` branch exists on origin with seed JSON.
- `test.yml` has `Publish coverage badge` step gated on `refs/heads/main`, with `contents: write`, `[skip ci]` commit, and push to `badges`.
- README.md contains shields.io endpoint badge link.
- Badge visibly renders in GitHub README; no recursive CI runs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-continuous-validation/04-02-SUMMARY.md`.
</output>
