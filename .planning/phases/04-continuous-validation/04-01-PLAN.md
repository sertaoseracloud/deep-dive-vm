---
phase: 04-continuous-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
must_haves:
  truths:
    - "vitest.config.ts contains a thresholds block under coverage with all four metrics at 95"
    - "Running npm run test:unit on a codebase whose coverable surface is below 95 percent exits non-zero"
    - "Running npm run test:unit on the current empty coverable surface (0/0) does NOT exit non-zero (vitest reports Unknown%)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Hard 95% coverage gate via thresholds.statements/branches/functions/lines"
      contains: "thresholds:"
  key_links:
    - from: "vitest.config.ts"
      to: "npm run test:unit"
      via: "--coverage flag drives v8 provider to enforce thresholds block"
      pattern: "thresholds:\\s*\\{[^}]*statements:\\s*95"
---

<objective>
Raise the Vitest coverage gate from advisory (no thresholds present) to a hard 95% gate on all four metrics: statements, branches, functions, lines (per D-01, D-02, D-03).

Purpose: CI must fail when coverage drops below 95% on any metric, enforcing quality as the coverable surface grows.
Output: Updated `vitest.config.ts` with a `thresholds` block under the existing `coverage` key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-continuous-validation/04-CONTEXT.md
@.planning/phases/04-continuous-validation/04-RESEARCH.md
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 95% thresholds block to vitest.config.ts coverage</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (current state — confirm the line `// thresholds removed as they are not needed` exists inside the `coverage` block on or near line 39)
    - .planning/phases/04-continuous-validation/04-RESEARCH.md (Pattern 1 — Vitest Coverage Thresholds, lines 153-178)
  </read_first>
  <action>
    Inside the existing `coverage` object in `vitest.config.ts` (currently defined around lines 30-40), replace the comment line `// thresholds removed as they are not needed` with a `thresholds` object literal having exactly four keys: `statements: 95`, `branches: 95`, `functions: 95`, `lines: 95`. Do NOT modify `provider`, `include`, `exclude`, or `reporter`. Implements D-01 (raise from 80→95 on all four metrics), D-02 (hard gate via vitest built-in exit-non-zero), D-03 (do NOT add utility modules first — the `.astro` exclusion remains).
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('vitest.config.ts','utf8'); const m = c.match(/thresholds:\s*\{[^}]*statements:\s*95[^}]*branches:\s*95[^}]*functions:\s*95[^}]*lines:\s*95[^}]*\}/s); if(!m){console.error('thresholds block missing or values != 95'); process.exit(1)} else {console.log('OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `vitest.config.ts` contains a `thresholds` property inside the `coverage` block.
    - Source assertion: All four keys present: `statements: 95`, `branches: 95`, `functions: 95`, `lines: 95`.
    - Source assertion: The string `// thresholds removed as they are not needed` is REMOVED from the file.
    - Behavior assertion: `npm run test:unit` runs to completion with the current (empty coverable surface) source tree and exits 0 (vitest/v8 reports Unknown% on 0/0 and does not fail the gate — per A1 in research).
    - CLI: `node -e "..."` verify command above prints `OK`.
  </acceptance_criteria>
  <done>
    `vitest.config.ts` contains the `thresholds: { statements: 95, branches: 95, functions: 95, lines: 95 }` block under `coverage`, the placeholder comment is gone, and `npm run test:unit` still passes on the current 0/0 surface.
  </done>
</task>

</tasks>

<verification>
- `git diff vitest.config.ts` shows only the thresholds block insertion (and removal of placeholder comment); no other config keys touched.
- `npm run test:unit` exits 0 on the current source tree (A1 — empty surface does not fail).
- `cat coverage/coverage-summary.json` shows either `{}` or a structure without `total` (badge guard in Plan 02 handles this).
</verification>

<success_criteria>
- vitest.config.ts thresholds block present with 95/95/95/95 (verified by regex grep).
- npm run test:unit exits 0 on current tree.
- No other lines in vitest.config.ts changed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-continuous-validation/04-01-SUMMARY.md`.
</output>
