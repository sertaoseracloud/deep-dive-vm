---
phase: 08-multi-lp-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/ec2-og.png
  - src/pages/deep-dive-ec2/index.astro
autonomous: true
requirements: [SCAFF-01]
tags: [astro, ssg, open-graph, sharp, lp-lite]

must_haves:
  truths:
    - "GET /deep-dive-ec2/ returns HTTP 200 after build"
    - "Page renders <h1>Deep Dive EC2</h1>, badge EM BREVE, paragraph description, and back-link to /"
    - "Page is indexed (no noindex meta), and og:image points to /ec2-og.png"
    - "public/ec2-og.png exists with dimensions 1200x630 PNG"
    - "Sitemap dist/sitemap-0.xml auto-includes https://mentoria.sertaoseracloud.com/deep-dive-ec2/"
  artifacts:
    - path: "public/ec2-og.png"
      provides: "Open Graph placeholder image 1200x630"
      contains: "PNG binary"
    - path: "src/pages/deep-dive-ec2/index.astro"
      provides: "EC2 coming-soon LP-lite page using Layout.astro"
      contains: "Layout import, title/description/url/ogImage props, h1, badge-coming-soon, back-link"
  key_links:
    - from: "src/pages/deep-dive-ec2/index.astro"
      to: "src/layouts/Layout.astro"
      via: 'import Layout from "../../layouts/Layout.astro"'
      pattern: 'import Layout from "\.\./\.\./layouts/Layout\.astro"'
    - from: "src/pages/deep-dive-ec2/index.astro"
      to: "public/ec2-og.png"
      via: 'Layout ogImage="/ec2-og.png" prop'
      pattern: 'ogImage="/ec2-og\.png"'
---

<objective>
Deliver the EC2 coming-soon LP-lite page (SCAFF-01 production artifacts): the Open Graph placeholder `public/ec2-og.png` (generated from `src/assets/claudio1.png` via a one-shot sharp script) and the route file `src/pages/deep-dive-ec2/index.astro` (Layout.astro without NavBar/Footer, h1 + EM BREVE badge + expanded description + back-link to hub).

Purpose: Provide the first concrete usage of the multi-LP pattern — the EC2 page is simultaneously the SCAFF-01 deliverable and the live reference that the SCAFF-02 HOWTO documents.

Output: `public/ec2-og.png` (1200x630 PNG) and `src/pages/deep-dive-ec2/index.astro` rendered as `dist/deep-dive-ec2/index.html` after `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-multi-lp-scaffold/08-CONTEXT.md
@.planning/phases/08-multi-lp-scaffold/08-RESEARCH.md
@.planning/phases/08-multi-lp-scaffold/08-PATTERNS.md
@.planning/phases/08-multi-lp-scaffold/08-UI-SPEC.md
@src/pages/index.astro
@src/layouts/Layout.astro
@src/data/courses.ts

<interfaces>
Layout.astro Props (from src/layouts/Layout.astro lines 5-13) — the page must conform exactly to this contract:

interface Props {
  title: string;
  description?: string;
  url?: string;
  offersUrl?: string;
  ogImage?: string;    // leading slash mandatory (concatenated as ${siteOrigin}${ogImage})
  noindex?: boolean;   // default false — omit to keep page indexed (D-01)
  jsonLd?: Record<string, unknown>;
}

CSS tokens already in Layout.astro :root (consume via var(); do NOT redefine):
--nucleo-eletrico, --texto-principal, --texto-secundario, --texto-terciario, --alerta, --abismo-profundo

courses.ts EC2 entry (src/data/courses.ts lines 17-22) is ALREADY declared; do NOT modify:
{ title: 'Deep Dive EC2', description: '...', url: '/deep-dive-ec2/', status: 'coming-soon' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate public/ec2-og.png placeholder (1200x630)</name>
  <files>public/ec2-og.png</files>
  <read_first>
    - .planning/phases/08-multi-lp-scaffold/08-RESEARCH.md (Padrão 2 — sharp script, lines 265-291; Pitfall 2 — build needs PNG before first run)
    - .planning/phases/08-multi-lp-scaffold/08-PATTERNS.md (public/ec2-og.png Pattern Assignment, lines 122-149)
    - public/hub-og.png (verify it exists — analog file, must already be present as reference)
    - src/assets/claudio1.png (verify it exists — source image for sharp resize)
  </read_first>
  <action>
    Create a temporary ESM script `gerar-ec2-og.mjs` at repo root that imports sharp, loads `src/assets/claudio1.png`, applies `.resize(1200, 630, { fit: 'cover', position: 'top' })`, calls `.png()`, then `.toFile('public/ec2-og.png')`, and prints confirmation. Run it with `node gerar-ec2-og.mjs`. After execution succeeds, verify dimensions with: `node -e "import('sharp').then(s=>s.default('public/ec2-og.png').metadata().then(m=>console.log(m.width,m.height,m.format)))"` — expected output exactly: `1200 630 png`. Then DELETE `gerar-ec2-og.mjs` (must NOT be committed — only `public/ec2-og.png` is tracked). Do NOT install any new packages — sharp ^0.34.5 is already in dependencies. This task must complete BEFORE Task 2 because the Astro build copies `public/` verbatim and a missing PNG will produce a 404 og:image URL in the deployed HTML (Pitfall 2 in RESEARCH.md).
  </action>
  <verify>
    <automated>node -e "const{statSync}=require('fs');const s=statSync('public/ec2-og.png');if(s.size&lt;1000)process.exit(1);console.log('OK',s.size)" &amp;&amp; node -e "import('sharp').then(s=>s.default('public/ec2-og.png').metadata().then(m=>{if(m.width!==1200||m.height!==630||m.format!=='png')process.exit(1);console.log(m.width,m.height,m.format)}))"</automated>
  </verify>
  <acceptance_criteria>
    - File `public/ec2-og.png` exists (statSync succeeds, size > 1000 bytes)
    - `sharp('public/ec2-og.png').metadata()` returns `{ width: 1200, height: 630, format: 'png' }`
    - Script file `gerar-ec2-og.mjs` does NOT exist after task completion (was deleted)
    - `git status` shows `public/ec2-og.png` as new file; shows no `gerar-ec2-og.mjs`
    - No new entry added to `package.json` dependencies or devDependencies
  </acceptance_criteria>
  <done>public/ec2-og.png is a 1200x630 PNG generated from claudio1.png; the helper script has been deleted; sharp metadata confirms dimensions and format.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/pages/deep-dive-ec2/index.astro (LP-lite page)</name>
  <files>src/pages/deep-dive-ec2/index.astro</files>
  <read_first>
    - src/pages/index.astro (hub LP-lite analog — Layout usage without NavBar/Footer, skip-link + main structure, ec2-container pattern)
    - src/layouts/Layout.astro (Props interface, ogImage concatenation rule, global :root tokens, .skip-link CSS already global — do not redefine)
    - src/data/courses.ts (confirms EC2 entry already exists — do NOT modify this file)
    - .planning/phases/08-multi-lp-scaffold/08-UI-SPEC.md (Copywriting Contract, Layout Structure, Interaction Contract for back-link and badge)
    - .planning/phases/08-multi-lp-scaffold/08-PATTERNS.md (Pattern Assignment for src/pages/deep-dive-ec2/index.astro, lines 23-119; CSS token list; badge pattern; container + responsive pattern)
    - .planning/phases/08-multi-lp-scaffold/08-RESEARCH.md (Padrão 1 template, lines 170-263; Pitfall 3 — ogImage leading slash mandatory)
  </read_first>
  <action>
    Create `src/pages/deep-dive-ec2/index.astro` as an LP-lite page following the hub analog. Import Layout from `../../layouts/Layout.astro` (two-level relative path — page is at `src/pages/deep-dive-ec2/index.astro`). Pass the following Layout props exactly per UI-SPEC Copywriting Contract: `title="Deep Dive EC2 — Em breve · O Sertão será Cloud"`, `description="Formação técnica focada em AWS EC2 — domine instâncias, auto scaling e arquitetura na prática. Em breve na plataforma."`, `url="https://mentoria.sertaoseracloud.com/deep-dive-ec2/"`, `ogImage="/ec2-og.png"` (leading slash mandatory per Pitfall 3). Do NOT pass `noindex` and do NOT pass `jsonLd` (D-01: page is indexed). Inside the slot: emit a skip-link `<a href="#conteudo-principal" class="skip-link">Pular para o conteúdo</a>` followed by `<main id="conteudo-principal" tabindex="-1" aria-label="Página do curso Deep Dive EC2">` containing a `<div class="ec2-container">` with, in order: (1) `<a href="/" class="back-link">← Ver todos os cursos</a>`, (2) `<h1>Deep Dive EC2</h1>`, (3) `<span class="badge badge-coming-soon">EM BREVE</span>`, (4) the description paragraph with the same text as the `description` prop. In the page-scoped `<style>` block, define `.ec2-container` (max-width 480px, margin 0 auto, padding 48px 32px, position relative, z-index 1), `.back-link` (inline-flex, gap 4px, color var(--texto-terciario), Space Grotesk 14px weight 400, padding 12px 0, margin-bottom 32px, transition color 0.2s ease; hover color var(--nucleo-eletrico)), `h1` (Chakra Petch 28px weight 700 line-height 1.1 color var(--texto-principal) margin 0 0 16px 0), `.badge` (JetBrains Mono 11px weight 400 uppercase letter-spacing 0.18em padding 2px 8px border-radius 2px white-space nowrap), `.badge-coming-soon` (color var(--alerta), border 1px solid var(--alerta)), `p` (Space Grotesk 16px weight 400 line-height 1.5 color var(--texto-secundario) margin 16px 0 0 0), `@media (max-width: 480px) { .ec2-container { padding: 48px 16px; } }`, and `@media (prefers-reduced-motion: reduce) { .back-link { transition: none; } }`. Do NOT redefine any CSS custom properties — consume `--nucleo-eletrico`, `--texto-principal`, `--texto-secundario`, `--texto-terciario`, `--alerta` via var() only. Do NOT add `astro.config.mjs` sitemap filters (Anti-Pattern in PATTERNS.md). Do NOT modify `src/data/courses.ts`. After writing the file, run `npm run build` and confirm `dist/deep-dive-ec2/index.html` exists and the build exits 0.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const{existsSync,readFileSync}=require('fs');const p='dist/deep-dive-ec2/index.html';if(!existsSync(p))process.exit(1);const h=readFileSync(p,'utf-8');const fail=(m)=>{console.error('FAIL',m);process.exit(1)};if(!h.includes('Deep Dive EC2'))fail('no h1 text');if(!h.includes('EM BREVE'))fail('no badge');if(!h.includes('ec2-og.png'))fail('no og image');if(h.match(/&lt;meta[^&gt;]+noindex/i))fail('noindex present');if(!h.includes('href=\"/\"'))fail('no back-link href /');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/deep-dive-ec2/index.astro` exists
    - File contains literal substring: `import Layout from "../../layouts/Layout.astro"`
    - File contains literal substring: `ogImage="/ec2-og.png"` (with leading slash)
    - File contains literal substring: `title="Deep Dive EC2 — Em breve · O Sertão será Cloud"`
    - File contains literal substring: `url="https://mentoria.sertaoseracloud.com/deep-dive-ec2/"`
    - File does NOT contain `noindex` and does NOT contain `jsonLd`
    - File contains `<a href="/" class="back-link">` and contains `← Ver todos os cursos`
    - File contains `<span class="badge badge-coming-soon">EM BREVE</span>`
    - `npm run build` exits 0
    - `dist/deep-dive-ec2/index.html` exists after build
    - Built HTML contains `ec2-og.png` in an og:image meta tag
    - Built HTML does NOT contain `noindex` (excluding HTML comments): `grep -v '^&lt;!--' dist/deep-dive-ec2/index.html | grep -ic noindex` returns 0
    - Built `dist/sitemap-0.xml` contains `https://mentoria.sertaoseracloud.com/deep-dive-ec2/`
    - `src/data/courses.ts` is NOT modified (git diff shows no changes to this file)
    - `astro.config.mjs` is NOT modified
  </acceptance_criteria>
  <done>EC2 page renders in dev and builds to dist/deep-dive-ec2/index.html with correct og:image, no noindex, back-link to /, and the badge + h1 + description content per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → repo | Sharp script runs locally with filesystem access during build-time only |
| build output → CDN/GitHub Pages | Static HTML and PNG served verbatim — no runtime input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | `gerar-ec2-og.mjs` sharp script | mitigate | Script is one-shot ESM created and deleted within Task 1; accesses only `src/assets/claudio1.png` (read) and `public/ec2-og.png` (write); never committed to git (Task 1 acceptance criteria asserts deletion). |
| T-08-02 | Tampering | `ogImage` prop in `src/pages/deep-dive-ec2/index.astro` | mitigate | `ogImage="/ec2-og.png"` is a string literal hardcoded in the Astro page — no interpolation from query params, cookies, or external data. Layout.astro concatenates only with the fixed `siteOrigin` constant. |
</threat_model>

<verification>
After both tasks: `npm run build` exits 0; `dist/deep-dive-ec2/index.html` and `dist/ec2-og.png` exist; `dist/sitemap-0.xml` contains the EC2 URL; built HTML has correct og:image and no noindex.
</verification>

<success_criteria>
- public/ec2-og.png is a valid 1200x630 PNG and was generated from src/assets/claudio1.png via a script that has been deleted
- src/pages/deep-dive-ec2/index.astro renders with all UI-SPEC copy and components
- `npm run build` produces dist/deep-dive-ec2/index.html with og:image=/ec2-og.png and no noindex
- Sitemap auto-discovery picks up the EC2 route (verified by grep on dist/sitemap-0.xml)
- No new dependencies, no astro.config.mjs changes, no courses.ts modifications
</success_criteria>

<output>
Create `.planning/phases/08-multi-lp-scaffold/08-01-SUMMARY.md` when done.
</output>
