---
phase: 08-multi-lp-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/e2e/ec2-coming-soon.spec.ts
  - tests/seo/seo-meta.test.ts
autonomous: true
requirements: [SCAFF-01]
tags: [playwright, vitest, axe-core, e2e, seo, open-graph]

must_haves:
  truths:
    - "Playwright spec tests/e2e/ec2-coming-soon.spec.ts exists with 3 describe blocks (load, accessibility, responsive) following hub.spec.ts structure"
    - "All EC2 E2E tests pass on chromium (HTTP 200, h1, badge, back-link, skip-link, axe WCAG2A/AA critical=0, mobile 375x812 no overflow)"
    - "tests/seo/seo-meta.test.ts contains test 16 declaring its OWN ec2IndexPath/ec2Html locally (NEVER reusing module-level DIST_INDEX or html)"
    - "Vitest seo-meta.test.ts passes including new test 16 verifying og:image contains ec2-og.png in dist/deep-dive-ec2/index.html"
  artifacts:
    - path: "tests/e2e/ec2-coming-soon.spec.ts"
      provides: "E2E spec covering SCAFF-01 page behavior across browser projects"
      contains: 'test.describe("EC2 coming-soon load"'
    - path: "tests/seo/seo-meta.test.ts"
      provides: "Test 16 — og:image points to ec2-og.png for the EC2 route"
      contains: '16. dist/deep-dive-ec2/index.html og:image points to ec2-og.png'
  key_links:
    - from: "tests/e2e/ec2-coming-soon.spec.ts"
      to: "src/pages/deep-dive-ec2/index.astro"
      via: 'page.goto("./deep-dive-ec2/")'
      pattern: 'page\.goto\("\./deep-dive-ec2/"\)'
    - from: "tests/seo/seo-meta.test.ts test 16"
      to: "dist/deep-dive-ec2/index.html"
      via: 'join(DIST_DIR, "deep-dive-ec2/index.html") declared locally inside it()'
      pattern: 'deep-dive-ec2/index\.html'
---

<objective>
Lock SCAFF-01 verification behind automated tests: create `tests/e2e/ec2-coming-soon.spec.ts` mirroring the 3-describe structure of `tests/e2e/hub.spec.ts` and append test 16 to `tests/seo/seo-meta.test.ts` verifying the EC2 og:image. Both files must enforce the Pitfall 1 contract from RESEARCH.md — test 16 declares its own local path and never reuses module-level `DIST_INDEX` or `html` (which still target deep-dive-vm and would produce a silent false positive).

Purpose: Provide CI gate for the EC2 page — every future build verifies HTTP 200, copy, accessibility, responsive layout, and og:image continuity.

Output: New `tests/e2e/ec2-coming-soon.spec.ts` and modified `tests/seo/seo-meta.test.ts` (append test 16 after existing test 15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-multi-lp-scaffold/08-RESEARCH.md
@.planning/phases/08-multi-lp-scaffold/08-PATTERNS.md
@.planning/phases/08-multi-lp-scaffold/08-VALIDATION.md
@.planning/phases/07-hub-page/07-03-SUMMARY.md
@tests/e2e/hub.spec.ts
@tests/seo/seo-meta.test.ts
@playwright.config.ts

<interfaces>
Playwright baseURL (from playwright.config.ts) is `http://localhost:4321/` — all `page.goto()` calls in this repo use RELATIVE URLs like `"./deep-dive-ec2/"` (not absolute). The `webServer` block auto-starts the preview server.

Vitest test file `tests/seo/seo-meta.test.ts` already declares at module scope:
  - `import { readFileSync, existsSync } from "node:fs"`
  - `import { dirname, join } from "node:path"`
  - `const DIST_DIR = join(__dirname, "../../dist")`
  - `const DIST_INDEX = join(__dirname, "../../dist/deep-dive-vm/index.html")` — points to deep-dive-vm, MUST NOT be reused for EC2
  - `let html = ""` loaded in beforeAll() from DIST_INDEX — MUST NOT be reused for EC2
  - `function extractMetaContent(html, property)` — module-wide helper, safe to call

axe-core import (from tests/e2e/hub.spec.ts): `import AxeBuilder from "@axe-core/playwright";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/e2e/ec2-coming-soon.spec.ts</name>
  <files>tests/e2e/ec2-coming-soon.spec.ts</files>
  <read_first>
    - tests/e2e/hub.spec.ts (DIRECT template — 3 describe blocks: "Hub load", "Hub accessibility", "Hub responsive"; AxeBuilder usage; goto pattern)
    - src/pages/deep-dive-ec2/index.astro (the page under test — produced by Plan 01; confirm classes `.badge-coming-soon`, `.back-link`, `.skip-link` and the id `#conteudo-principal`)
    - playwright.config.ts (confirm baseURL `http://localhost:4321/` and that the spec will be auto-discovered by glob `tests/e2e/**/*.spec.ts`)
    - .planning/phases/08-multi-lp-scaffold/08-PATTERNS.md (tests/e2e/ec2-coming-soon.spec.ts Pattern Assignment, lines 153-246)
    - .planning/phases/08-multi-lp-scaffold/08-RESEARCH.md (Padrão 3 template, lines 293-357)
  </read_first>
  <action>
    Create `tests/e2e/ec2-coming-soon.spec.ts` with imports `import { test, expect } from "@playwright/test";` and `import AxeBuilder from "@axe-core/playwright";`. Define exactly 3 `test.describe` blocks: (A) `"EC2 coming-soon load"` containing 4 tests — `"GET /deep-dive-ec2/ returns HTTP 200"` (asserts `response?.status()` is 200 after `page.goto("./deep-dive-ec2/")`), `"<h1> Deep Dive EC2 is visible"` (locator `h1` is visible AND contains text `"Deep Dive EC2"`), `"badge EM BREVE is visible"` (locator `.badge-coming-soon` is visible AND contains text `"EM BREVE"`), `"back link to hub is present and points to /"` (locator `a.back-link` is visible AND its `href` attribute equals `"/"`); (B) `"EC2 coming-soon accessibility"` containing 2 tests — `"skip link has href='#conteudo-principal'"` (locator `a.skip-link` has attribute `href` equal to `"#conteudo-principal"`), `"no critical axe-core violations (WCAG 2.0 A/AA)"` (run `new AxeBuilder({ page }).withTags(["wcag2a","wcag2aa"]).analyze()`, filter `violations` where `impact === "critical"`, assert length 0); (C) `"EC2 coming-soon responsive"` containing 1 test — `"mobile 375x812: h1 visible, no horizontal overflow"` (set viewport 375x812, goto, h1 visible, evaluate `document.documentElement.scrollWidth > window.innerWidth` returns false). All `page.goto()` calls MUST use the relative URL `"./deep-dive-ec2/"` to match the Playwright baseURL config — do NOT use absolute URLs and do NOT use a leading-slash `"/deep-dive-ec2/"`. After writing, verify locally: `npm run build` then `npx playwright test tests/e2e/ec2-coming-soon.spec.ts --project=chromium` exits 0 with 7 passing tests on chromium (total 28 across the 4 projects, but chromium is sufficient for this task gate).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/ec2-coming-soon.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/ec2-coming-soon.spec.ts` exists
    - File contains exactly 3 occurrences of `test.describe(` (no more, no fewer)
    - File contains literal: `test.describe("EC2 coming-soon load"`
    - File contains literal: `test.describe("EC2 coming-soon accessibility"`
    - File contains literal: `test.describe("EC2 coming-soon responsive"`
    - File contains at least 4 occurrences of `page.goto("./deep-dive-ec2/")` (one per top-level test in describe A; plus uses in B and C)
    - File contains literal: `AxeBuilder` and `.withTags(["wcag2a", "wcag2aa"])`
    - File contains literal: `.badge-coming-soon` and `EM BREVE`
    - File contains literal: `a.back-link` and an assertion that its href equals `"/"`
    - File contains literal: `#conteudo-principal` (skip-link target)
    - File contains literal: `setViewportSize({ width: 375, height: 812 })`
    - `npx playwright test tests/e2e/ec2-coming-soon.spec.ts --project=chromium` exits 0
    - Chromium test count for this file is 7 (4+2+1)
  </acceptance_criteria>
  <done>ec2-coming-soon.spec.ts mirrors hub.spec.ts structure; 7 chromium tests pass; covers HTTP 200, h1, badge, back-link, skip-link, axe critical=0, mobile no-overflow.</done>
</task>

<task type="auto">
  <name>Task 2: Append test 16 to tests/seo/seo-meta.test.ts</name>
  <files>tests/seo/seo-meta.test.ts</files>
  <read_first>
    - tests/seo/seo-meta.test.ts (FULL file — confirm DIST_DIR declaration on line 19, module-level DIST_INDEX pointing to deep-dive-vm, module-level `let html`, extractMetaContent helper, AND the exact pattern of test 15 to mirror)
    - .planning/phases/08-multi-lp-scaffold/08-PATTERNS.md (test 16 addition section, lines 250-308; CRITICAL constraint about not reusing module-level variables)
    - .planning/phases/08-multi-lp-scaffold/08-RESEARCH.md (Pitfall 1 — silent false positive risk if DIST_INDEX or html reused)
    - dist/deep-dive-ec2/index.html (verify it exists after `npm run build` — produced by Plan 01)
  </read_first>
  <action>
    Append a new `it()` block after the existing test 15 inside the same describe block of `tests/seo/seo-meta.test.ts`. The new test MUST be titled exactly `"16. dist/deep-dive-ec2/index.html og:image points to ec2-og.png"`. Inside the test body, declare a LOCAL constant `const ec2IndexPath = join(DIST_DIR, "deep-dive-ec2/index.html");` (using the already-imported `DIST_DIR` from line 19 — do NOT reuse module-level `DIST_INDEX` which targets `deep-dive-vm`; do NOT reuse module-level `html` which was loaded from `DIST_INDEX` in beforeAll). Assert `existsSync(ec2IndexPath)` is true with a descriptive error message. Read the file into a LOCAL `const ec2Html = readFileSync(ec2IndexPath, "utf-8");`. Call `const ogImage = extractMetaContent(ec2Html, "og:image");` (helper already defined module-wide). Assert `ogImage` is truthy and that it contains the substring `"ec2-og.png"`. Do NOT add new imports — `readFileSync`, `existsSync`, `join` are already imported. Do NOT modify any other tests, do NOT modify `beforeAll`, do NOT modify the module-level `DIST_INDEX` or `html` variables. After writing, run `npx vitest run tests/seo/seo-meta.test.ts` and confirm all 16 tests pass (15 pre-existing + 1 new).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run tests/seo/seo-meta.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/seo/seo-meta.test.ts` contains literal string: `16. dist/deep-dive-ec2/index.html og:image points to ec2-og.png`
    - File contains literal: `const ec2IndexPath = join(DIST_DIR, "deep-dive-ec2/index.html")`
    - File contains literal: `const ec2Html = readFileSync(ec2IndexPath, "utf-8")`
    - File contains literal: `extractMetaContent(ec2Html, "og:image")`
    - File contains literal: `expect(ogImage).toContain("ec2-og.png")`
    - Test 16 body does NOT reference module-level `html` and does NOT reference `DIST_INDEX`: `grep -c "DIST_INDEX\|^\\s*html\\b" <test-16-body>` returns 0 (matches between `it("16....` and the matching closing `})`)
    - `npx vitest run tests/seo/seo-meta.test.ts` exits 0
    - Vitest reports exactly 16 tests in this file (15 prior + 1 new), all passing
    - No new `import` statement was added to the file (`git diff` shows changes only inside the describe block, not in the import region)
  </acceptance_criteria>
  <done>seo-meta.test.ts has test 16 appended; test verifies dist/deep-dive-ec2/index.html og:image contains ec2-og.png using LOCAL variables only; vitest run passes with 16 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → built dist/ | Tests consume HTML files produced by `npm run build`; no external network reads in unit/SEO tests |
| Playwright → preview server | Auto-started localhost; closed-network, no external input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | og:image assertion in test 16 | mitigate | Test asserts og:image content from compiled HTML — catches any silent tampering of ogImage prop. Test uses LOCAL variables (ec2IndexPath, ec2Html) so it cannot pass with deep-dive-vm content (Pitfall 1 silent false positive). |
| T-08-03 | Repudiation | E2E + SEO CI coverage | accept | Standard Playwright + Vitest run is captured by GitHub Actions logs; no additional logging needed for static site coverage. |
</threat_model>

<verification>
- `npm run build && npx playwright test tests/e2e/ec2-coming-soon.spec.ts --project=chromium` exits 0 (7/7 chromium tests pass)
- `npx vitest run tests/seo/seo-meta.test.ts` exits 0 (16/16 tests pass)
- `npm run test:all && npx playwright test` (full suite) exits 0 after both tasks complete
</verification>

<success_criteria>
- ec2-coming-soon.spec.ts created with 3 describe blocks and 7 tests; all pass on chromium
- seo-meta.test.ts test 16 appended using LOCAL path/html variables; vitest passes
- No module-level variables modified in seo-meta.test.ts
- Full suite green; no regressions in pre-existing 15 SEO tests or 40 hub.spec.ts E2E tests
</success_criteria>

<output>
Create `.planning/phases/08-multi-lp-scaffold/08-02-SUMMARY.md` when done.
</output>
