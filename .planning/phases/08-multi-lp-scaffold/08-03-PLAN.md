---
phase: 08-multi-lp-scaffold
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - HOWTO-new-landing-page.md
autonomous: true
requirements: [SCAFF-02]
tags: [documentation, howto, developer-experience]

must_haves:
  truths:
    - "HOWTO-new-landing-page.md exists at the repository root"
    - "Document contains 7 numbered steps from creating src/pages/[slug]/index.astro through deploy validation"
    - "Document references the EC2 route as the live, in-repo example produced by Phase 8"
    - "All file paths, commands, and snippets in the HOWTO are exact and runnable (no pseudocode)"
  artifacts:
    - path: "HOWTO-new-landing-page.md"
      provides: "Developer checklist for adding a new landing page"
      contains: "src/pages/[slug]/index.astro, public/[slug]-og.png, tests/e2e/[slug]-coming-soon.spec.ts, npm run build, deep-dive-ec2"
  key_links:
    - from: "HOWTO-new-landing-page.md"
      to: "src/pages/deep-dive-ec2/index.astro"
      via: "explicit reference as the live example"
      pattern: "deep-dive-ec2"
    - from: "HOWTO-new-landing-page.md"
      to: "tests/e2e/ec2-coming-soon.spec.ts"
      via: "referenced as the analog spec to copy"
      pattern: "ec2-coming-soon\\.spec\\.ts"
---

<objective>
Deliver SCAFF-02: `HOWTO-new-landing-page.md` at the repository root — a numbered, runnable checklist (7 steps) for adding a new landing page to the platform. The document references the EC2 route created in Plan 01 as the concrete, in-repo proof that the process works.

Purpose: Make the multi-LP pattern self-service. A future developer (or future Claude) follows the HOWTO to ship a new LP without re-deriving the pattern.

Output: `HOWTO-new-landing-page.md` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-multi-lp-scaffold/08-CONTEXT.md
@.planning/phases/08-multi-lp-scaffold/08-RESEARCH.md
@.planning/phases/08-multi-lp-scaffold/08-PATTERNS.md
@src/pages/deep-dive-ec2/index.astro
@public/ec2-og.png
@tests/e2e/ec2-coming-soon.spec.ts
@tests/seo/seo-meta.test.ts
@src/data/courses.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write HOWTO-new-landing-page.md (7-step checklist)</name>
  <files>HOWTO-new-landing-page.md</files>
  <read_first>
    - .planning/phases/08-multi-lp-scaffold/08-RESEARCH.md (Q5 — Passos canônicos do HOWTO, lines 469-506 — exact 7-step outline)
    - .planning/phases/08-multi-lp-scaffold/08-CONTEXT.md (D-04 — formato checklist numérico, escopo completo)
    - src/pages/deep-dive-ec2/index.astro (the live example created by Plan 01 — extract real prop values and CSS structure to cite as the analog)
    - src/data/courses.ts (the courses array structure — Step 2 references this file)
    - tests/e2e/ec2-coming-soon.spec.ts (Step 4 references this as the analog spec to copy)
    - tests/seo/seo-meta.test.ts (test 16 is the analog for Step 5)
    - public/CNAME (Step 7 deploy checklist mentions CNAME survival)
    - .planning/phases/08-multi-lp-scaffold/08-PATTERNS.md (HOWTO-new-landing-page.md section, line 311+, confirms no code analog — use RESEARCH Q5)
  </read_first>
  <action>
    Create `HOWTO-new-landing-page.md` at the repository root. Use a numbered Markdown structure with these 7 sections in order: (1) `## 1. Create src/pages/[slug]/index.astro` — instruct to copy `src/pages/deep-dive-ec2/index.astro` as the analog and substitute `slug`, `title`, `description`, `url`, `ogImage`; explicitly state that `ogImage` MUST have a leading slash (e.g. `/[slug]-og.png`) per Pitfall 3, and `noindex` and `jsonLd` MUST be omitted for coming-soon LPs; cite the exact file path of the EC2 example. (2) `## 2. Add entry to src/data/courses.ts` — instruct to append an object `{ title, description, url: '/[slug]/', status: 'coming-soon' }` to the existing courses array; cite that the hub renders the array automatically; note that EC2 is already declared as the live reference. (3) `## 3. Generate public/[slug]-og.png placeholder (1200x630)` — provide the EXACT sharp ESM script template (matching what was used for ec2-og.png and hub-og.png): import sharp, fileURLToPath, dirname/join from url/path; load `src/assets/claudio1.png`; call `.resize(1200, 630, { fit: 'cover', position: 'top' }).png().toFile('public/[slug]-og.png')`; include the verify command `node -e "import('sharp').then(s=>s.default('public/[slug]-og.png').metadata().then(m=>console.log(m.width,m.height,m.format)))"` (expected output `1200 630 png`); explicit instruction: DELETE the helper script after running — commit only the PNG. (4) `## 4. Create tests/e2e/[slug]-coming-soon.spec.ts` — instruct to copy `tests/e2e/ec2-coming-soon.spec.ts` verbatim and substitute the describe titles, `page.goto("./[slug]/")`, h1 text, and badge text where applicable; note the 3-describe-block structure (load, accessibility, responsive); use relative `./[slug]/` URLs (NOT absolute, NOT leading-slash) to match Playwright baseURL. (5) `## 5. Add SEO test to tests/seo/seo-meta.test.ts` — instruct to append an `it()` block after the existing test 16 following the same pattern; CRITICAL warning: declare local `[slug]IndexPath = join(DIST_DIR, "[slug]/index.html")` and local `[slug]Html` — NEVER reuse module-level `DIST_INDEX` or `html` (which point to deep-dive-vm and would produce silent false positives — Pitfall 1). (6) `## 6. Validate locally` — provide the exact commands: `npm run build`, then `npx playwright test tests/e2e/[slug]-coming-soon.spec.ts --project=chromium`, then `npx vitest run tests/seo/seo-meta.test.ts`, then `npm run test:all && npx playwright test` for the full suite; show expected exit codes (0) and how to interpret failures. (7) `## 7. Deploy checklist` — list: commit only the page, courses.ts entry, PNG, spec, and seo test addition (NOT the helper sharp script); push to main; verify GitHub Actions test workflow goes green; verify CNAME survives in dist/ (`grep mentoria.sertaoseracloud.com dist/CNAME`); verify the live route returns HTTP 200 via curl or browser. Conclude the document with a brief `## Live Example` section pointing readers to: `src/pages/deep-dive-ec2/index.astro`, `public/ec2-og.png`, `tests/e2e/ec2-coming-soon.spec.ts`, test 16 in `tests/seo/seo-meta.test.ts` — as the concrete proof these steps produce a working page. Anti-patterns section at the end listing the four Pitfalls from RESEARCH.md: (a) reusing module-level DIST_INDEX in SEO test, (b) ogImage without leading slash, (c) creating index.astro before generating PNG, (d) adding sitemap filter() to astro.config.mjs. Do NOT include fenced code blocks longer than ~12 lines — prefer compact snippets and references to the in-repo example files. Do NOT instruct to add the new route to `.lhcirc.json` or `.lighthouserc.json` (out of scope per RESEARCH Q6).
  </action>
  <verify>
    <automated>node -e "const{readFileSync,existsSync,statSync}=require('fs');const p='HOWTO-new-landing-page.md';if(!existsSync(p)){console.error('missing');process.exit(1)}const t=readFileSync(p,'utf-8');const must=['## 1. Create src/pages/','## 2. Add entry to src/data/courses.ts','## 3. Generate public/','1200, 630','## 4. Create tests/e2e/','./deep-dive-ec2/','## 5. Add SEO test','DIST_INDEX','## 6. Validate locally','npm run build','npx playwright test','npx vitest run','## 7. Deploy checklist','CNAME','deep-dive-ec2','ec2-og.png','ec2-coming-soon.spec.ts'];const miss=must.filter(s=>!t.includes(s));if(miss.length){console.error('MISSING:',miss);process.exit(1)}console.log('OK',statSync(p).size,'bytes')"</automated>
  </verify>
  <acceptance_criteria>
    - File `HOWTO-new-landing-page.md` exists at the repository root (NOT in `.planning/` or any subdirectory)
    - File contains all 7 headings: `## 1. Create src/pages/`, `## 2. Add entry to src/data/courses.ts`, `## 3. Generate public/`, `## 4. Create tests/e2e/`, `## 5. Add SEO test`, `## 6. Validate locally`, `## 7. Deploy checklist`
    - File contains literal references to the EC2 live example: `deep-dive-ec2`, `ec2-og.png`, `ec2-coming-soon.spec.ts`
    - File contains the sharp resize parameters: `1200, 630` (the canonical dimensions)
    - File contains the exact validation commands: `npm run build`, `npx playwright test`, `npx vitest run`
    - File mentions `CNAME` in the deploy checklist
    - File warns about the `DIST_INDEX` reuse anti-pattern (Pitfall 1)
    - File mentions the four anti-patterns from RESEARCH.md (DIST_INDEX reuse, ogImage missing leading slash, PNG-after-page ordering, sitemap filter)
    - File does NOT instruct adding the new route to `.lhcirc.json` or `.lighthouserc.json`
    - File contains at least one fenced code block (for the sharp script template) but no code block exceeds ~12 lines
    - Document size between 2000 and 15000 bytes (sanity bound — not empty, not bloated)
  </acceptance_criteria>
  <done>HOWTO-new-landing-page.md at repo root with 7 numbered steps, EC2 cited as live example, four anti-patterns documented, all required commands and file paths present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → developer | HOWTO is consumed by humans/Claude; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | HOWTO-new-landing-page.md | accept | Document contains only public repo-structure information already visible in git history. No secrets, env vars, or internal endpoints. |
| T-08-05 | Tampering (via misleading docs) | HOWTO instructions | mitigate | Acceptance criteria enforce literal presence of correct command strings and Pitfall warnings; future drift is caught by code review since the HOWTO references real file paths grep-able in the repo. |
</threat_model>

<verification>
- `test -f HOWTO-new-landing-page.md && echo OK`
- Automated grep checks confirm all 7 sections, EC2 references, anti-patterns, and required commands
- Manual semantic review by user during `/gsd:verify-work 8` confirms steps are coherent
</verification>

<success_criteria>
- HOWTO-new-landing-page.md exists at repo root with 7 numbered steps
- EC2 route is cited as the live, working example
- All four RESEARCH.md anti-patterns are documented
- Validation commands match the project's actual test runners (vitest, playwright)
- No instructions reference out-of-scope files (.lhcirc.json, .lighthouserc.json)
</success_criteria>

<output>
Create `.planning/phases/08-multi-lp-scaffold/08-03-SUMMARY.md` when done.
</output>
